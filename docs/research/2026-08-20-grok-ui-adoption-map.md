# Grok UI adoption map — lead's working summary (full report in the session transcript, 2026-08-20)
Mapper's corrections to the brief: (1) views/ NOT uniformly inert — 11 modules already compiled (completion_dropdown, file_search, history_search, modal_window, permission_presentation, prompt_suggestion, prompt_widget, slash_dropdown, status_bar, suggestion_controller, timeline; views/mod.rs:4-27); room already imports 3 of them. (2) Coupling lives in app/ DRIVERS, not components — most views/ files have 0 AppView refs; cost = write a driver.
KEY UNLOCKS (cheapest-first shortlist, verified file:line in full report):
1. Scrollback click hit-testing ALREADY COMPILED (entry_index_at_screen_row @ scrollback/state/layout.rs:280; hit_test_visible_block @ text_selection.rs:394; hit_test_selectable_range :315) — click-to-fold/click-message/click-chip = one mouse arm away. Grok DESIGNED click-to-fold (doc comment @ text_selection.rs:420-424); DisplayMode{Collapsed,Truncated,Expanded} + toggle_fold_selected already live (room_scrollback.rs:377).
2. Room's ONLY mouse handling today = 6 lines (app/room_runtime.rs:317-326, scroll + prompt); HitArea primitive must be COPIED (app/agent_view/mod.rs:273-357, 24 lines — inert, can't import). Idiom to adopt: render-returns-rects.
3. modal_window.rs COMPILED with zero callers — modal frame + title + [✗] + TAB BAR + click-outside-close free (render_modal_window :288, handle_modal_key :991, handle_modal_mouse :1036; ModalWindowState in render crate, re-exported). Tab mechanism = free; usage panel BODY = EXPENSIVE (4 data crates deleted) — take tabs, rebuild content.
4. input/mouse.rs (1451 lines) COMPILED UNUSED — full wheel/trackpad scroll normalization; room hardcodes scroll(3). Immediate feel win.
5. Bench picker: views/picker.rs (0 coupling, cfg flip; click-to-select :2676-2687, hover-preview, tabs, keyboard) + shortcuts_bar; chip-row miniature = welcome/workspace_mode.rs:197-352 (render→rects→hit-test→enum, 155 lines) + welcome/menu.rs (mount like room_logo).
6. "Jump to my last message" may need NO UI: ScrollbackState::jump_to_entry(EntryId) @ scrollback/state/nav.rs:147-155 (compiled) + track the id at send-time. views/jump.rs + overlay_list.rs = CHEAP 2-file flip if UI wanted.
7. Also cheap: timeline.rs (turn-rail beats scrollbar for interleaved agents), progress_bar.rs (zero deps). MODERATE: turn_status/agent_status chips (2-6 refs to strip), toast (re-author ~15 lines from notices.rs:325-333 pattern — no compiled primitive).
8. AVOID (EXPENSIVE): block_viewer (imports non-vendored xai_grok_workspace @ :15), agents_modal (foreign agent-definition model), modal.rs ActiveModal, btw_overlay.
LATENT DEFECTS (fix if touched): list_pane/state/mod.rs:16 broken `use crate::key;`; block_viewer.rs:15 non-vendored import. DEAD FILES: pager-render room_render.rs + room_terminal.rs mounted nowhere.
