# Review report — lens: test-integrity
(delivered by finder-test-integrity via SendMessage, 2026-08-16; saved verbatim by main — condensed only where noted)

**Document:** `D:\Zer0 Chat V2\.council\review-copy\standalone-v2-spec.md`

**Verdict up front:** the spec's central proof — the S1 oracle and its one falsifier — does not hold. The falsifier cannot be executed as described, the mechanism it claims is wrong, oracle step 1 is red on the baseline the spec says it will be green on, and one of the eight sub-gates in `gate-l5` is specifically blind to the exact mutation the falsifier uses. Twenty findings, twelve confirmed by execution.

- **F1 · spec:215-216 (§4.5 Falsifier) · important · CONFIRMED** — the falsifier cannot be run as written: `stageZer0V2Sidecar` (`scripts/package-zer0-v2-sidecar.mjs:34-38`, `:44-47`) runs `build:production` and rm+cp's `dist` into the staged sidecar, so a deletion in the staged sidecar has no window; deleting at source fails at step 1 (`scripts/copy-evidence-schema.mjs:23-32`, ENOENT during `npm run build`), not at step 3 as the spec asserts.
- **F2 · scripts/gate-l5-mandates.mjs:420-424 · important · CONFIRMED** — `gateSchemaDiscipline()` opens with `if (!existsSync(schemaPath)) return true;` — bites on corruption (EXIT 1 with a wrong version line), blind to deletion (EXIT 0).
- **F3 · gate-l5-mandates.mjs:247-249, :370-372, :422-424 · important · CONFIRMED** — G5, G6, G7 become permanent no-ops in the V2 tree (temporal barrel absent; no at-risk activities; schema gate returns true on absence). Executed against a tree with only `src/x.ts` + `src/x.test.ts` → EXIT=0.
- **F4 · spec:208 (§4.1) + :235 (S1) · important · CONFIRMED** — oracle step 1 as specified is red on the S0 baseline: `assertProductionJavaScript`'s regexes (`/\btsx\b/i`, `/\bts-node\b/i`, `/\.ts["']/i`) fail 30 of 182 dist files, all false positives from preserved comments (`.ts's`, `cockpit.tsx's`); the host entry itself passes; today the packager runs it on exactly two files.
- **F5 · spec:212 (§4.3) vs :105-107 (P6) and :132 (I5) · important · READ** — the oracle's `_schema_version` row-set `{14}` assertion and P6 (a journal row per close) are mutually exclusive on the same DB: room-host opens global mode → `{14}`; the digest opens memory mode (`digest-failsafe.ts:138` `openMemoryDb` → `db.ts:138-140`, lands at `{14,15}`); `journal_entries` exists only at v15 (`migrations-v15.ts:25`). After S6 the assertion races the detached child.
- **F6 · spec:108-109 (P7) and :242 (S7) · important · CONFIRMED** — P7 is satisfied by the empty set: default transport is ACP (`dispatch-acp.ts:40`; header "DEFAULT ON; ZER0_ACP=0 opts out"), so no PTY session is ever opened; opened/closed PID sets ∅ = ∅. P7 is non-vacuous only under `ZER0_ACP=0`, which decision 4 says is broken and defers to S10.
- **F7 · gate-l5-mandates.mjs:60-64 and src/chat/dispatch-pty.test.ts:18-20 · important · CONFIRMED** — `disposeAllPtySessions`' only real test is `src/cli/commands/chat-tui.test.ts:49-60` (dropped); the G1 exemption cites `dispatch-pty.test.ts` as the consumer test but that test `vi.mock("./pty-session-registry.js")` — it never executes the module. After extraction: zero coverage plus a standing exemption. Nit: 17 of 178 retained production files have no sibling test (all exempted); §3's "sibling test for every retained file" overstates by 17.
- **F8 · spec:152-153 (G-G, G-B) · important · CONFIRMED** — G-G and G-B are declared in the §2.4 table and scheduled nowhere (grep: each appears once, in the table only).
- **F9 · spec:153 vs src/memory/digest.test.ts:217 · important · READ** — MT3d proves watermark idempotence between two digest passes in one process; it says nothing about a host holding the DB open in WAL while a detached child runs the v15 migration. "Reuse MT3d" answers an unasked question.
- **F10 · spec:90-92 (P3) vs package.json:41 · important · CONFIRMED** — P3 drops four checks the current gates script runs: `gate-agent-files`, `gate-encoding`, `test:live`, `test:integration` ("vitest" names only the default pool).
- **F11 · scripts/generate-agent-files.mjs:15-17 vs spec §3 :166-167, §3b :194 · important · CONFIRMED** — `gate-agent-files` regenerates `CLAUDE.md` / `AGENTS.md` / `GEMINI.md` from `docs/agents/appendix.*.md` with an embedded checksum; the module map has no `docs/agents/`, no `CLAUDE.md`, no `GEMINI.md`, retains both scripts, and says AGENTS.md is "carried and rewritten" → the gate is red by construction; S3b's config-edit list never mentions the generator.
- **F12 · scripts/gate-encoding.mjs:76-84 vs S3b :238 · nit · CONFIRMED** — `trackedFiles()` shells to `git ls-files -z`, falls back to a directory walk on throw; in the no-git candidate it scans a different file set than in the repo (git exit 128 observed) — "gates green on the candidate" does not transfer for this gate.
- **F13 · scripts/gate-scripts-scope.mjs:33-38 · nit (preexisting) · CONFIRMED** — its only RETIRED_FIELDS token (`/\bchatMode\b/`) exists nowhere in the retained scripts (only in the gate itself, which excludes itself); on the V2 tree it is behaviourally `process.exit(0)`.
- **F14 · scripts/gate-clamps.mjs:9,92 and tsconfig.json:40 vs §3 :189-191 · important · CONFIRMED** — the five new scripts (`release`, `oracle-standalone`, `allow-list`, `docs-truth`, `reachability` .mjs) sit outside every quality gate the spec cites: gate-clamps walks only `src/` and `tests/` and only `.ts/.tsx`; tsconfig includes only `src/**`, `tests/**`; gate-scripts-scope is inert (F13).
- **F15 · spec:207-220 (§4) · important · READ** — the oracle proves packaging and handshake, not room behaviour: nothing asserts any table `schema.sql` defines (`db-drift.ts:45-48` is a "safe no-op on fresh DBs"); a ~60-line stub that writes a SQLite file with `_schema_version(14)`, compares `realpath(cwd)`, and echoes writes passes all six assertions. The live turn is manual, so the automated push-gate cannot distinguish "runs a turn" from "completes a handshake".
- **F16 · vitest.live-files.ts:4-19 vs §3 :186, S3b :238 · important · READ** — "tower/tui rows out" leaves row 18 (`tests/e2e/ghost-composer-conpty.test.ts` — boots the Ink CLI) in; a stale include row matches nothing silently, and the same array is the exclude in `vitest.config.ts:45`, so it silently stops excluding too — the exact drift the file's own header exists to prevent.
- **F17 · spec:114-116 (P10) and :238 (S3b) · important · READ** — P10's "resolve from a repo-root marker" is unreachable for the Rust half: the corpus enters via `include_str!("../../../../../protocol/...")` / `include_bytes!` (`conformance.rs:23`, `transport_protocol.rs:12-24`) — compile-time literal paths; no runtime marker resolution exists. Options: a `.cargo/config.toml` `[env]` root consumed via `concat!(env!(...))`, or runtime `fs::read` (loudness then depends on how it is written). The TS half already meets the spirit loudly (`room-protocol.test.ts:23-26` reads at module top level).
- **F18 · spec:229-230 (§5 process rule) · nit · READ** — "failing test first" has no meaningful red for S2/S3/S4 (a tag, two text files, a rewritten history) — either the rule is skipped for three of nine slices or satisfied by mirror-the-implementation tests; the spec does not say which.
- **F19 · knip.config.js:57-63 vs P4 :99 · nit (preexisting) · CONFIRMED** — `ignoreDependencies` excludes the two ACP bridges and `@openai/codex`; the "12 dependencies" count has a three-package blind spot.
- **F20 · knip.config.js:16,48-49,76-78 vs S3b :238, G-F :149 · important · READ** — "knip entry = host" is ambiguous: today's entry array ends with `src/**/*.test.{ts,tsx}!`; dropping those lines makes every retained test an unreachable file (`rules.files` "error"); repairing with an ignore glob makes test-only helpers read as unused. G-F claims "knip entry = host" as its mechanism while `reachability.mjs`'s "every table has a reader" has no defined implementation.

## Checks that came back clean
- Refusal test names its answerer: `cwd must match the host project root` at `zer0-v2-host.ts:508`; `requireRoomCwd` (`:499-509`) checks absolute → realpath → root-match with distinct messages.
- The Rust conformance harness is not vacuous (`conformance.rs:119`, `:137` panic; `:150-152` asserts `!cases.is_empty()`).
- A missing protocol corpus is loud on both sides (compile-time in Rust; module-top-level read in TS). 39 files under `protocol/`, all tracked in the outer workspace repo.
- Decision 6 checks out: `gate-no-claude-p.mjs:79-91` treats a missing tripwire file as a violation; the two rows the spec keeps (`src/adapters/registry.ts`, `src/chat/dispatch-headless.ts`) are in the closure; `agent-bins.ts` is not.
- "schema.sql not staged" already has a real killer test: `package-zer0-v2-sidecar.test.mjs:62-63` compares staged bytes + sha256 to source.
- P3's typecheck claim holds for the test tree (tsconfig include `src/**`, `tests/**`; dep-check runs `src tests`).
- No test in the retained suite mocks the module it tests (census over 161 files); `vi.mock` with a non-resolving path throws — G-G's stated exposure is mostly a loud failure mode; the wrong-reason passes actually found (F2, F3, F6, F13) are elsewhere.
- P6's append-only claim is store-enforced (`migrations-v15.ts:53-56` BEFORE DELETE trigger).
- No coverage percentage appears anywhere in the spec.

Commands executed for the CONFIRMED findings (read-only or scratch-dir only; no state-changing git command under `D:\Zer0 Chat V2`): `copyEvidenceSchema` against a temp root lacking `schema.sql` (F1); `gate-l5-mandates.mjs` against a scratch tree (F3), with a planted wrong / deleted `schema.sql` (F2); node walk of `dist/` applying the three regexes (F4, 30/182); `git ls-files -z` in a non-repo dir (F12, exit 128); grep census of `vi.mock` and sibling tests (F7 nit); grep of `chatMode` across `scripts/` (F13).
