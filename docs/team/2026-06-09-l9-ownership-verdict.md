# L9 Ownership Verdict — zer0 chat (2026-06-11)

**Author:** incoming L9 lead (ownership pass; prior artifacts are inputs, not law). Every decision reversible; reversal conditions named.
**Read:** PRD v0.2 · TEAM.md v0.2 · schema v13 · SYNTHESIS (team-meeting-2026-06-09) · 3× turbocharge research · final-architecture rev-2 · dispatch-headless.ts · schema.sql.

## A. VERDICT per artifact

**PRD v0.2 — KEEP-WITH-EDITS.** The reality note (§0) mapping every §4.1 criterion to today's gap is the single most honest section in the stack, and §4.1 is genuinely binary. Two edits: (1) the PRD is one rev behind its own review — SYNTHESIS "PRD/TEAM deltas to fold on approval" (Action Broker named as the §4.1 artifact, cold-session reviews, per-session FIFO, quota reader as build item, falsify-first rephrase) are not folded; fold them as the first doc commit. (2) §10 carries open questions into build week — they are closed below (D1–D4); delete §10 and reference this register.

**TEAM.md — KEEP-WITH-EDITS.** §1.7 gate topology is the best thinking in the system and §2/§3.7 are compilable. Edits: (1) internal contradiction — §3.6 marks `--setting-sources` "✅ RESOLVED" while §4 checkbox 1 still lists it open (turbocharge-claude.md resolves it VERIFIED); delete the checkbox. (2) §3.5 activation theory is research prose inside a constitution — the compiler needs to know what COMPILES; tag each section `[PACK]` or `[REFERENCE]`. (3) Evolution loop (§3.6) is half-specified — see C3.

**Schema v13 — KEEP.** The strongest artifact: approvals-as-VIEW over the proven tower triple (decision 1) is exactly the anti-two-stack-drift call the 2026-05-28 audit demanded; the red-team folds are in the DDL (spawning-row-before-spawn, pid+start-time tuple, FIFO partial index, terminal-immutability triggers, chain/hop guards); the wall by TYPE CONSTRUCTION with an auditable persisted blob (§4) is the L9 move. Honest UNVERIFIED list and ±30% LOC estimate. Build from this doc as written.

**Build order rev-2 — KEEP-WITH-EDITS (one swap + one insert).** Item 0 (claude `-p` retirement) and item 1 (Temporal swap) confirmed — both verified real: `src/adapters/claude.ts:21` (`PROMPT_FLAG = "-p"`) is the DEFAULT product path via `dispatch-headless.ts:57-58` (registry unless `ZER0_PTY=1`), and `src/temporal/server.ts:10,49` runs `TestWorkflowEnvironment.createLocal`. Swap: single write path moves AHEAD of persistence+reattach (D8). Insert: the single-instance lock is load-bearing for the schema's whole concurrency story (schema §1 "races excluded by the PRD single-instance lock") yet NO build item creates it — add it to the broker work package.

## B. DECISION REGISTER

**D1 codex rate-limit downgrade — ADOPT the split (chat: auto-accept + persistent model badge; build: approval event).** A chat downgrade is reversible and visible (badge reads `agent_sessions.model`); a build downgrade changes WHO does supervised work mid-task — identity changes inside an approval-governed flow route through the broker like any other action. Enforcement at the pty layer via dialog frame signature, never output-text matching (red-team #5 — current auto-`\r` fires on an agent QUOTING the phrase). **Reverse if** build-downgrade cards fire >1/week and are 100% approved → collapse to global auto-accept+badge.

**D2 read-only auto-allow — ADOPT effect-classified per transport, fail-closed.** app-server: structural (JSON-RPC method knowable); pty: only mechanically knowable action types; everything unclassifiable = pending row. `decided_by='auto_read_only'` (schema §1.3) keeps the audit trail. **Reverse if** the stats view (D-missing #3) shows the operator rubber-stamping pty reads <2s → invest in pty classification, never in loosening the default.

**D3 gemini post-EOL — ADOPT pin + degrade, but INVERT the framing: degrade is the PRIMARY plan.** The EOL is server-side — Google "stop[s] serving Google AI Pro… requests on 2026-06-18" (turbocharge-gemini, official blog); a pinned binary does not guarantee a served backend, so treat 06-18 as a probable hard lane outage in 7 days. Every formation in TEAM.md §3.7 must be runnable gemini-absent; invest only in verified-transfer surfaces (skills, GEMINI.md). **Reverse if** Antigravity CLI ships a subscription automation path → new transport behind the seam.

**D4 tower-gemini ACP — DELETE NOW (overruling PRD §10-D2's "keep till EOL").** "Don't delete working code days before EOL" is sunk-cost reasoning: the path is the sole ACP transport, leaks ACP vocabulary (codex R4), and is a seventh bypass surface the write-path gate test must otherwise cover for 7 days of life. The v13 `lane_id` XOR check already treats it as legacy attribution. **Reverse by** git revert if a supervised gemini need appears before 06-18 (pty lane covers it — md-only regardless).

**D5 Team Pack delivery — three calls.** (a) Static-first CONFIRMED (validate content before compiler — PRD Q4 stands). (b) Claude carrier: pack rides `--append-system-prompt-file` on every zer0-spawned claude (system-prompt level beats CLAUDE.md's user-message slot — turbocharge-claude §3, mechanism VERIFIED, magnitude unbenchmarked); project CLAUDE.md keeps only repo-engineering content. (c) Collision (AGENTS.md/GEMINI.md/CLAUDE.md/ROUND_TABLE.md all exist at root — verified): TEAM.md is the ONE genome; root files get fenced generated sections (build-pillar + chat-pack blocks, provenance headers); ROUND_TABLE.md is RETIRED as a generator, content folds into TEAM.md. One file per agent surface — parallel namespaced files recreate two-stack drift; codex's 32KiB `project_doc_max_bytes` makes one AGENTS.md fine. **Reverse if** fenced merge produces observable instruction conflicts → move chat-pack to skill/TOML surfaces.

**D6 builder-authors-own-brief — ADOPT AS DEFAULT with a kill metric.** Public precedent is weak (gemini), but our empirical base is the observed dumb-executor degradation (TEAM.md §1.7) and the topology already contains the safety: the brief is attacked cross-family BEFORE code, so a blind self-brief dies at the brief gate, not in the diff. **Kill metric (the reversal):** over the first 10 builds, if brief-gate BLOCK rate on self-authored briefs >2× the dispatcher-authored historical rate, OR 2 builds ship P0s traceable to self-brief blindness that the gate missed → revert to dispatcher-authored briefs for infra-shaped tasks.

**D7 cold-session reviews — ACCEPT the tax, SCOPED to brief-gate + code-gate.** Those are the two checkpoints where contamination = sycophantic convergence (red-team #6/#7); research synthesis and Shape-A outputs are coordination-plane by definition and may run warm. The boot cost ≈ what dispatch.sh already pays per review today; warm reviews save seconds and spend the moat. **Reverse if** review p50 latency from boot tax alone exceeds ~3 min → allow warm-but-walled sessions for P2-only re-reviews.

**D8 build order first three — REORDER: (0) `claude -p` retirement + grep-gate, (1) Temporal `start-dev --db-filename` swap, (2) single write path (Action Broker + v13 + single-instance lock).** Item 0 is the only dated item and Monday IS 2026-06-15 — it lands THIS WEEK, before Monday. The Temporal swap is a verified half-day (`server.ts:49`) killing the observed sleep-corruption class. The broker is promoted above persistence+reattach because SYNTHESIS convergence #1 names it THE central item, every pre-broker feature adds bypass surface (codex found six), and schema §1.3's `lane_id` XOR `agent_session_id` CHECK explicitly permits broker-before-registry; persistence then dispatches THROUGH the broker from day one. **Reverse if** the broker rewire proves to need session identity the XOR cannot carry — the schema says it won't.

## C. THE THREE WEAKEST THINGS

**C1 — The wall is celebrated as fixed while 2 of its 5 channels stand open.** The red-team's own finding: a reviewer observes prompt, session memory, filesystem, git, recall. WakeUpReview-by-type closes prompt+recall; cold sessions close session memory; but a cold reviewer with repo read can still read the author's reasoning from the FILESYSTEM and GIT (`.council/runs/`, spec drafts, project-state.md) — and the §4 falsifying test greps only the persisted prompt blob. **Fix:** review dispatches run in a clean worktree at base SHA + diff applied, with `.council/`, `docs/research/` drafts, and `project-state.md` excluded; the gate test asserts against the review WORKTREE contents, not just the blob.

**C2 — The economics layer is fictional while the whole operating model leans on it.** TEAM.md §3.7 prices every formation in codex cost; the PRD's origin story is two weekly-bucket burnouts; yet codex R5 stands: NO production bucket reader exists (`capacity-snapshots.ts:123` hardcodes success) and the reservation ledger is "in-memory + journaled" — vapor. **Fix:** quota reader + reservation ledger becomes a named build item immediately after the broker, before any app-server work, with a falsifying test (dispatch against a stale snapshot must warn/block).

**C3 — The genome has no fitness function.** TEAM.md §3.6's evolution loop says "winners graduate by edit," but `lessons` (schema §1.6) carries no experiment id, variant, baseline, or outcome field — you cannot graduate winners you never measured, and at n=1 noisy fitness the loop degenerates into vibes-based constitution edits. **Fix:** either add `experiment_id/variant/outcome` columns to `lessons` v13 now, or delete the evolution-loop section and call TEAM.md edits what they are — judgment. The half-spec is worse than either.

## D. WHAT'S MISSING ENTIRELY

**M1 — Operator-data disaster recovery.** SQLite is the spine and temporal.db corruption-on-sleep is already an observed incident, yet no backup/restore path exists. One page + one script: nightly file-copy of the evidence DB, `zer0 doctor`, and the written "DB lost → rebuild T1/T3 from T2 files + git" procedure — the 3-tier brain almost guarantees this; nobody wrote the rebuild.

**M2 — Definition-of-done per build-order item.** PRD §0 maps criterion→closing item; the inverse is missing: a 10-line table (build item → falsifying test → exit evidence, e.g. "persistence done = crash-relaunch ×3 + sleep/wake smoke green"). Without it, "done" reverts to vibes at exactly the moments the HARD RULE (real E2E per phase) was written to prevent.

**M3 — Team-level metrics (`zer0 stats`).** Approvals/hour, deny rate, time-to-decision, dispatch failure rate, quota burn per task class, BLOCK rate per gate. D2's fatigue trigger, D6's kill metric, D7's latency reversal, and C3's fitness function ALL read from this; it is one SQLite view + one command at this scale, and without it every reversal condition in section B is unmeasurable.

## E. THE SHIP CALL

**GREEN — with one dated exception that cannot wait for Monday:** the `claude -p` flag-flip + grep-gate lands before 2026-06-15 (Monday), or every default-path chat turn starts billing the metered pool. The schema is build-ready, the decisions above close every open item, and the build order needs only the D8 swap.

**Wiki sentence:** _Build the broker first and route every byte of agency through it — everything else in this system is furniture around that one door._
