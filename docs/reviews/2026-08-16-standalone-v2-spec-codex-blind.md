| # | Sev | Lens | Claim/Finding | Evidence (file:line, command + output) | Consequence for the operator | Fix (only if verified at source) |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | P0 | plan-logic / test-integrity | **OBSERVED → INFERRED:** The oracle’s required migration set is impossible on the real `session/new` path. | **OBSERVED:** `standalone-v2-spec.md:128,210-212` requires `{14}`. `src/room/room-host.ts:116-132` calls `initializeRoomSession`; `src/room/room-host-support.ts:30-48` calls `initCarrierRuntime`; `src/chat/lane-transport.ts:115-128` opens the lane DB. `src/evidence/migrations-v16.test.ts:67-76` asserts `14,15,16,20`. | **INFERRED:** S1 and every later fresh-clone proof stop red even when the host is correct. | **OBSERVED:** Assert `{14,15,16,20}` after a real `session/new`; `{14}` is valid only for the narrower `openDb` path. |
| 2 | P0 | plan-logic / gate-audit | **INFERRED:** The positive oracle’s “fresh temp project root” is not constructible unless it is initialized as a Git repository. | **OBSERVED:** `standalone-v2-spec.md:210` omits Git initialization. `src/memory/project-scope.ts:149-165` returns `unscopable` outside Git; `src/chat/boot-liveness.ts:82-98` then has no project ID or lock; `src/room/room-host.ts:121-123` throws. The existing production-path test explicitly runs `git init -q` at Rust-ref `crates/zer0-v2-bin/tests/host_lifecycle.rs:356-368`. | **INFERRED:** Literal implementation fails before schema or RPC assertions run. | **OBSERVED:** Run `git init -q` in the oracle project root before spawning the host, matching the existing lifecycle test. |
| 3 | P0 | docs-truth / gate-audit | **OBSERVED → INFERRED:** G-E requires a fresh clone to resolve absolute paths that exist only in the old machine workspace. | **OBSERVED:** `standalone-v2-spec.md:5-8` embeds `D:\Zer0 Chat V2\...`; `:148` says G-E resolves every path in “this spec”; `:207` enrolls it in gates and CI; `:243` runs those gates from a clean clone. | **INFERRED:** G-E must either fail on another machine or silently exempt claims despite promising to resolve every path. Both invalidate the fresh-clone proof. | **UNVERIFIED:** `scripts/docs-truth.mjs` does not exist, so no source-backed exception or portable provenance mechanism can be verified. |
| 4 | P0 | docs-truth | **OBSERVED:** The result map retains the generated-agent gate while omitting required paired inputs and outputs. | **OBSERVED:** Result map `standalone-v2-spec.md:162-187` includes only root `AGENTS.md`, omits `docs/agents`, `CLAUDE.md`, and `GEMINI.md`, but retains both scripts. `scripts/generate-agent-files.mjs:14-19,43-58` reads `core.md` plus three appendices and reads/writes all three targets; `scripts/gate-agent-files.mjs:8-15` invokes that check from `package.json:31,41`. | **INFERRED:** `npm run gates` throws on missing inputs or targets; allowing S3 to add them instead makes the authoritative result map false. | **OBSERVED:** Carry `docs/agents/core.md`, all three appendices, and `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`; alternatively trim the generator and gate in the same slice. |
| 5 | P1 | test-integrity / docs-truth | **OBSERVED → INFERRED:** The oracle does not define its write, read, or expected value, and its “spawns no agent” claim is false under default configuration. | **OBSERVED:** `standalone-v2-spec.md:210-219` says only “one write and one read.” Available methods are enumerated at `src/room/zer0-v2-host.ts:327-373`. After `session/new`, `:317-323` calls `activateRecovered`; `src/room/room-host.ts:176-181` starts eager sessions; `src/memory/memory-flags.ts:19-60` defaults the carrier on. | **INFERRED:** A schema-presence-aware canned stub can pass, while the real gate may start external ACP processes and depend on installed agent CLIs. | **OBSERVED:** `ZER0_MEMORY=0` or `ZER0_NATIVE_RESUME=0` prevents eager agent warm-up. **UNVERIFIED:** no exact source-backed write/read assertion is specified. |
| 6 | P1 | reachability | **OBSERVED:** G-F’s “every table has a reader” condition cannot pass honestly while the retained schema remains unchanged. | **OBSERVED:** `standalone-v2-spec.md:149,178,248-250` retains the schema and defers trimming. A closure scan returned `requirements 0`, `capacity_snapshots 0`, `tournament_results 0`, `agent_failure_patterns 0`; definitions are `src/evidence/schema.sql:21,192,208,219`. | **INFERRED:** A semantic reachability gate stays red; a name-only grep passes while certifying unreachable persistence surfaces. | **UNVERIFIED:** Removing or retaining these tables requires a migration/consumer decision not present in the specification. |
| 7 | P1 | reachability / plan-logic | **OBSERVED:** The result retains `agent-prompt-stack`, its test, and a relocated fixture after deleting its only production caller. | **OBSERVED:** `standalone-v2-spec.md:175,179-180,237` removes `prompt-builder.ts` but retains its closure and relocates the adversarial fixture. `rg -n 'agent-prompt-stack' src -g '*.ts'` returned only `agent-prompt-stack.ts`, its test, and the production import in `prompt-builder.ts:28`. | **INFERRED:** The “only what V2 needs” repository contains an orphan feature specifically preserved by the extraction plan. | **OBSERVED:** Omit the module, test, and fixture unless the result map names a new production caller. |
| 8 | P1 | plan-logic | **OBSERVED:** Slice postconditions are temporally impossible. | **OBSERVED:** P11 requires digest watermark/content at `standalone-v2-spec.md:117-119`; S5 claims P11 at `:240`; digest wiring is later S6 at `:241`. S7 says README prerequisites come from S8 output at `:242`, while S8 follows at `:243`. | **INFERRED:** S5 and S7 must be reopened and rerun; their declared completion evidence cannot exist when claimed. | **OBSERVED:** Put S6 before the P11/S5 proof, and derive/update the prerequisite matrix before declaring S7 complete. |
| 9 | P1 | plan-logic / reachability | **OBSERVED:** S6 does not define a constructible lifecycle owner for digest catch-up or loaded sessions. | **OBSERVED:** `DigestRequest` and `bootCatchUp` require `projectId` at `src/memory/digest-runner.ts:20-25,138-151`. JSON-RPC `initialize` only flips a Boolean at `src/room/zer0-v2-host.ts:241-247`; project identity is local to `AliveRoomHost.create` at `src/room/room-host.ts:116-132`. S6 creates attached state only at `session/new` (`standalone-v2-spec.md:241`), while production also attaches through `session/load` at `zer0-v2-host.ts:306-314`. | **INFERRED:** Boot catch-up cannot run where specified, and a loaded room can close without a complete digest request. | **UNVERIFIED:** The spec must choose an identity owner before implementation; current source exposes identity through `carrierRuntime()` only after room initialization. |
| 10 | P1 | gate-audit | **OBSERVED:** The planned gates lack collector floors, coverage of their own critical scripts, and a clean-after-generation condition. | **OBSERVED:** `scripts/gate-clamps.mjs:8-11,71-79` walks whatever `src/tests` remain; running it returned `GATE PASS: 1138 files checked, 187664 lines, no violations`. `tsconfig.json:40-41` and `.dependency-cruiser.cjs:171-172` exclude `scripts`; `standalone-v2-spec.md:207,237-238` states no minimum counts or `git status --porcelain` postcondition. | **INFERRED:** A truncated collector, broken oracle script, or stale generated artifact can remain green. | **UNVERIFIED:** Candidate-specific floors and script validation cannot be set before the candidate tree and new scripts exist. |
| 11 | P1 | gate-audit / test-integrity | **OBSERVED:** CI success and test-integrity semantics are under-specified. | **OBSERVED:** Current `.github/workflows/gates.yml:45-69` neither runs `npm run gates` nor `test:live`, and explicitly skips the real CLI smoke. `standalone-v2-spec.md:207,229-239` does not require a terminal dependency fan-in, merge-result run, or test-file diff/reason for deletions. | **INFERRED:** A skipped or unenrolled check can look green, and Rust manifest pruning can remove tests without a blocking record. | **UNVERIFIED:** The future workflow and branch-protection configuration do not exist. |
| 12 | P1 | gate-audit / test-integrity | **OBSERVED:** The current Rust “actual Node” lifecycle proof is excluded from the stated `cargo test --locked`, and its enabled path targets the old sibling checkout. | **OBSERVED:** Rust-ref `host_lifecycle.rs:1` is `cfg(feature = "test-support")`; `zer0-v2-bin/Cargo.toml:8-18` excludes that feature from defaults. When enabled, `host_lifecycle.rs:411-439` appends `zer0-agent-ci` instead of using the final monorepo root. | **INFERRED:** Default Cargo tests can be green without exercising the shipped host; enabling the omitted proof in the new layout fails at the old path. | **OBSERVED:** The final layout requires dropping `.join("zer0-agent-ci")`. **UNVERIFIED:** enabling the test also requires reconciling pruned dev-dependencies. |
| 13 | P1 | gate-audit / risks | **OBSERVED → INFERRED:** The retained packager can delete an existing output child, while S7 specifies no output ownership or old-workspace guard. | **OBSERVED:** `scripts/package-zer0-v2-sidecar.mjs:32-46` resolves caller input and recursively removes `<output>/zer0-v2-node`; `standalone-v2-spec.md:122-124,242` requires an old-workspace refusal but names no release-output validation. | **INFERRED:** A colliding output path can lose data, including when an operator cannot inspect the command implementation. | **UNVERIFIED:** No ownership marker or guarded release implementation exists to validate. |
| 14 | P1 | plan-logic / trust boundaries | **OBSERVED:** The core S0-S8 delivery knowingly precedes the required external-input validation. | **OBSERVED:** `standalone-v2-spec.md:135-143,243-246` records unvalidated JSON-RPC, env, argv, and JSON files and places Zod hardening after S8. `scripts/digest-run.ts:97-115` accepts malformed invocation by silently returning success. | **INFERRED:** The fresh-clone/open-source milestone violates the operator’s non-negotiable trust-boundary and user-feedback requirements and needs a separate hardening rebuild. | **OBSERVED:** Move S11 before any S8 completion claim; Zod is already a retained dependency. |
| 15 | P1 | plan-logic / quality invariants | **OBSERVED:** The result explicitly defers structured correlated logging, recoverable errors, cursor pagination, and the 250-line ceiling. | **OBSERVED:** Deferral is explicit at `standalone-v2-spec.md:190-191,248-250`. Current counts are `zer0-v2-host.ts 590`, `room-host.ts 596`, `db.ts 321`. `zer0-v2-host.ts:257-289` rejects cursors and truncates to the first page; `zer0-v2-rpc.ts:221-223` emits a raw stack; `package-zer0-v2-sidecar.mjs:168` uses `console.error`. | **INFERRED:** The delivered repository violates four stated non-negotiables; sessions after the fixed first page become inaccessible and failures lack recovery guidance/correlation. | **OBSERVED:** These items must move before S8. **UNVERIFIED:** no decision-complete module split, pagination contract, or logging contract is specified. |
| 16 | P1 | plan-logic / postconditions | **OBSERVED:** P8’s proposed `git status` plus tree-ID proof can pass while the old D: workspace changes. | **OBSERVED:** P8 is defined at `standalone-v2-spec.md:110-111`. Command output for a real ignored file was `Example=.cache/conpty-input-probe...`, `CheckIgnore=.gitignore:4:.cache/`, `Tracked={}`; ignored/untracked bytes are absent from both the tree ID and ordinary porcelain status. | **INFERRED:** Mutation or deletion under ignored `.cache`, `.council`, or similar paths remains invisible to the operator’s protection artifact. | **UNVERIFIED:** No complete before/after manifest or protected-path hashing mechanism is specified. |
| 17 | P2 | docs-truth / plan-logic | **OBSERVED:** Two §1 facts are wrong at their cited resolution. | **OBSERVED:** `standalone-v2-spec.md:44` says 39 protocol files. Command output: `{"PhysicalFileCount":38,"GitTrackedCount":38}`. `standalone-v2-spec.md:35-36` locates the one-line wrapper at `prompt-budgeter.ts:33`; that line defines `estimatePromptTokens`, while the wrapper is `prompt-builder.ts:83-84`. | **INFERRED:** G-E is born red against its own authoritative document, creating avoidable argument over whether the gate or map is correct. | **OBSERVED:** Correct the corpus count to 38 and cite `src/chat/prompt-builder.ts:83-84` for the wrapper. |
| 18 | P2 | docs-truth / allow-list | **UNVERIFIED:** The allow-list algorithm does not explicitly define type-only edges, and the result map overstates the sibling-test rule. | **OBSERVED:** S3 says only “static imports (closure-181)” at `standalone-v2-spec.md:237`; `scripts/allow-list.mjs` is absent. Result-map `:179` requires a test for every production file, but current `scripts/gate-l5-mandates.mjs:39-41` exempts type-only `types.ts`, and `src/chat/types.test.ts` does not exist. | **INFERRED:** Two implementers can produce different valid-looking allow-lists, or G1 can disagree with the map. | **OBSERVED:** State that `import type` edges are included and carry the existing type-only-file exemption. |
| 19 | P2 | plan-logic / postconditions | **OBSERVED:** The document calls all postconditions machine-checkable while P2 includes manual operator acceptance; I2 also conflicts with the positive proof’s writes. | **OBSERVED:** `standalone-v2-spec.md:82` makes the blanket claim; P2 at `:86-89` requires a live three-agent turn, while `:218-220` calls it operator acceptance. I2 at `:124-125` says every verification opens the evidence DB read-only, but the positive oracle at `:210-212` creates, migrates, and writes its temp DB. | **INFERRED:** Completion can be disputed because automated and manual evidence—and old versus proof-owned databases—are not separated. | **OBSERVED:** Split automated artifact proof from manual acceptance and narrow I2 explicitly to pre-existing source evidence databases. |

**Empty severity bands:** **OBSERVED:** P3 is empty; P0, P1, and P2 contain findings.

**VERDICT: REJECT** — **INFERRED:** the specification’s first mandatory oracle is internally red, its docs gate is non-portable, and its authoritative result map cannot pass its retained generator gate.

**the single biggest risk:** **INFERRED:** the operator receives a green-looking “standalone” proof whose oracle and gates either cannot run from a fresh clone or can pass without exercising the shipped Rust-to-Node-to-database path.

**the one thing to reorder or add:** **OBSERVED → INFERRED:** make S1 a fully executable packaged-artifact oracle contract—Git-initialized root, `{14,15,16,20}`, exact RPC/readback values, agent warm-up disabled—and require it to pass before allow-listing or history surgery.

**what you could not determine:** **UNVERIFIED:** no candidate tree or proposed oracle/release/gate scripts exist, and the read-only sandbox prevented a fresh Windows install. The settling run is a clean Windows VM executing `npm ci`, `npm run gates`, `cargo metadata --locked`, `cargo test --locked`, `npm run release -- <empty-dir>`, the packaged positive/negative/falsifier oracle, a live turn, and final `git status --porcelain`.

**confidence:** **INFERRED — YELLOW (94%):** all shipping-path findings above resolve against pinned source lines, but candidate-only behavior, native-module installation, and clean-machine tooling remain unexecuted.

**checks you ran that came back clean:** **OBSERVED:** Node ref `106d5ea775d4eaa2e617fcc0207a213e0798e824` and Rust ref `82d25249cfd28ca721eca1b1d4355c75ac4cb078` both ended with `StatusCount=0`; the closure is 181 lines and 181 unique paths with no unresolved relative imports; the Rust `.d` resolves to 292 Rust files and 19 package roots; all four retained `prompt-builder` callers use only `estimateTokens`; the real submit path traces Rust command → Node RPC → durable message → visible events; the cwd refusal names the answering rule exactly; the retained closure contains zero `as any` and zero `@ts-ignore`; the inspected third-party licence chains and paired protocol consumers resolved; neither prohibited reviews directory was read.