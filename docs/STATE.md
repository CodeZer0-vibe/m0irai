# m0irai — STATE

Single source of truth for **what has been proved, in what order**. Every row is derived from a real command's output (exit code, counts, hashes). Roots are written as `OLD` (operator workspace, read-only), `WORK` (this cutting repo), `TEMP` (disposable baseline/verification roots), `FINAL` (single-commit export). This file never quotes prompts, replies, credentials or home paths, and never cites the tree id of the commit that contains it (that lives in the external verification receipt).

Plan: `docs/specs/2026-08-17-m0irai-standalone-plan-v5.md` (final plan; findings land here, never in a v6).

**Overall status: INCOMPLETE** — the newest position is `## POSITION 2026-09-08` at the end of this file; the ordered next-action list is the TOP block of `.lead/RESUME-2026-08-21-AFTERNOON.md` (outside the tree).

---

## Phase 0 — baseline (2026-08-17) — GATE: PASS

Plan: `docs/specs/2026-08-17-m0irai-standalone-plan-v5.md` §6 Phase 0. Baseline root: `TEMP` = `D:\m0irai-baseline` (disposable; layout mirrors OLD: `sources/zer0-agent-ci`, `sources/grok-build`, `protocol/`). `OLD` = `D:\Zer0 Chat V2` was only read (`git clone` from its object stores, `git archive` of the outer commit; two vitest files were executed inside `OLD/sources/zer0-agent-ci` to reproduce K1 — temp DBs only, `git status` 0 lines before and after).

### 0.1 OLD state (before) — `phase0/old-before/summary.txt`
| repo | HEAD | tree | status lines |
| --- | --- | --- | --- |
| outer `D:\Zer0 Chat V2` (master) | `168037373aeae8d7f315a9011ec22367271890aa` | `f3833b29617ac73a49d3fb15178dbbe458d39354` | 51 (live runtime files; ls-tree sha `b2c690922c512c0e`) |
| node `sources/zer0-agent-ci` (codex/v2-room-layer) | `31bd3eaf458b44eebcdab70d6cc2dd1ec1bdc666` | `eb65b32a379f2e9ad3b82684d27c6129baa160f5` | 0 |
| rust `sources/grok-build` (zer0-v2/alive-room-20260801-161536) | `fd2be4fd32c661b781e8517d988cbd42651a14ce` | `324822f77b9da19e3e76b8715f117b2a40dfe7f8` | 0 |
| rust worktree `grok-build-b13-candidate` | `c44391f6ab0223ce26d759584fbb065c21a8f745` | `81717079a6448b0094fcb547c58abbfa6a1dca76` | 0 |

Pins verified: Rust `82d25249cfd28ca721eca1b1d4355c75ac4cb078` is a commit whose tree equals the worktree's (`81717079…`); protocol subtree at outer `1680373` = `b13faf2ca6a7b33f8ff268059123883e5aaec0b6`, live `protocol/` clean (0 status lines); Node pin = `31bd3ea` (HEAD; identical to `106d5ea` outside `docs/`).

### 0.2 Baseline clones
- `sources/zer0-agent-ci` = `git clone` of OLD node repo, detached at `31bd3ea`, 1,883 tracked files.
- `sources/grok-build` = `git clone --no-checkout` of OLD `grok-build`, detached at `82d2524`, 3,166 tracked files; `.cargo/config.toml`, `rust-toolchain.toml` (1.94.0), `Cargo.lock` present.
- `protocol/` = `git -c core.autocrlf=false -c core.eol=lf archive 1680373:protocol | tar -x`, **39/39 files hash-verified against their blobs** (`git hash-object` == ls-tree blob id).

### 0.3 Node baseline (`phase0/node/steps.tsv`, logs beside it) — Node v24.18.0, npm 11.16.0
| step | rc | s | note |
| --- | --- | --- | --- |
| `npm ci --no-audit --no-fund` | 0 | 66 | 1,121 packages; postinstall `patch-lifecycle.mjs` → "zer0 patch receipt verified (3 patches)". **`better-sqlite3@11.10.0` compiled from source with MSVC** (no prebuilt for Node 24 / ABI 137; `build/Release/*.iobj,.pdb,obj/`); `node-pty@1.1.0` used its shipped `prebuilds/win32-x64`. npm `allow-scripts` warnings are advisory (scripts ran). |
| `npm run build` | 0 | 13 | |
| typecheck / biome | 0 / 0 | 39 / 4 | |
| gate-agent-files, gate-clamps, gate-encoding, gate-no-claude-p, gate-patches, gate-scripts-scope, gate-l5 | 0 ×7 | ≤4 each | |
| dep-check (dependency-cruiser) / dead-code (knip) | 0 / 0 | 24 / 47 | |
| `npm test` (unit, CI unset) | 1 | 286 | 590 files, **4,815 tests: 4,812 pass, 3 fail** → 1 was my fixture-bytes error (F1; `room-protocol.test.ts` re-run 6/6 pass), **2 are pre-existing = K1**. True baseline: 4,813 / 4,815, 2 known-red. |
| `npm run test:integration` (CI unset) | 1 | 141 | 19 files: 14 pass, **1 fail = K1** (`memory-recall.e2e`), 4 skipped (`ZER0_REAL_CLI` unset). Tests 32 pass / 1 fail / 12 skipped. |
| compiler closure (`tsc -p tsconfig.production.json --listFiles`, repo files) | 0 | 5 | **181** |
| `npm ls --omit=dev --depth=0` | 0 | 7 | 22 lines; package.json: **21 dependencies, 12 devDependencies** |
| `git ls-files` | 0 | 0 | 1,883 |
| `test:live` | not run | — | inventory: `vitest.live-files.ts` = 8 rows (real-agent suites; a 250-s codex call among them) |
| `package-zer0-v2-sidecar.mjs <TEMP>/release-baseline` | 0 | — | staged sidecar **1.1 GB**: `@openai` 354M, `@anthropic-ai` 254M, `@temporalio` 158M, better-sqlite3 67M, node-pty 64M, `@agentclientprotocol` 33M, `@swc` 24M; 297 top-level node_modules entries |
| packaged handshake probe (`phase0/probe-handshake.mjs`, fresh `git init` project) | 0 | 1.0 s | initialize → `session/new` → `session/list` contains created id → `zer0/room/shutdown` acknowledged → exit 0; `_schema_version` = **{14,15,16,20}**; `chat_sessions` = 1; 60 tables; DB `<project>/.zer0/evidence.db` |

### 0.4 Rust baseline (`phase0/rust/steps.tsv`) — rustc/cargo 1.94.0, `CARGO_TARGET_DIR=<TEMP>/target`
| step | rc | s | note |
| --- | --- | --- | --- |
| `cargo metadata --locked` | 0 | 20 | 83 workspace members, 1,285 packages. **Feature-aware retained set (`cargo tree -p zer0-v2-bin -e normal,build`) = 19 packages** + `ptyctl` (dev, `conpty_ui`) = 20: dagre_rust graphlib_rust mermaid-to-svg ordered_hashmap prod-mc-cli-chat-proxy-types xai-grok-config xai-grok-markdown xai-grok-markdown-core xai-grok-mermaid xai-grok-pager xai-grok-pager-render xai-grok-paths xai-grok-version xai-prompt-queue xai-ratatui-inline xai-ratatui-textarea xai-tty-utils zer0-room-protocol zer0-v2-bin (+ptyctl). (`cargo metadata`'s resolve graph is NOT feature-aware: it reaches 77 — do not use it for the closure.) |
| `cargo build --locked -p zer0-v2-bin --profile release-dist` | 0 | 235 | `target/release-dist/zer0-v2.exe` 8,013,312 bytes; `--version` → `zer0-v2 0.1.0` *(Phase 0 record; the binary is `m0irai.exe` since Phase 6 — see the Phase 6 section)* |
| `cargo test --locked -p zer0-v2-bin -p zer0-room-protocol -p ptyctl` | 0* | 63 | 17 test binaries, **68 tests: 68 pass** after F1 (before F1: 67/1, the 1 = `transport_protocol` `frame-exact-limit`). *rc 101 in `test-room-2.log` is the pre-F1 run; `test-protocol-3.log` shows conformance 3/3 + transport 14/14 after. |
| `cargo test --locked -p zer0-v2-bin --features test-support --test host_lifecycle` | 0 | 239 | **9/9 pass** — real Node host boots, `session/new`, `--continue` (`session/list → session/load`), shutdown; needs sibling `sources/zer0-agent-ci` built. |
| `cargo test -p <all 20>` / pager-only compile | 101 | 100 / 77 | **K2** below. |

### Known baseline failures (named; their tests are the falsifiers)
- **K1 — memory recall into the chat briefing is empty (pre-existing at the pin; reproduced in OLD at `31bd3ea`).** 3 tests: `src/memory/compose-prompt-lanes.test.ts` › "memory ON: chat lane equals the committed per-agent golden…", › "W0 … a live prompt naming a file pulls another agent's file-tagged decision", `tests/integration/memory-recall.e2e.test.ts` › "MT-FINAL-a…". Root cause: codex's V2 commits (`4fcaa29`/`aa32bd5`, 2026-08-02/14) added `operatorOnly: true` at `src/chat/headless-prompt.ts:238` and `src/chat/lane-carrier.ts:218`; `src/memory/briefing.ts:129-130` then admits only `author === "operator"` rows. The digest writes `author: "ledger"` (`src/memory/digest.ts:148,162,221`) → **today's V2 code can never brief digest output to an agent**. Not touched in Phase 0. **Decision at Phase 5** (product call, operator): restore framed recall of ledger rows (recommended — the shared-brain thesis and MT-FINAL-a depend on it; framing + `isSafeSharedMemoryBody` remain) or keep operator-only and accept a write-only digest for M1.
- **K2 — the pager crate cannot be tested on Windows at the pin.** `cargo test -p xai-grok-pager` (any feature set) builds its dev-deps → `xai-grok-agent` → `xai-grok-tools-api` whose `build.rs` needs `protoc`; `bin/protoc` is a dotslash launcher script (not a Win32 binary) and no `protoc` is on PATH. The product build (`zer0-v2-bin` → pager with `default-features = false`, `room-runtime`) never needs it. Consequence for Phase 4.1: the pager's dev-edges to agent/shell are untestable here regardless — pruning them loses nothing that runs today. *(Dated correction, Phase 6: this was a before-the-prune condition — after Phase 4/4b, `cargo test -p xai-grok-pager --features room-runtime --lib` runs green on this machine, 805 tests in the Phase 6 lane. The paragraph above stands as the Phase 0 record.)*

### Findings from the harness itself (fixed before any number above was trusted)
- **F1 — `git archive` on this machine (`core.autocrlf=true`, no `.gitattributes`) rewrote every trailing LF→CRLF in all 39 protocol files** (`frame-exact-limit.bin` 1,048,576 → 1,048,577 bytes; both the Node and Rust conformance tests went red). Fix used: `git -c core.autocrlf=false -c core.eol=lf archive` + per-file `git hash-object` == blob check. Carried into Phase 1: the import commands are recorded verbatim in `docs/provenance/manifest.json`; **m0irai gets a `.gitattributes` (`protocol/** -text`, `*.bin -text`)** — the outer OLD repo has none, so any fresh Windows clone of it today gets converted fixtures (latent defect, now understood).
- F2 — my runner exported `CI=1`; Ink tests refuse a CI env by design ("neutralize the CI env before Ink is first imported"): 41 spurious unit failures + `cockpit-launch`/`phase-1-dod` integration failures. Re-measured with CI unset (the numbers above).
- F3 — the baseline root first lacked the `sources/` level that both `src/room/room-protocol.test.ts:23` (`../../../../protocol/`) and `crates/zer0-room-protocol/tests/conformance.rs:23` (`../../../../../protocol/`) hard-code; restructured, re-measured.
- F4 — first `test-retained` used `cargo metadata`'s 77-package reach (aborted, log kept as `test-retained-ABORTED-77set.log`); replaced by the `cargo tree` set.

### Gate verdict
Every baseline command is green or has a named pre-existing failure with a retained falsifier (K1: 3 tests; K2: pager dev-dep compile). Source pins resolve; licence files present (Node MIT, Rust Apache-2.0). **Phase 0 gate: PASS — cutting may start.**

---

## Phase 1 — assemble WORK, uncommitted (2026-08-17)

| step | observable |
| --- | --- |
| 1.1 fresh clone of the Node pin → `WORK`; `.git` (verified `git rev-parse --git-dir` = `.git`) removed; `git init`; no remote | 1,883 working files; **1,883/1,883 hash-identical** to pin tree `eb65b32a…` (`git hash-object --no-filters` vs ls-tree blobs) |
| 1.2 `git -c core.autocrlf=false -c core.eol=lf archive 82d2524` → `rust/` | 3,166 files, **3,166/3,166 hash-identical**; `.cargo/config.toml`, `rust-toolchain.toml` (1.94.0), `Cargo.lock` present |
| 1.2 same for outer `1680373:protocol` → `protocol/` | 39 files, **39/39 hash-identical** |
| 1.3 created | `docs/STATE.md` (this), `docs/provenance/manifest.json` (input commits/trees/remotes/methods/licence hashes/tool versions/plan sha256), `docs/provenance/deletions.json`, `NOTICE`, root `README.md` placeholder (old "Zer0 Agent CI" README replaced; preserved in OLD/backups) |
| 1.4 removed before first commit (recorded in `deletions.json`, 418 paths) | `.council/` 397 · `codex review/` 13 · `scratch/` 2 (Ink viewport spikes) · `.codex/` 4 + `.gemini/` 1 (old-repo agent tooling config) · `.zer0/workflows/adversarial-locked-override.md` → **moved** to `tests/fixtures/workflows/`, consumer `src/chat/agent-prompt-stack.test.ts:40` repointed |
| 1.5 layout literals repointed | `src/room/room-protocol.test.ts:23` `../../../../protocol/` → `../../protocol/`; `rust/crates/zer0-room-protocol/tests/conformance.rs` ×33 and `rust/crates/zer0-v2-bin/tests/transport_protocol.rs` ×8 `../../../../../protocol/` → `../../../../protocol/`; `rust/crates/zer0-v2-bin/tests/host_lifecycle.rs:419-424` sibling `zer0-agent-ci` lookup → `source_root().parent()` (repo root) |
| 1.5 `.gitattributes` | Node repo already had `* text=auto eol=lf` (+ binary list); appended `protocol/** -text`, `*.bin -text` (F1) |
| reference clone | `git clone https://github.com/xai-org/grok-build.git D:\grok-ref` started (reference only; HEAD recorded in manifest when complete) |

Gate 1 (still uncommitted; every file traceable to a recorded git object; forbidden-root query zero): see Phase 2 row "pre-commit checks".

---

## Phase 2 — proof harness, then the first commit (2026-08-17)

Created (all in `scripts/`, each with `isMain` + exported function; referenced from `package.json` so knip treats them as entries):
- `oracle-standalone.mjs` — hermetic/release modes; proofs 1,2,3,6,7,8,9 + **H (hermetic refusal at named seams for claude, codex, gemini)**; `--falsify schema` (must-fail, cause required in stderr). Proofs 4/5 (digest close/replay) deferred to Phase 5 and listed as `deferredProofs` in the registration.
- `verify-rust.mjs` (`npm run verify:rust`) — metadata closed inside `rust/` · room-crate tests (`zer0-v2-bin`, `zer0-room-protocol`, `ptyctl`; `--workspace` after Phase 4 closes the workspace) · `host_lifecycle` with `test-support` (nonzero tests asserted) · `digest_handoff` with `test-support` (nonzero tests asserted; added at Phase 5c) · release-dist build (exe existence + size).
- `gate-tracked-surface.mjs` (+ `docs/provenance/tracked-surface.json`, exact inventory, policy `work`) · `gate-cut-closure.mjs` (+ `deletions.json`) · `gate-oracle-registration.mjs` (+ `docs/provenance/oracle.json`, fail-closed sha256) · `verify-staged.mjs` (index tree → archive → `npm ci && npm run verify` → external receipt `.verify-logs/receipts/<tree>.json`; `--assert-head`) · `ship-gate.mjs`.
- `package.json`: `gates` = build first … oracle + falsifier last, **`test:live` removed from gates**; `verify` = gates + verify:rust; `verify:staged`; `ship:gate`; `oracle`. `.github/workflows/verify.yml` (windows + ubuntu, Node 24, rustup 1.94.0, `npm ci && npm run verify`, fan-in job) replaces `gates.yml`.
- Product seam: `src/shared/hermetic.ts` (`ZER0_HERMETIC=1`, exact value only) + `assertNotHermetic()` at ten agent-process seams (`acp-session.spawnServer`, `acp-turn-session.spawnServer`, `agy-pty-spawn.agyExePath` + `.spawnAgyPty`, `pty-session-registry.spawnChild`, `agy-version.runRealProbe`, `digest-extractor.codexExec`, `adapters/claude.run|health`, `adapters/codex.run|health`) and an early return in `RoomEagerSessions.start`. Unit test `src/shared/hermetic.test.ts` (3 tests) proves the flag semantics and that the ACP seam refuses before any process exists.

Deviations from the plan text, forced by the guards (rule 4 / rule 9):
- **`.council/` retained under the `work` policy** (79 tracked files) because the cut guard found live consumers: `src/temporal/manifest/{loader,schema,semantic-validators}.test.ts` use `.council/packets/packet-11b/canonical-brief.md` as a fixture; old cli/temporal readers of `.council/adr-index.json`; historical docs. It leaves with its last consumer (Phase 3.5/7). The pin's own `.gitignore` marks **318** council files transient — never staged in WORK, removed from disk, recorded in `deletions.json` (`scan:false`).
- Guard blind spots fixed on first contact: dotfile-config conventions (`.codex/config.toml` vs `~/.codex/config.toml`) → `scan:false` + a boundary matcher; relocated fixture whose new path shares the old tail → `relocatedTo`; historical `docs/**` excluded from the reference scan under `work` (only `docs/agents/**` is held to it).
- Reconciliation: pin tracked 1,883 → WORK tracked 4,765 (Node 1,883 − 340 absent + Rust 3,166 + protocol 39 + new files); **340 absent = 340 recorded in `deletions.json`** (`comm` over sorted path lists).

Gate results before staging (in-place, CI unset): build/typecheck/biome/9 gates/dep-check/knip green (knip: pre-existing warning `promptfoo` unused devDependency — same as baseline; removed in Phase 3.9); `gate-cut-closure` 340 deletions / 4,292 files scanned / 0 dangling; `gate-tracked-surface` exact inventory; `gate-oracle-registration` sha256 matched; oracle **12/12** (incl. H for claude, codex, gemini at seams `acp-turn-session.spawnServer` ×2 and `agy-pty-spawn.agyExePath`); falsifier **must-fail satisfied with the cause in stderr**. Two harness fixes on first contact: biome now ignores `rust/**`, `protocol/**` (byte-exact fixtures must never be formatted), `docs/provenance/**` (generated inventories); the seam test moved beside its adapter (dependency-cruiser `shared-foundation-isolation` — `src/shared` may not import adapters).

**First commit** = the whole assembled tree, verified by `npm run verify:staged` on the exact index tree (external receipt under `.verify-logs/receipts/<tree>.json`); its tree id is recorded in the next STATE update (this file never cites the tree that contains it). Reference clone: `D:\grok-ref` = xai-org/grok-build `main` @ `9fabade` (contains base `b13fa52`).

K1 handling for green commits (rule 3): the three K1 tests are marked `it.fails` with the K1 note in-file (`src/memory/compose-prompt-lanes.test.ts` ×2, `tests/integration/memory-recall.e2e.test.ts` ×1) — the suite is green, each test still runs and must fail, and the Phase 5 fix flips them red until the markers are removed. Staged run 1 (tree `561a943…`) was RED for a harness reason (my guards need a git index in the stage → `verify-staged` now inits one); staged run 2 (tree `66f6383…`) RED only on K1 (4,816/4,818 unit).

---

## Checkpoint receipts (each row = the PREVIOUS commit's tree, cited by the next update)

| commit | tree | receipt | result |
| --- | --- | --- | --- |
| `92c8fea` (Phase 1–2 import + harness) | `523f1f4509573bb603e3c330e50bec3c6a8dec54` | `.verify-logs/receipts/523f1f45….json` (external) | GREEN — `npm ci` 63 s, `npm run verify` 1,255 s: unit 4,818/4,818, integration 33 pass/12 skipped, oracle 12/12 + falsifier, Rust room crates + lifecycle + release-dist. Pushed to private scratch `CodeZer0-vibe/m0irai-work`. |
| `5423574` (3.1 seams) | `d41388b10f835dbc4ccdb1dda6e7048649453b5b` | `.verify-logs/receipts/d41388b1….json` | GREEN — `npm ci` 60 s, `npm run verify` 1,026 s. Closure after 3.1 = **170** (`.verify-logs/closure-after-3.1.txt`; dropped: prompt-builder, agent-prompt-stack, temporal/memory-compiler, observability/lessons-reader, security/{filter,denylist,entropy}, memory/{persistence,scoped-query,snapshot-builder,redaction}, evidence/memory-queries; added: shared/{hermetic,render-escape}, room/status-mode-language, chat/adapter-contract). Pushed. |
| `77cd580` (3.2 CLI cut + harness fixes) | `bd17de461f1d7d384517fb6abecb6342bfcb1543` | `.verify-logs/receipts/bd17de46….json` | GREEN — `npm ci` 80 s, `npm run verify` 1,094 s: unit 4,575/4,575 (551 files), integration 27 pass/11 skipped, oracle 13 proofs + falsifier, Rust room crates + lifecycle + release-dist. Two earlier 3.2 trees were RED **under machine load only** (F5): `07219d5…` twice (the 128-room falsifier at 98 s and 61 s of its 60 s budget while the Rust lane compiled — evidence kept as `.verify-logs/receipts/07219d5….RED-load-run{1,2}.*`; run 2's other 4,574 tests passed), then `906a732…` once (a first-event/last-assert race in `room-host-eager.test.ts`). Pushed. |
| `b353441` (3.3 TUI cut) | `8e8d7dd011429bc06a9367955b33af99242c88c5` | `.verify-logs/receipts/8e8d7dd0….json` | GREEN — `npm ci` 70 s, `npm run verify` 1,128 s: unit **3,294/3,294 (423 files; was 4,575/551)**, integration 18 pass/11 skipped, oracle 13 proofs + falsifier, Rust unchanged. First run of this tree was stopped by the lead at 163 s on 08-17 (operator closing the laptop) — kept as `8e8d7dd0….RED-interrupted-by-lead-run1.*`, not a verdict. Pushed. |
| `1f32648` (3.4 loop cut) | `0f5cb41ee0f3c4b0670ea0d0a812150e9635516b` | `.verify-logs/receipts/0f5cb41e….json` | GREEN — `npm ci` 89 s, `npm run verify` 1,291 s (the Rust lane's `CARGO_BUILD_JOBS=8` compiles overlapped the whole run; the suite held): unit **2,995/2,995 (397 files)**, integration 18 pass/11 skipped, oracle 13 proofs + falsifier. Pushed. |
| `9cf68e5` (3.5 temporal cut) | `2487d211e7c1e3b0b4db8f6899a4dd9bc5bfaa40` | `.verify-logs/receipts/2487d211….json` | GREEN — `npm ci` 67 s, `npm run verify` **809 s** (the pipeline suites were the heaviest in the pool; ~19 → ~13.5 min per proof): unit **2,642/2,642 (346 files)**, integration 7 pass/10 skipped (3 files + 3 skipped), oracle 13 proofs + falsifier. Pushed. |
| `59059cd` (3.6 tower/debate/cockpit cut) | `1ab930f3928859f335cc2e58b51d60d47f303518` | `.verify-logs/receipts/1ab930f3….json` | GREEN — `npm ci` 61 s, `npm run verify` 789 s: unit **2,091/2,091 (269 files)**, integration 4 pass/10 skipped (2 files + 3 skipped), oracle 13 proofs + falsifier. Pushed. |
| `dc2120f` (3.7 observability + orphans + F6) | `37ed83e9c7b386b7d93ed1d1196bb61ba092499c` | `.verify-logs/receipts/37ed83e9….json` | GREEN — `npm ci` 67 s, `npm run verify` 745 s: unit **1,841/1,841 (229 files)**, integration 4 pass/10 skipped, oracle 13 proofs + falsifier. Pushed. |
| `504b17c` (3.9 deps/config) | `965839efaf55f62d23ce9d8ad020d51b7f8a8b49` | `.verify-logs/receipts/965839ef….json` | GREEN — `npm ci` 44 s, `npm run verify` 766 s: unit 1,841/1,841 (229 files), integration 4 pass/10 skipped, oracle 13 proofs + falsifier; knip 0 findings. Pushed. |
| `9ad07bf` (Phase 4 Rust prune) | `31b946040daf023f7cbc6f99cffdb5af88ba0302` | `.verify-logs/receipts/31b94604….json` | GREEN — `npm ci` 59 s, `npm run verify` 972 s (the first proof to run `cargo test --locked --workspace` inside `npm run verify`): unit 1,841/1,841 (229 files), integration 4 pass/10 skipped, oracle 13 proofs + falsifier, Rust workspace 3,052 passed/0 failed/38 ignored, host_lifecycle 9/9, release-dist exe 8,009,216 B. A first staging of Phase 4 content (tree `d4baee80…`) was RED once — F9/FL-028, receipt kept `d4baee80….RED-digest-catchup-unexplained-run1.*`. Pushed. |
| `d8dae89` (Phase 4b lane F2 sweep + record) | `87ec45e1709803b2a33d036d33a32dd0b0273c43` | `.verify-logs/receipts/87ec45e1….json` | GREEN — `npm ci` 42 s, `npm run verify` 717 s. Pushed. |
| `426247c` (Phase 5 write side + 5c-Node + FINDINGS ledger) | `a00988ee8d8fc211bebea852f4d5d6cd6a7fad41` | `.verify-logs/receipts/a00988ee….json` | GREEN — `npm ci` 39 s, `npm run verify` 871 s: unit 1,908/1,908 (236 files; the lane tip measured 1,909 — K1's net +1 test lands with this table's next row), integration 4 pass/10 skipped, oracle proofs + both falsifiers (schema, digest-entry), Rust workspace + host_lifecycle + release-dist. Run 1 of this commit's first staging (tree `e9fd6cdb…`) RED in 84 s = FL-069 (inventory updated before FINDINGS.md existed), receipt kept `e9fd6cdb….RED-tracked-surface-inventory-order-run1.*`. Pushed. |
| `ed26e6f` (K1 read side) | `2dd09457e6b3008e70fef9e5079d282279825945` | `.verify-logs/receipts/2dd09457….json` | GREEN — `npm ci` 33 s, `npm run verify` 802 s (run 2, quiet machine): unit 1,909/1,909 (236 files; the K1 trio plain and passing), integration 4 pass/10 skipped, oracle proofs + both falsifiers, Rust workspace + host_lifecycle + release-dist. Run 1 of the same tree RED once — `host_lifecycle`'s 5 s post-shutdown exit window missed under 4-way machine load (FL-070); receipt kept `2dd09457….RED-load-hostlifecycle-exit5s-run1.*`. Pushed. |
| `a74e8ce` (Phase 5c — F10 closed) | `e54ad0cc…` | `.verify-logs/receipts/e54ad0cc….json` | GREEN — 923 s full verify (unit, integration, oracle 18/18 + A6 must-fail, Rust workspace + handoffTests 6 + release-dist). Run 1 of this commit's FIRST staging (tree `e00f249e…`) RED on oracle proof 5 = the REAL cross-session digest collision, evidence preserved to `.lead/evidence/oracle-proof5-2026-08-19/` → FL-074 (fixed at `847e2b9`); receipt kept `e00f249e….RED-oracle-proof5-cross-session-catastrophe-run1.*`. Pushed. |
| `a7198dc` (3.8 reachability + sweep) | `ea5a2d09…` | `.verify-logs/receipts/ea5a2d09….json` | GREEN — 1,115 s full verify incl. the new reachability gate + falsifier in the chain. Prior staging RED once (tree `1bba3c93…`): the gate's test was a node:assert script, not a vitest suite — the pool collects `scripts/*.test.mjs`; receipt kept `1bba3c93….RED-reachability-test-not-a-vitest-suite-run1.*`. Pushed. |
| `5b8fe3f` (FL-071 — the working row retires on delivery) | `2a0eb45e…` | `.verify-logs/receipts/2a0eb45e….json` | GREEN — 1,033 s full verify (log `verify-staged-22`); lane proofs: pager 773 lib tests, real-ConPTY working-absence assertions, vitest src/room 157/157. Pushed. |
| `847e2b9` (FL-074 — busy DB ≠ catastrophe) | `243bfca5…` | `.verify-logs/receipts/243bfca5….json` | GREEN — `npm ci` 39 s + `npm run verify` 958 s (log `verify-staged-23`); lane proofs: 3 falsifiers with real forked children vs a real `BEGIN IMMEDIATE` holder, both durable sinks asserted. Pushed. |
| `2403895` (Phase 6 — rename + brand + boot) | `b06a8164…` | `.verify-logs/receipts/b06a8164….json` | GREEN — `npm ci` 42 s + `npm run verify` 974 s (log `verify-staged-24`); two-builder lane (WIP boundary commit preserved), all ten acceptance items quoted in the handbacks. Pushed. |
| `74d5117` (boot tuning — hero mark, living shine, plain name) | `14082ec6…` | `.verify-logs/receipts/14082ec6….json` | GREEN — `npm ci` 35 s + `npm run verify` 895 s (log `verify-staged-26`, quiet machine). Run 1 of the near-identical pre-amendment tree (`6908d429…`) RED once — pty-session idle-cap under 4-way load, the FL-013/FL-070 class; receipt kept `6908d429….RED-load-ptysession-idlecap-run1.*`; the flake did NOT recur on the quiet re-run. Pushed. (This commit's own row lands with the next update, as always.) |
| `6844cca` (research day — channel inventories, usage root cause, locked policy) | `300cdba6…` | `.verify-logs/receipts/300cdba6….json` | GREEN — `npm ci` 41 s + `npm run verify` 900 s (log `verify-staged-27`). Documents only: the three channel inventories and the grok UI adoption map land in `docs/research/`, the release-polish wave spec v2.1 and the slice-H contract in `docs/specs/`, FL-094 root-caused with the locked display policy. |
| `96f8412` (slice-H contract v2 — the referee's fourteen findings folded) | `92f1ddfc…` | `.verify-logs/receipts/92f1ddfc….json` | GREEN — `npm ci` 33 s + `npm run verify` 913 s (log `verify-staged-28`). Documents only. The round's load-bearing correction cuts against our own record: the test v1 called "a lying pin" is a correct stale-rejection guard (codex finding 8), so FL-094 carries a strike-through rather than a fix. |
| `6cb820d` (the record tells the truth again) | `67e92bd2…` | `.verify-logs/receipts/67e92bd2….json` | GREEN — `npm ci` 33 s + `npm run verify` 1,040 s, Node v24.18.0. Documents only: the codex docs-vs-disk audit folded — seven status columns that contradicted their own row, five evidence claims the tree had since disproved, and `docs/HANDOFF-m0irai.md` rewritten rather than patched. Row added at the wave-0 merge (2026-08-21); the table was two commits behind. |
| `c6d1e95` (one contract per slice, and a commit rule that says whose commits) | `66da0291…` | `.verify-logs/receipts/66da0291….json` | GREEN — `npm ci` 83 s + `npm run verify` 1,276 s, Node v24.18.0. Documents only: wave spec v2.3 rewrote §§7-9 in place rather than layering a correction over them, and all 19 referee findings were accepted. Row added at the wave-0 merge (2026-08-21). |
| `4087a2f` (clamp answered by extraction + the FL-172 record corrected) | `fba8fc5d…` | `.verify-logs/receipts/fba8fc5d….json` | RED UNDER LOAD — `npm ci` 50 s + `npm run verify` 732 s exit 1 (2026-09-01, Node v24.18.0): 7 test files / 10 tests, all wall-clock timeouts in the src/room process-spawn tests + digest-runner, box at 97 % CPU from four other sessions; re-run alone 5/7 pass. Those budgets were fixed by FL-175 at `840c05f`. Not a proof of anything but the load. |
| `9f03907` → `1bd57d2` (seven merges: M5, ledger, M4, D, FL-175, M1b, RP — see POSITION 2026-09-06) | — | none | NO RECEIPT — each merge was proven on its own index (typecheck, lint + gates, the affected suites, cargo where Rust changed) but never by `verify:staged`: every window had the box loaded. The batch proof is the receipt of the 2026-09-06 docs commit's tree; its row lands in the update after it, as always. |
| `dc6b721` (the position on 2026-09-06 — seven merges above `4087a2f`) | `fd3681eb…` | `.verify-logs/receipts/fd3681eb….json` | GREEN — `npm ci` 18 s + `npm run verify` 1,051 s (2026-09-06 08:00–08:18 UTC, Node v24.18.0): the batch proof for the seven merges in the row above. Not pushed. |
| `e549e96` → `701158e` (the harness/ACP wave's first batch — see POSITION 2026-09-08) | — | three RED attempts on 2026-09-07 (trees `5184842…`, `1eca8901…`, `2213df83…`) | NO GREEN RECEIPT — each merge was proven on its own index; each attempt failed on exactly one item (a clamp, a type error, one wall-clock Rust test), the first two fixed on mainline the same evening, the third the class BOOT r3 replaced. The batch proof is the receipt of the 2026-09-08 docs commit's own tree; its row lands in the next update, as always. |

## Phase 3 — seams, then Node cuts

### 3.1 Seam commit (moves only; nothing deleted)
| seam | what moved | consumers repointed |
| --- | --- | --- |
| `src/tower/render-escape.ts` | the INV-13 primitive (`EscapeOptions`, `escapeUntrusted`, its constants) → **`src/shared/render-escape.ts`** (no color deps; own sibling test split out of the tower test); the tower keeps only its chrome constants and re-exports the primitive for its remaining internal importers | 5 in closure (`headless-turn`, `permission-ask`, `room-engine-primitives`, `room-host-support`, `room-mode`) + 6 outside (`cli/…/chat-tui-cockpit-loop`, `tui/{cockpit-control-dispatch,cockpit-loop-bridge,live-rows,safe-text,use-model-picker-handlers}`) |
| `src/tui/status-mode-language.ts` (+ test) | → **`src/room/status-mode-language.ts`**; the tui path is now a re-export shim (4 tui importers) that leaves with the tui; the reducer-coupled describe of its test moved to `src/tui/status-mode-language-live-catalog.test.ts` (tui-owned reducer) | `room-mode.ts` → `./status-mode-language.js` |
| `src/tower/types.ts` | the whole native-agent adapter contract → **`src/chat/adapter-contract.ts`** (engineering call vs the plan's "merge into chat/types.ts": one cohesive 110-line contract, keeps the 205-line types hub small); `src/tower/types.ts` is a type-only re-export shim for the tower's 25 internal importers | 8 non-tower importers (`agent-bins`, `chat-bridge-session`, `tower-bridge`, `tower-bridge-lane` + tests) |
| `estimateTokens` | 4 closure callers (`commands`, `evidence`, `evidence-strict`, `headless-turn`) → `estimatePromptTokens` from `prompt-budgeter.ts`; `prompt-builder`'s wrapper untouched until its cut | — |

Gate edits: `gate-l5-mandates` G1 exemptions for the type-only `adapter-contract.ts` and the temporary tui shim (reasons in-file). Typecheck, biome, clamps, l5, dep-check (0 errors), knip (baseline warning only), 117 touched tests green before staging.

### 3.2 Legacy entry cut (the CLI)
Deleted (all recorded in `deletions.json`, 442 entries total now): `src/cli/**` (82 files) except `lease.ts`+test → **moved** to `src/temporal/activities/lease-store.{ts,test.ts}` (temporal's dynamic `LEASE_MODULE_PATH` repointed — consumer kept whole until 3.5); `bin/zer0.mjs` (npm CLI launcher) and `bin/zer0-v2.mjs` (dev tsx launcher — nothing referenced it); `scripts/smoke-chat.mjs`, `scripts/e2e/boot-scrollback-conpty.ts` (CLI smoke/boot scripts); CLI-only tests: `tests/integration/{cockpit-launch,build-packet-fix-loop,build-packet-happy,context-compiler-fix-loop,phase-1-dod}.test.ts`, `tests/integration/seed-task.ts`, `tests/e2e/ghost-composer-conpty.test.ts` (booted `bin/zer0.mjs chat`; live-files row removed), `tests/e2e/loop/{ac2-crash-resume,ac10-vanished-worktree,ac12-hostile-bytes}.e2e.test.ts` (boot the REAL `zer0 chat` Ink binary via ConPTY); `src/tui/git-gate.tsx`, `src/tui/trust-gate.tsx`(+test) (knip orphans — their only consumer was the CLI launch path); `src/README.md` (old src module map).
Config: `package.json` `bin` + `dev` removed; knip entries `src/cli/index.ts!`, `scripts/smoke-chat.mjs!` removed; dependency-cruiser rules `no-upward-deps-temporal-activities`/`no-cli-as-dep` removed (target no longer exists); 5 stale comment mentions in loop e2e helpers reworded; two fixture strings that merely looked like CLI paths neutralized (`rubric-parser.test.ts`, `prompt-builder.test.ts`).
**PTY drain (forced by the cut):** new `src/room/room-process-drain.ts` (`drainProcessOwnedTransports` = `closeAllLaneTransports` + `disposeAllPtySessions`, concurrent, failures independent) wired into `AliveRoomHost.shutdown` under the existing deadline; sibling test (3 tests) proves both families drain and neither failure hides the other. `gate-no-claude-p` unchanged (its tripwires live in files cut at 3.6 — rewrite lands there).
Parallel lane: **Rust prune (Phase 4) dispatched to a builder-hard agent** on worktree `D:\m0irai-work-rust`, branch `rust-prune` (from `92c8fea`; rust/ is disjoint from the Node cuts). Brief: scratchpad `brief-rust-prune.md`. It lands only through `verify:staged` like every other commit.

**Harness fixes forced by the proof runs (same commit; product code untouched):** (a) `src/room/room-host-process.test.ts` — the 129-room fixture wrote every room TWICE through `createSession`+`persistSession` (each persist = 3 git spawns for the evidence project id + a DB open): 774 process creations on Windows, 45–50 s of a 60 s budget in the 4-fork pool and over it under any extra load. Now one real `createSession` seed (layout derived from it, no store paths hard-coded), one `persistSession` per room, written in bounded batches of 8; `session/list` reads exactly what this writes (run dir, transcript, marker). Measured: 29.9 s → 12.6 s in isolation *while 11 rustc processes ran*; 28.4 s in-suite in the green run (was 98 s). Timeout unchanged at 60 s. (b) `src/room/room-host-eager.test.ts` — `cycleOne` emits `pending` synchronously and `active` only after the awaited lane apply (`room-mode.ts:158-199`); the test waited for the *first* claude `agent.mode` event and asserted the *last* was `active`. It now waits for this cycle's terminal state (a `pending` seen, last `active`, never `failed`).

### Findings from the harness (Phase 3)
- **F5 — a timed proof that depends on the machine being idle is not a proof.** With the Rust prune lane compiling in parallel (cargo defaults to 12 jobs on this 12-core host) the whole unit suite ran 2.27× slower (per-file ratio vs the 3.1 green run; unrelated files 2–6×), and two room tests with thin margins flipped RED (details in the 3.2 addendum). Both were latent test-design defects, not products of the load; fixed so the suite tolerates ≥3× load without touching a timeout. Operating rules for the rest of the extraction: the Rust lane runs cargo with `CARGO_BUILD_JOBS=8` (leaves 4 cores for the Node harness); a RED receipt is never overwritten silently — it is renamed `<tree>.RED-<cause>-runN.*` before any re-run and the cause is recorded here.

### 3.3 TUI cut (the Ink cockpit)
Deleted (228 paths, all in `deletions.json`, 670 entries total): `src/tui/**` (207 files: cockpit, reducers, Ink components, their `.test.tsx`); its **production-orphans** — modules whose every non-test importer was in `src/tui` (measured with a reverse-import graph over the tracked tree, transitive): `src/chat/{address-catalog,agent-auth-probe,agent-command-match,control-commands,pairing-store,session-resume,slash-catalog}.ts`, `src/memory/memory-view-loader.ts`, `src/shared/usage-display-policy.ts` (+ their 9 sibling tests); TUI-only tests `tests/integration/independence.test.ts` (imports `src/tui` cockpit-turn/session-to-turns) and `tests/integration/shared-brain-resume.test.ts` (bound to the CLI/TUI boot resolver `resolveBootSession`; the room resumes via `session/load` — `room-host-support.ts:46`, `room-host.test.ts:519`, and the Rust `--continue` path); the Ink patch `patches/ink+5.2.1.patch` (`package.json` `zer0Patches` updated; sidecar test expectation updated).
Second-order dead code the cut exposed (knip `exports`/`types` = error): `runQueuedTurn` (`cockpit-turn-exec.ts`; the Ink cockpit's poison-proof queue wrapper), `unifiedWord`/`StanceWord`/`ENGINES` (`room/status-mode-language.ts`; the TUI control-row stance word), `PendingAskInfo` (`permission-ask.ts`; ask-line render shape), `AgentCatalogs` (`agent-commands/types.ts`; command-menu snapshots) — removed with their doc blocks.
Consumers edited: `eager-session-boot.test.ts` (dropped the auth-probe mock — the boot module never imported the probe; the "never consults the probe" describe is now the plain ready-resolution test), `codex-usage-consumers.test.ts` (dropped the `visibleMeters` describe; the lane-recovery-clock and loop-budget consumers stay until their own cuts), 8 comment/fixture mentions reworded so the cut-closure scan is clean (`loop/budget.{ts,test.ts}`, `headless-prompt.ts`, `codex-usage-drift-producer.test.ts`, `status-mode-language.test.ts`, `request-files.test.ts` fixture path, `patch-integrity.{ts,test.ts}`; the latter's operator notice still says "terminal drawing" — the module is outside the closure and leaves at 3.8).
Deps: `ink`, `react`, `is-unicode-supported`, `wrap-ansi` (deps) and `@types/react`, `ink-testing-library` (devDeps) had zero consumers outside `src/tui` → removed; `package-lock.json` regenerated (`npm install --package-lock-only`); `promptfoo` stays a knip *warning* until 3.9 as planned. Config: `tsconfig.json` `jsx` removed (no `.tsx` remains anywhere), `tsconfig.production.json`/`biome.json` `.test.tsx` globs removed, dependency-cruiser `chat-must-not-import-tui` rule removed (target gone), `gate-l5-mandates` tui exemptions + `.test.tsx` acceptance removed, `vitest.setup.ts` `CHATZERO_UNICODE` pin removed (only `src/tui/glyphs.ts` read it). `tracked-surface.json` regenerated (4,673 → 4,446 paths, the new `docs/HANDOFF-m0irai.md` agent handoff included).
Rehearsed first on an exported copy of the 3.2 tree (`D:\m0irai-lab33`, node_modules junctioned, disposable): tsc 0, biome 0, knip (baseline warning only), dep-check 0 errors, cut-closure 670/0 dangling, l5/agent-files/clamps/encoding/no-claude-p/scripts-scope PASS, build OK, 145 targeted tests green — then applied to WORK by the same anchored script.

### 3.4 Loop cut (the autonomous goal loop)
Deleted (68 paths, all in `deletions.json`, 738 entries total): `src/loop/**` (46 files) and its `/loop` chat commands `src/chat/loop-commands.{ts,test.ts}`; the **production-orphans** `src/chat/lane-scheduler.ts` and `src/chat/loop-events.ts` (+ sibling tests; every non-test importer was in `src/loop`); the loop's real-binary e2e suite `tests/e2e/loop/**` (7) with its own `vitest.config.loop-e2e.ts` and the `test:loop-e2e` package script; the manual goal-loop harness `scripts/goal-loop{,-scope,-council,-dispatch,-pipeline,-trace,-worktree}.ts` (7; knip entries removed) and `scripts/e2e/loop-overhead-measure.ts` (knip entry `scripts/e2e/*.ts!` removed — the directory is empty now).
Consumers edited: `codex-usage-consumers.test.ts` (dropped the loop-budget describe + the now-unused `WEEKLY_ONLY_97` fixture import; only the lane-recovery-clock consumer remains), `vitest.config.ts` (the `tests/e2e/loop/**` exclude row), 3 stale comment mentions of loop files reworded (`events.ts` ×3: lane-scheduler's awaitDispatch, `src/loop/types.ts` ×2; `evidence-db-identity.test.ts`: `loop/bridge.ts`).
**Left in place, recorded (F6):** the loop's event *vocabulary* stays declared in the chat bus contract — `events.ts` (`loop.progress|stopped|iteration` types + `LaneQueuedEvent`), `event-schemas.ts` (their zod schemas), `chat-trace.ts` (their trace flags) — with no emitter and no consumer after this cut. It is not a file-level orphan (knip is silent: every symbol is used inside its own file), so it is not this cut's closure; strip it in 3.7 together with the observability/trace modules. No production behaviour depends on it.
Rehearsed on the lab export of the 3.3 tree: tsc 0, biome 0, knip (baseline warning only), dep-check 0 errors, cut-closure 738/0 dangling, l5/agent-files/clamps/encoding/no-claude-p/scripts-scope PASS, 57 targeted tests green — then applied to WORK by the same anchored script. `tracked-surface.json` 4,446 → 4,378 paths.

### 3.5 Temporal cut (the build pipeline)
Deleted (117 paths, all in `deletions.json`, 855 entries total): `src/temporal/**` (74 files: workflows, activities, worker/server/client, signals, manifest loader+schema+validators, the relocated `lease-store`) — **except** `activities/memory-compiler.{ts,test.ts}`, which `src/chat/prompt-builder.ts` still imports and which leaves with prompt-builder at 3.6 (as v5 §6 3.5 anticipated: "once the cut guard clears every caller"); the pipeline's **production-orphans** (every non-test importer was in `src/temporal`; measured transitively): `src/evidence/{commit-intents,dispatch-claims,phase-events}.ts`, `src/gates/{build-gate,engine}.ts` (the directory is empty now), `src/observability/tracking/**` (compactor, index, paths, reader, writer, schemas/{current-state,decision-record,result}), `src/shared/{application-failure,git-bash,rubric-parser}.ts` (+ 15 sibling tests); the pipeline integration suites `tests/integration/{build-packet-cancel,build-packet-chaos,build-packet-phase-events,build-packet-retry-policy,crash-recovery,real-cli.smoke}.test.ts` and their manifest fixtures `tests/fixtures/manifests/*.yaml` (4); `docker-compose.yml` (Temporal server); `.prettierrc.json` (prettier is not a dependency — biome formats; its only override targeted a workflow file). `application-failure.ts` had no importer outside the pipeline (v5: "only if proven unreachable" — proven by the graph, then by tsc/knip on the cut tree).
Consumers edited: `runs-bootstrap.test.ts` (the claimDispatch/commit_intents FK describe left with both modules; `ensureRunRow` tests stay — the room's evidence writes use it), `observability/inspect.test.ts` (the PHASE_EVENT_KINDS superset guard left with phase-events), `observability/replay.test.ts` (the three tests seeded through the pipeline's `persistReplayEvidence` left with it, with their now-unused helpers/imports; the three plan/reject/non-zero tests stay), `shared/types/agent.ts` + `shared/types.ts` (`CostReport`, the pipeline's cost aggregate — knip `types` error), 5 stale comment/fixture mentions reworded (`shared/types/evidence.ts` ×2, `observability/schemas/event.ts`, `prompt-builder.test.ts` fixture paths + one assertion on the new fixture basename).
Gates: `gate-l5-mandates` **G5 activity-registration and G6 test-activity-isolation removed** — both had no inputs without the pipeline (G5 already returned true when the barrel was missing: a zero-input pass, exactly the class 3.9's floors are for); dependency-cruiser `workflow-sandbox-purity` rule removed (its target dir is gone) + one comment reword; knip entries `src/temporal/{worker,server}.ts!` removed. Deps: `@temporalio/{activity,client,common,worker,workflow}` + devDep `@temporalio/testing` removed (zero consumers), lock regenerated (1,956 lines fewer). `tracked-surface.json` 4,378 → 4,261 paths.
Rehearsed on the lab export of the 3.4 tree: tsc 0, biome 0, knip (baseline warning only), dep-check 0 errors, cut-closure 855/0 dangling, l5/agent-files/clamps/encoding/no-claude-p/scripts-scope/oracle-registration PASS, production build OK, targeted tests green — then applied to WORK by the same anchored script.

### 3.6 Tower / debate / cockpit cut
Deleted (154 paths, all in `deletions.json`, 1,009 entries total): `src/tower/**` (67 files: controller, decision-router, integration, adapters claude/codex + events, start-guard, worktree, render + chrome, types/render-escape shims); its **forward importers** in `src/chat` (imported the tower directly or through each other): `agent-bins`, `chat-bridge-session`, `cockpit-bridge`, `cockpit-turn-exec`, `cockpit-turn-triggers`, `run-debate`, `run-debate-dispatch`, `run-debate.fixtures`, `tower-bridge`, `tower-bridge-turn`; the **production-orphans** the cascade exposed (every non-test importer was in that cluster; measured with the reverse-import graph, transitive): `src/chat/{agent-prompt-stack,chat-base-sha,cockpit-turn-persist,cockpit-turn-route,controller-confirm,controller-council,council-synthesis,debate-events,dispatch-error-summary,fan-out-round,headless-waves,prompt-builder,run-debate-persistence,run-debate-state,synthesize-trigger,turn-evidence,undo-trigger,worktree-service}.ts`, `src/evidence/memory-queries.ts`, the memory verify-loop `src/memory/{evidence-capture,reconciler,report-protocol,snapshot-builder,verify-runner,work-journal}.ts`, `src/observability/lessons-reader.ts`, `src/security/{filter,denylist}.ts` (the secret prefilter — operator decision 2026-08-16: not carried; `security/entropy.ts` stays for now, `memory/redaction.ts` imports it), and the last Temporal remnant `src/temporal/activities/memory-compiler.ts` (prompt-builder was its only caller — the directory is gone); 63 sibling/variant tests of those modules; 7 tests whose subject left (`council-debate-claude-routing`, `prompt-builder-{customization,framing,snapshot}`, `turn-git-free`, `security/__tests__/filter.integration`, `tests/integration/zer0-mvp-memory.e2e` — the verify-loop e2e).
Consumers edited: `transcript-order.test.ts` (only the `canonicalTranscript` leaf-sort describe stays; buildTranscript/countFramedInPrompt were prompt-builder's), `memory/compose-prompt-lanes.test.ts` (the cockpit/council real-root wiring describe, its helpers and the headless-turn/ui mocks left with cockpit-turn-exec + controller-council; **the two K1 `it.fails` falsifiers stay**), `vitest.live-files.ts` (the 5 tower real-adapter rows), `gate-no-claude-p` (**rewritten to the two surviving tripwires** — dispatch-headless pty routing and the retired registry dispatcher, both in the room closure; the tower quarantine pattern removed), dependency-cruiser `no-orphans` exception for the tower types shim removed, `shared/types/chat.ts` + `shared/types.ts` (`ChatPeerOutput`, the council peer-output shape — knip `types` error), 9 stale comment mentions reworded (`kill-tree.ts`, `render-escape.ts`, `adapter-contract.ts`, `gate-l5-mandates`, `security/entropy.ts`, `shared/types/evidence.ts`, `tests/setup/codex-home-global.ts`, …).
**Harness fix (F7):** `gate-cut-closure` matched a deleted path's token as a PREFIX of a longer retained path (`chat/tower-bridge` inside `chat/tower-bridge-lane`; 4 false positives) — the boundary check only looked at the character before the token. It now requires a non-word boundary on both sides; falsified on the lab: a planted real import of a deleted file is still caught (1 hit), a planted prefix-only import is not (0).
Rehearsed on the lab export (3.5 content): tsc 0, biome 0 (exit code), knip (baseline warning only), dep-check 0 errors, cut-closure 1,009/0 dangling, l5/clamps/agent-files/encoding/no-claude-p/scripts-scope/oracle-registration PASS, production build OK, 65 targeted tests green (incl. room-host, headless-turn, tower-bridge-lane) — then applied to WORK by the same anchored script. `tracked-surface.json` 4,261 → 4,107 paths. No dependency lost its last consumer in this cut (`yoctocolors` stays: `chat/ui.ts` is in the closure).

### 3.7 Observability + named orphans (+ F6)
Measured first: the production closure of `src/room/zer0-v2-host.ts` after 3.6 is **171 files** (170 + `room-process-drain`; `.verify-logs/closure-after-3.6.txt`), leaving **53 production TS files outside it**. Two of those are NOT dead: `src/memory/digest-runner.ts` (Phase 5's wiring target — its own closure adds nothing outside the host closure) and `src/chat/chat-trace.ts` (imported by the digest entry `scripts/digest-run.ts`, whose closure is otherwise inside the host closure: 89 files, only itself + chat-trace outside). Two more are test support (`src/room/room-host-process.ts`, `src/memory/baseline-fixture.ts`). The remaining 49 + the whole observability tree are V1 surface with no room caller.
Deleted (90 paths, all in `deletions.json`, 1,099 entries total): `src/observability/**` (the CLI's inspect/replay/agent-guide/schema-bundle/otel/state-writer/stream surface; the knip entry `src/observability/index.ts!` removed); V1 chat features `src/chat/{agent-capabilities,boot,git-change-summary,git-hash-identity,handoff,md-export,md-gate,md-gate-diff,review-readout,structural-diff,structural-diff-fetch,structural-diff-zip,trace-analyze,worktree-path-guard}.ts`; the agy interactive PTY `src/adapters/pty/{agy-interactive,agy-interactive-spawn,agy-interactive-types,node-guard}.ts` (the room's agy path is `agy-pty-spawn`); `src/evidence/capacity-snapshots.ts`; the memory L2/L3 modules `src/memory/{anchors,artifacts,curation,persistence,projections,redaction,report-lookup,scoped-query}.ts` (outside both the host and the digest closures; recoverable from the pins); `src/security/entropy.ts` (the last of the secret prefilter — operator decision 2026-08-16); `src/shared/{patch-integrity,replay-sentinels,temp-dir}.ts`; and their sibling tests (incl. `structural-diff.live.test.ts`, its live-files row removed).
Consumers edited: `evidence-ledger-gate.test.ts` reader smoke — the plan's "direct read": the review's independent reader was the V1 inspector; the smoke now opens its own handle and counts the run's dispatch rows through the schema (`dispatches ⋈ tasks WHERE run_id`); `createQueries` has no run-level dispatch listing, so a prepared statement is the direct read.
**F6 done (loop vocabulary):** `events.ts` loop.* types + union members + @exports, `event-schemas.ts` loop schemas + union, `chat-trace.ts` loop flags removed. **F8 (recorded, not done):** other V1-only kinds remain declared in the chat-bus contract with no producer/consumer in the closure (measured per kind: `agent.stderr`, `build.lane.*`, `build.run.started`, `council.*`, `diff.ready`, `route.pick`, `tui.feed`, `undo.result`; `build.card`/`build.reviewed`/`debate.*` have one closure mention each; `lane.queued` is also a ROOM event kind — string-ambiguous) — the bus-contract tests still exercise them as fixtures, so stripping them is a contract-test rewrite: M2 hygiene, or an M1 pass after Phase 5 if time allows.
**Sequencing decision (engineering call, recorded):** the reachability sweep + gate (3.8) moves AFTER Phase 5. Reason: 3.8's definition is "every production TS outside the closure + declared runtime-entry set", and Phase 5 is what pulls `digest-runner` and the digest entry into the closure; running the sweep first would either delete Phase 5's own inputs or require a hand-kept allowlist that duplicates the wiring. Order from here: 3.9 deps/config → Phase 4 Rust merge → Phase 5 digest wiring → 3.8 sweep + gate + planted-orphan falsifier → Phase 6 → Phase 7.
Rehearsed on the lab export (3.6 content): tsc 0, biome 0, knip (baseline warning only), dep-check 0 errors, cut-closure 1,099/0 dangling, all gates PASS, production build OK, 105 targeted tests green (events, event-schemas, chat-trace, ledger-gate, digest-runner, digest, headless-turn, room-host) — then applied to WORK by the same anchored script. `tracked-surface.json` 4,107 → 4,017 paths.

### 3.9 Deps / config
Deps: `promptfoo` (devDependency, zero consumers — knip warned since Phase 2) removed; lock regenerated. Result: **12 runtime deps** (`@agentclientprotocol/{claude-agent-acp,codex-acp,sdk}`, `@openai/codex`, `better-sqlite3`, `execa`, `figures`, `node-pty`, `patch-package`, `ulid`, `yoctocolors`, `zod`) and **8 devDeps** (`@biomejs/biome`, `@types/better-sqlite3`, `@types/node`, `dependency-cruiser`, `knip`, `tsx`, `typescript`, `vitest`) — v5 predicted ~12/~8. Every remaining dependency has a measured consumer (`figures`/`yoctocolors`: `src/chat/ui.ts`, in the closure).
Config: knip — the no-match entry `spike/**/*.ts!` and the no-match ignores (`.council/**`, `docs/**`, `dist/**`) removed (knip's own hints); knip is now silent. dependency-cruiser — `no-upward-deps-security` / `no-upward-deps-gates` removed (their source dirs are gone), the `no-orphans` exceptions for `src/index.ts`/`spike/` and the `spike/` clauses removed, one comment reworded. Agent standing files — `docs/agents/core.md` now names m0irai (room + Rust UI + host + protocol), points at STATE/HANDOFF/plan v5 and states the verify:staged rule; `AGENTS.md`/`CLAUDE.md`/`GEMINI.md` regenerated by `scripts/generate-agent-files.mjs` (gate-agent-files green). The remaining conduct text is unchanged.
CI: **measured** on the 3.7 tree — the unit suite is green under `CI=1` (1,841/1,841); the Ink-era refusal (F2) left with the TUI. The `CI: ""` override in `.github/workflows/verify.yml` and the matching env-strip in `scripts/verify-staged.mjs` are removed, so a local proof and the GitHub run see the same environment shape.
Deferred to 3.8 (after Phase 5, per the 3.7 sequencing decision): the scripts sweep (`smoke:headless`, `seal:agy-build`, `dep-graph`, `scripts/reality-gate.ts`, `scripts/memory-baseline-capture.ts`, `scripts/agy-dispatch.ts`, `scripts/diagnose/**` … — V1 entries knip only tolerates because they are declared) and the nonzero floors on zero-input gates (floors are set on the final tree). `package.json` name/description/private stay for Phase 6 as planned.
Rehearsed on the lab export (3.7 content): tsc 0, biome 0, knip 0 findings, dep-check 0 errors, cut-closure 1,099/0, all gates PASS — then applied to WORK by the same anchored script + `npm install --package-lock-only` + `node scripts/generate-agent-files.mjs`.

## Phase 4 — Rust prune merged (squash of `rust-prune` `cf07701..1c71762`)
The lane (builder-hard, two sessions, brief in `.lead/tools/brief-rust-prune.md`, its own record committed as `docs/provenance/rust-prune-PROGRESS.md`) shipped on branch `rust-prune`: `cf07701` declarations + lock (19 optional product declarations pruned — 16 pager, 3 pager-render — plus every optional dep whose only activator was a deleted feature; `[patch.crates-io] async-openai` dropped; workspace members **83 → 20**; `unexpected_cfgs check-cfg` for the never-set `grok-runtime`/`local-workspace` values; two test files touched so the `--workspace` flip does not install a hang — **K3**: a pre-existing Windows hang in the RETAINED crate `xai-grok-config` (`managed_text` FileIdentity is `(len, modified)` of the parent dir on Windows; NTFS bumps it on every child write → every `apply()` reports `ParentChanged`), proved pre-existing by a stash-and-rerun control at `92c8fea`; 11 tests `#[cfg_attr(windows, ignore)]` — they still run on unix CI; the module is unreachable from the room; M2 item) · `65f13c4` deletions (**1,956 tracked files**: 63 crate directories incl. the 4 pager dev edges — all four measured untestable/deleted-binary-dependent, `rust/bin/protoc` dotslash launcher, `zer0-v2-bin/src/ui` quarantined UI) · `89ddc1e` the 20-member closure pin in `rust/crates/zer0-v2-bin/tests/room_dependency_closure.rs` (proved to bite by three mutations: a pinned member dropped, a real 21st member, `ptyctl` removed — each FAILED, each reverted) + `scripts/verify-rust.mjs` `ROOM_TEST_SCOPE → ["--workspace"]` + rustfmt repair · `5ef3adf` lane record.
Measured final workspace: `dagre_rust graphlib_rust mermaid-to-svg ordered_hashmap prod-mc-cli-chat-proxy-types ptyctl xai-grok-config xai-grok-markdown xai-grok-markdown-core xai-grok-mermaid xai-grok-pager xai-grok-pager-render xai-grok-paths xai-grok-version xai-prompt-queue xai-ratatui-inline xai-ratatui-textarea xai-tty-utils zer0-room-protocol zer0-v2-bin` — 626 resolved packages (was 1,285), lock pairs 1,306 → 633 with **0 version drift** (only removals). Acceptance as measured by the lane on its final tree: A1 `cargo metadata --locked` exit 0 / 20 members / 0 manifests outside `rust/`; A2 `cargo test --locked --workspace` **3,052 passed / 0 failed / 38 ignored** (116 s; pager alone 774 passed / 17 ignored); A3 `host_lifecycle` 9/9 against the real Node host; A4 release-dist `zer0-v2 0.1.0` **8,009,216 bytes** (Phase 0 pin 8,013,312); A5 `node scripts/verify-rust.mjs` ok; A6 clean tree. Deviations from the brief, all measured (lane record §6): pager `default = ["room-runtime"]` not `[]` (with `[]` the crate's own test targets only build through workspace feature unification); more than the 19 declarations removed (unactivated optional deps still pin lock packages).
Follow-ups the lead asked for before merging (lane record §5): **F2 (pager theme race)** — RESOLVED in `0e8f955`: 29 reader tests now hold `cache::pin_theme()` for their whole body — 15 in `xai-grok-pager/src/scrollback/blocks/user.rs` (direct `Theme::current()` readers) and 14 in `blocks/quote_bar.rs` (indirect: `quote_bar_style()` reads `Theme::current().md_muted`) — `pin_theme()` takes the same `cache::test_lock()` the writer test in `blocks/thinking.rs` holds, so reader and writer are serialized; no product code, no writer test, no theme module touched. Instrument calibrated on the known-real case first (control, lib test binary run directly): **2 failures / 200 unguarded runs** (`user::tests::all_token_ranges_invalid_renders_plain`, `quote_bar::tests::wrapped_quote_continuations_exclude_reinjected_prefix`) → **0 / 400 guarded runs** (at the ~1% control rate a clean 400-run treatment leaves ~1.8% chance of luck; a 40-run loop would have left ~67%, which is why the loops are this size). `quote_bar.rs` was found by the control run, not by inspection — the lead's brief named only `user.rs`. Still open by design (M2): the root defect is the process-global theme — 614 `Theme::current()` sites in the pager, six files now pinning. **F5 (`conpty_ui` 8 s first-screen deadline)** — RESOLVED in `0e8f955`, and the finding itself corrected: `STEP` was never "the first-screen deadline" — it was ONE 8 s constant shared by 23 structurally different waits in `rust/crates/zer0-v2-bin/tests/conpty_ui.rs`. Split into two harness budgets, both derived from measurement, neither a product latency contract (this is a ConPTY correctness proof): **`BOOT` = 20 s** for everything before the first frame (PowerShell spawn + 49 filler lines + debug binary start + Node host spawn + 4-round-trip handshake + first render; 14 runs on this 12-core box: 2.93–3.56 s idle, 3.93–8.24 s under a concurrent `CARGO_BUILD_JOBS=8 cargo build`; worst 8.24 s × 2.4 = 20 s — the old 8 s sat INSIDE the loaded range: 2 of 6 loaded runs timed out at 8.02 s with the screen still on PowerShell filler) and **`STEP` = 8 s unchanged** for the 22 post-boot reaction waits (worst observed 1.10 s, 7.3× headroom; deliberately not tightened — those budgets only spend wall-clock when the test is already failing). Every wait helper is now `#[track_caller]` (a timeout names its call-site line, elapsed and budget) and `ZER0_CONPTY_TIMING=1` prints every satisfied wait — the derivation is re-runnable and written into the file. After: 0 failures / 8 runs (3 loaded, boot 3.51–6.67 s). Recorded as the Rust instance of the F5 machine-load rule (a wait that fails under ≥3× load is a harness-design defect: measure, separate, self-diagnose — never merely raise). Not fixed by design: **F3** `rust/README.md` is still grok's (Phase 6 README owns it); **F4** = K3 above (M2).
Merge mechanics: `git merge --squash rust-prune` (no file touched by both branches — measured: 816 paths on master since `92c8fea`, 1,970 on the lane, intersection empty); the 1,956 deleted Rust paths appended to `deletions.json` by `.lead/tools/fold-rust-deletions.mjs` (category from the lane's directory-level `deletions-rust.json`, `scan: false` — cargo metadata/build/test on the pinned workspace is the reference checker for Rust; the gate's path-tail tokens such as `src/lib.rs` are not unique across crates); `tracked-surface.json` regenerated; `scripts/verify-rust.mjs` header + test-step label repaired at merge time (the lane version still described the pre-Phase-4 scope as a plan and labelled the `--workspace` step "room crates" — no consumer parses the label). Rehearsed first on a disposable worktree of `504b17c` + the squash: biome, every gate, knip, dep-check, tsc all exit 0 (the only red, `gate-patches`, was the lab's junctioned `node_modules` — and it exposed that WORK's own working install still carried the 3.3-removed Ink patch receipt; `npm ci` in WORK fixed the working copy; staged proofs were never affected because they install fresh). Verified whole by `verify:staged` (this is the first proof that runs `cargo test --workspace` inside `npm run verify`). `biome.json` now ignores `.lead/**` — the lead's git-excluded tools directory (documented in HANDOFF); biome scans the filesystem, not the index, so without it `npx biome check .` in WORK reports the lead's ad-hoc scripts (measured: two `useTemplate` findings) while the staged proof, which exports the index, never sees them.

**F9 — one unexplained RED on the first Phase 4 proof (tree `d4baee80…`, receipt kept as `.verify-logs/receipts/d4baee80….RED-digest-catchup-unexplained-run1.*`).** `npm run verify` exited 1 after 267 s: unit **1,840/1,841** — the single failure `src/memory/digest-runner.test.ts:164` "REAL boot catch-up: 3 undigested sessions all land in the journal after the background pass" (three REAL detached tsx children; `poll(decisionCount >= 3, 45_000)` returned false after 45 s; the four single-child REAL tests in the same file passed right after it in 1.9–2.4 s each). The machine was carrying this proof's 4-fork suite plus the Phase 5 lane's own activity plus the Rust lane's cargo. Investigation, all measured with `.lead/tools/probe-catchup.ts` (the same real `bootCatchUp` + real children + DB poll): idle WORK 3 runs **2.0–2.5 s**; a fresh `git archive` export of the same tree (cold path) 3 runs **2.1–2.6 s**; the export with a full unit suite running concurrently (13 node processes) 4 runs **2.7–3.6 s**; a genuinely cold `tsx` compile cache (fresh TEMP; 80 files compiled per child) 2 runs **2.1 s**. Ten probes, none within an order of magnitude of 45 s; SQLite BUSY exhaustion is implausible inside 45 s (`busy_timeout` 5 s × 10 retries). **Root cause NOT established** — the temp workspace (and with it any `.zer0/journal/digest-failures.log`, scratch rows or lease files) is deleted by the test's `afterEach`, so the failing run left no evidence. Consequence, per the F5 rule: (a) the receipt is preserved and the tree re-proved as-is (Phase 4 does not touch this code; the test's mechanism is measured robust under ≥3× load); (b) the next occurrence must be self-diagnosing — the Phase 5 lane, which rewrites these real-spawn tests around the compiled entry, is required to make every real-child wait dump the decision timeline, scratch rows, failure log and lease files into the assertion message on timeout; (c) if the re-proof reds on the same test, the tree is NOT committed until the cause is found (retain the workspace, read the record).

### Phase 4b — the lane's last two commits, folded (`rust-prune` `61d46d4`, `4f8c049`)
The Rust builder committed twice more after the squash point `1c71762` (the branch ref moved while the first Phase 4 proof ran; noticed when `git branch -D rust-prune` reported tip `4f8c049`). Both are folded here as one verified commit — nothing else changes. **`61d46d4` F2 sweep:** guarding only the two files that failed is not an argument that nothing else races, so the builder enumerated (44 retained-crate files read `Theme::current()` inside `#[cfg(test)]`; 39 unguarded; intersected with the 771 tests the binary actually lists → only **6** compiled and run, the other 33 sit in grok-runtime-gated modules not in this binary) and guarded those 6 (11 more `#[test]` fns — `room_composer_menu.rs`, `room_scrollback.rs`, `scrollback/blocks/credit_limit.rs`, `views/completion_dropdown.rs`, `views/modal_window.rs`, `views/slash_dropdown.rs`; branch total 40 guards; every guard inside a `#[test]`, never a shared helper — a helper would self-deadlock on the non-reentrant mutex behind `pin_theme()`). Measured with a calibrated instrument: a steady forced native lock is the WRONG oracle (render and assertion agree under it), so the writer's window was widened instead (750 ms sleep while `thinking_body_dim_italic_survives_the_terminal_native_palette` holds the native lock) — amplifier ON / guards OFF **5 failing runs / 5** (calibration); amplifier ON / 29 guards 0/12; amplifier ON / all 40 guards **0/20**; no amplifier, all 40 guards, `cargo test --locked -p xai-grok-pager -q` **40 passed / 40** (937 s); the render crate's own second writer (`syntax.rs` `with_native_lock`) amplified 0/12 with a calibrated null (a deliberately unguarded probe reader in that module DID fail under it). **`4f8c049` lane record §5** — F2 gains that enumeration/table; F5 gains the statistics behind BOOT: first-screen latency over 17 successful runs min 2,925 ms / median 3,969 ms / max 8,236 ms (idle n=10: 2,925 / 3,559 / 4,533; loaded n=7: 3,933 / 6,252 / 8,236) plus the 2 runs the old 8 s budget truncated at 8.02 s. Docs + Rust test files only; no product code.

**Operator decisions recorded 2026-08-18:** **K1 = YES** — the memory digest's read side (framed recall into the agents' prompts) is restored in Phase 5 as its own commit after the write side lands, and the three `it.fails` markers go with it. **Repo name = `m0irai`** — the Phase 7 export targets `D:\m0irai` and the private repository `CodeZer0-vibe/m0irai`; creating that remote and the first push are still confirmed with the operator at Phase 7 (outward-facing).

## Phase 5 — the memory digest wired into the room host (write side; squash of `digest-wiring` `504b17c..8e44e66`)
Built on a lane (worktree `D:\m0irai-work-p5`, builder-hard `digest-wiring-builder`, brief `.lead/tools/brief-phase5-digest.md`, zero file overlap with Phase 4, merged by squash after it) — commits `57c4b2c` C1–C5 · `8b5410c` C6 · `cecd624` C7 · `47aed2c` C8 · `812a60a` F9 self-diagnosis · `075fa40` R1 spawn-error record · `5858b8f` codex #2/#3 · `6b0d2be` codex #9/#10 · `dd74426` codex #13 · `917f762` codex #19 (recovered by the lead after the pause; re-verified by the lane) · `3a5c96e` codex #14/#15/#16 · `06c7b3c` codex #18 · `ab3a956` clamp split (`digest-runner.test.ts` 502 → 350 lines, the four could-not-start tests moved to `digest-spawn-failures.test.ts`, 15 tests before and after) · `b3716c1` Phase 5c Node side (the handoff writer — below). The lane's K1 commit `6e20ce5` is NOT in this squash; it is the next commit, alone. **What runs now (plain words):** open a room and close it — by quitting, by the client dropping the pipe, or by the shutdown command — and the room finishes writing its transcript first, then hands that session to a background process that reads it and writes what was decided into the project's memory; once per close, never twice; opening a NEW room also catches up any session a crash left undigested (skipping the one just opened); if the digesting piece is missing from a build, the failure is written down instead of vanishing; nothing in the shipped package needs a TypeScript runtime.

**Design as built (measured against the code, not the plan).** `src/room/attached-session-lifecycle.ts` (`AttachedSessionLifecycle`): ONE `attach(room, origin)` called from both `session/new` and `session/load` in `RoomRpcServer` right after the room exists (identity frozen from `AliveRoomHost.projectId()` — new accessor, resolved once at `create()` from `resolveBootLiveness`, never re-derived); a second attach throws (the host's `requireUnattachedRoom` keeps refusing over the wire; `isAttached()` stays true once a close has begun). ONE `detach(reason)` for every trigger — `zer0/room/shutdown`, stdin EOF (`main()`'s finally, reason `stdin-eof` or `stdin-error: …`), `RoomRpcServer.shutdown()`; concurrent triggers share one promise (its rejection handled once, delivered to every caller); order = `room.shutdown()` (every transcript write is awaited inside the request/lane flows, so the file the child reads is complete) → digest spawn (a forced-drain failure does NOT cancel it; it is re-thrown after) → durable close line `<repoRoot>/.zer0/journal/room-close.log` (`<iso> session= reason= digest=requested|skipped(memory-off)|failed(…) drain=ok|<message>`). Boot catch-up: `bootCatchUp({repoRoot, dbPath, projectId, exclude}, spawnFn?)` at `session/new` only, gated by `memoryEnabled()`, fire-and-forget. **Compiled entry:** `src/memory/digest-entry.ts` (moved out of `scripts/digest-run.ts`, deleted after parity; knip entry removed; `tsconfig.production.json` now has two roots) — `digest-runner.ts` resolves the entry as its own sibling with its own extension and spawns `process.execPath [...process.execArgv, entry, sessionId, repoRoot, absoluteDbPath, projectId]` (the packaged host is started by the Rust binary as `node.exe <launcher>` with no flags — `host_process.rs:1126` — so `execArgv` is empty there; the test-support host started with `--import <tsx>` hands the loader down; measured: a vitest worker's `execArgv` is `--conditions node --conditions development`, no loader, so unit tests inject spawns), cwd = os temp dir, detached/ignore/windowsHide, env = `childEnv()` + `NODE_NO_WARNINGS` + `ZER0_DEBUG`/`ZER0_DIGEST_FAKE`/`ZER0_CHAT_TRACE` (+ `TSX_TSCONFIG_PATH` only if the parent has it). `createDetachedSpawn(entry)` factory: a missing entry → `recordDigestCatastrophe` (durable, byte-silent, no spawn, close not blocked); a spawn `error` (R1) or a nonzero child exit while the host still lives (codex #9) is recorded the same way — the host is never killed by its own digest child. Packaging: `scripts/package-zer0-v2-sidecar.mjs` requires `dist/src/memory/digest-entry.js` and asserts EVERY compiled file is loader-free — the assertion now matches quoted specifiers/paths naming `.ts`/`.tsx`, `tsx` as a specifier or path segment, `ts-node` (the old bare-word regexes matched English in 27 of 172 compiled files: "driver-boot.ts's own misuse"); measured 0/172 dist files match while the sibling test still fails on `import "tsx"`, `import "ts-node/register"`, `import "./host.ts"`, a tsx loader path, `import "./cockpit.tsx"`, and three prose fixtures prove a comment is not a reference (stated trade-off: a dynamically built specifier evades this check as it did the old one).

**Oracle** (`scripts/oracle-standalone.mjs`, hermetic mode, `ZER0_DIGEST_FAKE` = a real canned extraction — the old value `"1"` was not valid extractor JSON, F5 below): proofs **4** close → journal fact + watermark, **4b** replay changes nothing (the compiled entry re-run synchronously under plain Node, no flags, exit 0, byte-silent — facts 2→2, watermarks unchanged), **5** empty extraction advances only the watermark, **7b** explicit shutdown and stdin EOF each close exactly once (one `room-close.log` line per trigger), and the must-fail **A6 falsifier** `--falsify digest-entry` (compiled entry deleted from a staged copy → durable `[digest-catastrophe] … digest entry is missing` record and NO fact); registration re-pinned (19 proofs). On any failed proof the oracle now retains its temp root and prints the path.

**Tests** (+68 tests / +7 files vs 3.9: unit 1,909/1,909 (236 files) and integration 4 pass/10 skipped, measured whole on the lane tip before the merge (91.7 s; the K1 it.fails trio still inverted until the K1 commit)): `attached-session-lifecycle.test.ts` (9), `digest-entry.test.ts` (7), `digest-runner.test.ts` (11) + `digest-spawn-failures.test.ts` (4; the could-not-start cases, split out when the clamp caught the file at 502 lines — extraction, never an exemption), `room-host-digest-process.test.ts` (4, real host from source + real child: new → submit → shutdown, stdin EOF, and the ZER0_DIGEST_HANDOFF case), `digest-evidence.test.ts` (11), `digest-handoff.test.ts` (5), `zer0-v2-host-params.test.ts` (6; extracted with the #3 fix), `zer0-v2-host.test.ts` (13), `package-zer0-v2-sidecar.test.mjs` (23). RED-before-GREEN, quoted in the lane's handback: with the close-once guard removed, "concurrent shutdown and EOF triggers share ONE close" failed (`expected 2 to be 1`) and "a forced-drain failure still lets the digest out" failed; with the missing-entry guard disabled the A6 falsifier reported `durable-record=false` and exit 1. **F9 closed for the future, not explained for the past:** every wait on a real detached child now returns terminal evidence or the whole diagnosis as its assertion value (`src/memory/digest-evidence.ts`: row timeline in ms, catastrophe rows, `digest-failures.log`, `room-close.log`, leases with holder pid liveness, spawned pids) and retains the workspace on failure (`afterEach` keyed on the vitest task state); budgets unchanged (40 s / 45 s / 60 s — measured 0.8–1.4 s unit, 2.5–3.2 s process-level, so 12–17× the worst observation) and now diagnosis triggers, not pass conditions. It caught a real defect on its first full run — one the builder had introduced in the rewrite 30 minutes earlier (a relative `dbPath` resolved against the test process instead of the workspace: the watcher watched a non-existent file while the digest landed correctly) — fixed at the seam (`resolveDbPath` mirroring `digestSpawnArgs`) and pinned by a test. `readDigestRows` opens the DB read-only so the watcher cannot manufacture the stall it exists to diagnose.

**Deviations from plan v5 §6 Phase 5, each measured:** D1 `bootCatchUp` takes a request object (a 5th positional after the optional spawn would force every caller to pass one; `gate-clamps` caps params at 5). D2 the production-JavaScript prohibition is about quoted specifiers, not bare words (above). D3 item 1's "a second attach closes the first idempotently" NOT built — one room per process (`requireUnattachedRoom`; `zer0/room/shutdown` → `stop`); the Rust client attaches once per host process (`cli.rs:220-275`); an untestable branch with no caller. D4 replay proven by re-running the compiled entry synchronously, not by waiting out a detached child. D5 digest proofs hermetic-only (release mode submits no turn). D6 close reason in a new durable file, not a room event (a new event type would touch the protocol and the Rust client). D7 `createDetachedSpawn` factory. D8 process tests in a new file rather than pushing `room-host-process.test.ts` past the clamp.

**Findings recorded (not fixed here):** F1 `src/room/room-host.ts` 599 and `src/room/zer0-v2-host.ts` 596 lines — the next change to either must extract. F2 a writer/transport failure closes only via EOF (`zer0-v2-host.ts:99` stops accepting lines; the room stays attached until stdin EOF reaches `main()`). F3 the close record says `digest=requested` even when the entry was missing (the cause is one hop away in the same folder). F4 `room-close.log` has no rotation (~100 B per close; Phase 7 public-tree guard). F5 the oracle's old `ZER0_DIGEST_FAKE: "1"`. F6 every room close now forks a real extraction outside hermetic mode — the Phase 6 live operator turn is the first close that costs a model call. R2 (lead) `zer0/room/shutdown` whose detach REJECTS leaves `stop` unset → the process lingers until EOF with the room closed (base left the room attached — worse); decision deferred. Production closure after Phase 5: **175 files** (+`digest-entry`, `digest-runner`, `attached-session-lifecycle`, `chat-trace` — exactly what STATE 3.7 predicted); test-support outside it now `room-host-process.ts`, `baseline-fixture.ts`, `digest-evidence.ts` (+ the two `*.fixtures.ts`).

**Cross-family audit (codex gpt-5.6-sol, effort ultra, read-only, 2,310 s; `.lead/reviews/codex-phase5-review.md`): 5 BLOCK / 14 MAJOR / 1 MINOR reported, every item verified against the code before anything was built.** Folded as write-side fixes (each with a RED-before-GREEN test): #2 a failed writer made `main()`'s `drain()` throw and skip `shutdown()` (no detach, no digest, no close record) → detach is now unconditional in the finally; #3 = the lead's R2 upgraded — a `zer0/room/shutdown` whose detach rejects left `stopping` unset (process lingered until EOF) → the shutdown request always ends the process, success or failure (host file extracted below the 600-line clamp first, never raised); #9 a child that starts but fails to bootstrap exited silently → `exit` listener records a catastrophe while the parent lives (boot-catch-up children fully; the close child only while the host is still up — the packaged-entry smoke in Phase 6 covers the rest); #10 inherited `--inspect*`/`--debug*` flags would pause a detached child forever → filtered from the child argv; #13 the loader-free assertion missed backtick literals and `.ts?query` → widened, falsifiers added; #14/#15/#16 proofs 4/5/7b and the A6 falsifier were `> 0` / "at least one matching" → exact watermark id sets, exact decision lists, exact close-line cardinality per session, acknowledged shutdown + exit 0 + zero watermark delta; #18 nothing compared the oracle RUN against the registration → the hermetic run now fails if a registered proof did not execute or an unregistered one ran; #19 `awaitDigestEvidence` declared success before reading failure evidence → failure records for the session are checked first. **Refuted as blocks / recorded as designed:** #4 (forced drain then a late transcript write — the watermark is a per-message-id set, `INSERT OR IGNORE`, gap-tolerant: the late message is digested at the next boot; a bounded `sessionWrites` barrier is an M2 nicety); #6 (subprocess-backed reads accepted just before an explicit shutdown are abandoned; the process exits when they end; Rust reaps); #7/#20 (unref'd stagger timers: an immediate quit loses the remaining catch-up spawns until the next boot — by design, "scheduled" not "done"); #8 (best-effort close line, reported via `diagnostic()`); #11 (pid-reuse in the 300 s digest lease — pre-existing V1 design); #12 (the entry does not verify the argv tuple against the DB's project row — the only caller passes the host's frozen tuple; M2 defensive check); #17 (the codex dispatch boundary is covered at the `createCodexDispatch({exec})` seam + the live pool + the Phase 6 live turn); #5 (the tracked-surface inventory — folded at merge). **#1 CONFIRMED and reproduced — F10, the one the plan did not foresee (below).**

**F10 — in the packaged product the close digest is killed by the Rust binary's Job Object.** `rust/crates/codegen/xai-tty-utils/src/lib.rs:485-507` creates the process scope's job with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` (only), and `rust/crates/zer0-v2-bin/src/host_process.rs` enrolls the Node host in it (`scope.enroll(&child)`); libuv's `detached: true` never sets `CREATE_BREAKAWAY_FROM_JOB`, so the digest child the host forks at close is in that job and dies the instant the Rust process exits — seconds before a real extraction could finish. Measured with `.lead/tools/job-survival-probe.ps1` (a KILL_ON_JOB_CLOSE job around `node.exe`, which forks the same detached/unref'd child shape as `createDetachedSpawn`; the child sleeps 4 s then writes a marker; the job handle is closed after the parent exits): **KILLED**; `+ JOB_OBJECT_LIMIT_BREAKAWAY_OK`: **KILLED** (Node never requests breakaway); `+ JOB_OBJECT_LIMIT_SILENT_BREAKAWAY_OK`: **SURVIVED** — but that flag lets EVERY child of the host (agent CLIs, PTY sessions) escape the reaper the job exists for, so it is not the fix; control without a job: **SURVIVED**. V1 never met this: `zer0 chat` ran in the operator's terminal with no Rust parent. Every dev/test path (vitest, the source host, the hermetic oracle) runs with no job, which is exactly why nothing red pointed at it — the finding came from the cross-family read. **Fix = Phase 5c — the Node half is IN this commit (`b3716c1`), the Rust half lands as its own verified commit after K1; the Phase 6 live turn depends on both:** `src/memory/digest-handoff.ts` — `resolveDigestSpawns()` returns `{close, catchUp}`: when the parent set `ZER0_DIGEST_HANDOFF=1` the CLOSE spawn is `createHandoffSpawn`, which serialises exactly what `digestSpawnArgs()` planned (11 keys, nothing recomputed: `v:1`, `kind:"digest"`, `sessionId`, `projectId`, `repoRoot`, `dbPath`, `execPath`, `argv`, `cwd`, `env`, `requestedAt`) as one JSON line appended with `O_APPEND` + `fsync` to `<repoRoot>/.zer0/journal/digest-requests.jsonl` before the close completes; boot catch-up ALWAYS forks in-process (`spawnCatchUpDigest`, it runs while the host runs); the close record reports the seam's own word (`digest=handed-off` vs `requested`), never the lifecycle's guess; without the flag nothing changes. Tests: `digest-handoff.test.ts` (field-by-field equality with the plan; second call appends; no child spawned — an injected spawn fails the test if called; folder created; a write failure → `failed(…)` in the close record, close not blocked), `attached-session-lifecycle.test.ts` (the seam word reaches the record), `room-host-digest-process.test.ts` (real host started with the flag: one request line for the session after `zer0/room/shutdown`, `room-close.log` says `handed-off`, no lease/no rows/no catastrophe = nothing ran in-process); the oracle's hermetic mode runs without the flag (no Rust parent there). The design as originally written follows. the digest child must be created by a process OUTSIDE the job, and the only such process is the Rust binary — the host, when launched with the handoff flag the Rust binary sets, writes the close digest as a durable request (`<repoRoot>/.zer0/journal/digest-request.jsonl`: one JSON line carrying exactly what `createDetachedSpawn` would have used — `execPath`, `argv`, `cwd`, the `digestChildEnv()` map, sessionId/projectId/dbPath, requestedAt) instead of spawning; the Rust binary, once the host has exited and before it drops the job, consumes the file and spawns each request unassigned to the job (`DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP`, null stdio, `env_clear()` + the request's env, the request's cwd), marking it consumed; a spawn failure is written durably; a stale request at the next boot is harmless (the host's boot catch-up is idempotent) and is cleared. Boot-catch-up children keep spawning in-process (they run while the host runs; an immediate quit loses at most what the next boot redoes). Proof: a Rust `test-support` test launches the REAL host under the real job, runs new → submit (hermetic) → shutdown, lets the host exit, drops the job, and asserts the digest record appears in the DB within a measured budget; its falsifier runs the same sequence with the handoff disabled and must observe NO record (the KILLED case, i.e. this finding reproduced from Rust); Node unit tests pin the request line byte-for-byte against `digestSpawnArgs`.

Merge mechanics: `git cherry-pick -n` of every `digest-wiring` commit in order EXCEPT `6e20ce5` (K1 — cherry-picked alone as the next commit) onto `d8dae89` (no file touched by both — Phase 4 was rust/, provenance, STATE/HANDOFF, biome.json, verify-rust.mjs); `deletions.json` gains `scripts/digest-run.ts` (`relocatedTo: src/memory/digest-entry.ts`); `tracked-surface --update`; this section + HANDOFF; verified whole by `verify:staged`.

**Operator rules recorded 2026-08-18 (evening):** **`docs/FINDINGS.md` is the improvements ledger** — created in this commit, backfilled from every existing record (STATE across phases, the Rust lane record, the Phase 5 handbacks, the codex audit), one row per finding with a stable `FL-nnn` id, severity, status, evidence pointer and fix/decision; from here on a row is added or flipped **in the same commit as the work** that finds or fixes it (its ledger history names each such commit). **M2 research produces documents only** (`docs/research/2026-08-18-m2-*.md`, sourced summaries — "how is it already solved" prior art), starts only after this commit, and changes no code before the operator's live test (checkpoint 2, Phase 6). Both files are on Phase 7's keep-list.

## K1 — framed recall restored (the digest's read side; cherry-pick of `digest-wiring` `6e20ce5`, alone)
Operator decision 2026-08-18: YES. **What runs now (plain words):** what the digest wrote into the project's memory can reach the agents again — a decision recorded by an earlier session, or by another agent, is briefed into a prompt inside the untrusted frame, attributed, never as instruction text; anything that looks like an instruction to the model is still refused before it gets there.
Change, as measured on the commit (8 files, +148/−60; no production file other than the two call sites): `src/chat/headless-prompt.ts` (buffered chat) and `src/chat/lane-carrier.ts` (carrier) drop the operator-only clamp — written as an explicit `operatorOnly: false` rather than omitted, because this is the provider prompt boundary and the line that was wrong, so the decision is visible where it is made. Neither remaining guard is relaxed: `isSafeSharedMemoryBody` screens every non-operator row and what survives is rendered inside the untrusted frame (`briefing.ts`). The three retained K1 falsifiers (`compose-prompt-lanes.test.ts` ×2, `memory-recall.e2e.test.ts` ×1) are plain `it()` again; the two tests that pinned the clamp (`lane-carrier-memory-safety.test.ts`, `lane-carrier-review-fixes.test.ts`) now assert the new contract with the same falsifying power (framed + attributed, never in instruction text); `briefing.test.ts` gains the proof the clamp used to make unnecessary — with `operatorOnly: false`, an unsafe core row and an unsafe router pull are both still refused.
**Finding (FINDINGS FL-059):** the integration falsifier `tests/integration/memory-recall.e2e.test.ts` could never have passed as written — its decision body was itself prompt-injection text, which the safe-body filter refuses to promote, so it failed inside the digest long before recall, and `it.fails` hid which assertion was failing. It now seeds a real decision AND the injection and proves the injection reaches neither the journal nor the prompt. **Recorded (FL-060):** `isSafeSharedMemoryBody`'s 6-pattern list admits "Disregard the prior prompt…" (framed as untrusted, but admitted) — the strictness of that list is a product call, M2.
Verified whole by `verify:staged` (external receipt by tree id; its receipts-table row is added by the next update, as always). HANDOFF's "known pre-existing red" no longer lists K1.

## Phase 5c — the close digest survives the packaged host's Job Object (F10 closed; squash of `digest-handoff` `426247c..835b3bd`)
Built on a lane (worktree `D:\m0irai-work-p5c`, two builder-hard sessions named `digest-handoff-builder`, brief `.lead/tools/brief-phase5c-handoff.md`), rebased onto the Phase 5 commit before its final proofs — commits `8824273` (recovered WIP: consumer + launcher flag) · `4c62be5` R4(a) unit tests · `1dc7197` R1/R3/R4(b)(c) · `93965a0` R3 kill-branch observable · `98a1574` R2 seam key checks · `0852c4a` the R4(b)/R4(c) pair became a real proof at the rebase · `f5b9ec7` R5 verify-rust step · `835b3bd` a causal claim in the budget note corrected. **What runs now (plain words):** when the packaged app closes a room, the memory-writing step no longer dies with the app — the app's inner engine leaves a note on disk saying exactly what to run, and the outer program (which outlives the engine) reads the note and starts that work safely outside its own cleanup net; if the note can't be acted on, the failure is written down; a note that was acted on is removed.
**Design as merged:** `rust/crates/zer0-v2-bin/src/digest_handoff.rs` — `consume_digest_requests(operator_root)` reads `<repoRoot>/.zer0/journal/digest-requests.jsonl`, validates every seam key (`v`==1, `kind`=="digest", non-empty `sessionId`/`projectId`/`repoRoot`/`dbPath`/`execPath`/`cwd`/`requestedAt`, `argv` non-empty strings, `env` string map; unknown extra keys deliberately accepted — `v` governs skew, so an additive writer change never loses a session's digest), spawns `execPath argv…` with `env_clear()` + the request env, the request cwd, `DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP | CREATE_NO_WINDOW`, null stdio, never enrolled in the `ProcessScope`; deletes the file after the loop; a malformed line is skipped and recorded to `digest-failures.log` naming the offending field; no file → no-op. `host_process.rs` sets `ZER0_DIGEST_HANDOFF=1` on the launcher (test-only `without_digest_handoff()` opt-out) and calls the consumer synchronously at all three exit sites — the graceful `shutdown()` branch, the kill branch, and `Drop` — each pinned by its own test (`a_host_dropped_without_shutdown_still_starts_the_handed_off_digest`, `a_host_that_has_to_be_killed_still_starts_the_handed_off_digest`; the kill-branch test first asserts `shutdown()` returned `Err` so it cannot degrade into proving the graceful path twice).
**The end-to-end proof and its falsifier** (`rust/crates/zer0-v2-bin/tests/digest_handoff.rs`, in `npm run verify` via the new verify-rust step): R4(b) `the_close_digest_survives_the_job_when_the_host_hands_it_off` — the REAL compiled host under the REAL job, hermetic env, initialize → session/new → submit → shutdown → job close → the digest facts appear in the DB (measured over 8 runs: **job-close → rows-readable 508/694/829 ms min/median/max**, budget 20 s ≈ 24×, re-derivable via `-- --nocapture`); R4(c) `without_the_handoff_the_close_digest_does_not_survive_the_job` — the identical run without the flag leaves `{completed: 1, journal: 0, watermark: 0}` where the flagged run leaves `{completed: 1, journal: 2, watermark: 1}` — same work present, only the handed-off digest survives; F10 reproduced as a permanent falsifier, non-vacuity-guarded (the run must leave a completed message). Windows-only pair (`#[cfg(windows)]`); the job exists only there. Before the writer landed, R4(b) was RED by construction and its diagnosis printed exactly why ("the host never wrote a handoff request"); the lane also proved the mechanism Node-free first with a mock-host control pair (FINDINGS FL-063). No `malformed` line ever appeared: the Rust consumer parsed the Node writer's first-ever real line and spawned from it — zero seam drift. `cargo test --locked --workspace` 3,060 passed / 0 failed / 38 ignored; `cargo fmt --check` clean; unit consumer tests 8/8 with a measured marker budget (50 markers: 0.6/83.5/113.2 ms min/median/max, budget 2 s).
**Gate:** `scripts/verify-rust.mjs` now runs `cargo test --locked -p zer0-v2-bin --features test-support --test digest_handoff` with a zero-tests guard, mirrored on the lifecycle step (RED probe quoted in the lane handback: a filter matching nothing → `"digest_handoff reported zero tests executed"`, exit 1); summary carries `handoffTests: 6` beside `lifecycleTests: 9`. This closes the gate blind spot FL-066 (every 5c proof was invisible to `npm run verify` before).
**Lead harness edit folded at merge:** `src/room/room-host-digest-process.test.ts` (handoff case) now pins the request's `projectId` — 64-hex, equal to the DB's `projects.project_id` row and to `argv`'s last element — because the Rust lane measured that a wrong value there makes the digest child exit 0 while writing only `FOREIGN KEY constraint failed` to the failure log (silent to anything watching exit codes).
**Lane deviations, each measured (handback on file):** `npm ci` skipped after the rebase (lockfile diff `d8dae89..master` empty; only the `gates` script line changed); three in-lane comment corrections where the tree had outrun the text (the module doc's "RED until the writer lands", the diagnose NOTE that read an absent request file as "writer missing" when a healthy consumer also deletes it — now distinguishing never-asked / asked-and-lost / asked-and-landed, and the budget note's proxy measurement replaced by the real path); the budget note's "two fastest runs were parallel" causal claim caught by the builder itself as an inference dressed as an observation and corrected (FINDINGS FL-073) — the recorded fact is now the 8-sample set with the parallel/isolated split stated and no cause claimed.

## 3.8 — reachability gate + the V1 scripts sweep + gate floors (after Phase 5c, as sequenced in 3.7)
**Measured first** (on `a74e8ce`): the production program is **177 src files** (two roots, `tsc --listFiles`), the tracked production inventory 182, and the difference is exactly the declared test support — `src/room/room-host-process.ts`, `src/memory/baseline-fixture.ts`, `src/memory/digest-evidence.ts` + the two `*.fixtures.ts` — **0 orphans** (Phase 5 pulled `digest-runner`/`chat-trace`/the digest entry into the closure exactly as 3.7 predicted).
**Shipped: `scripts/gate-reachability.mjs`** — every tracked production TS under src/ must be in the compiler's own production closure or be declared test support; a declared file that IS in the program, or is untracked, also fails; nonzero floors (120/120; measured 177/182) refuse a broken tsc/git invocation. In `npm run gates` after tracked-surface, plus its must-fail `--falsify` (a planted tracked orphan MUST be reported) beside the oracle falsifiers. Sibling `scripts/gate-reachability.test.mjs` (5 injected-set cases in the unit pool: clean pass, named orphan, stale declaration both ways, floors) — the first staging RED taught it its shape: scripts/*.test.mjs here ARE vitest suites, and a plain node:assert script fails collection ('No test suite found'), which the lab missed because its targeted check ran the file directly instead of through the pool.
**Deleted (16 paths, recorded in `deletions.json` — 3,072 entries):** `reality-gate.ts` (ConPTY smoke of the Ink CLI deleted in 3.2/3.3), `smoke-headless.ts` + `seal:agy-build`/`smoke:headless` package scripts, `seal-agy-build.ts`, `agy-dispatch.ts` (its knip note claimed an external dispatch.sh consumer — measured: zero references there), `memory-baseline-capture.ts` (a golden regenerator for fixtures pinned at a fixed commit — the fixtures are the record; its two now-dead exports `scriptedTurnCapture`/`composeBaselinePrompt` removed from `baseline-fixture.ts` with the header/import), `smoke-claude-buildmode.mts` (knip's project glob never matched `.mts` — it was invisible to dead-code detection all along), `smoke-diff-review.md`, `probe-keys.mjs`, `adr-index-build.mjs`+test (writes into `.council`, which leaves at Phase 7), `scripts/diagnose/**` (6 files; V1 pipeline diagnostics — room diagnostics are ZER0_DEBUG + `.zer0/journal` + the oracle). `dep-graph` package script + knip `ignoreBinaries: ["dot"]` removed with it.
**Kept, with the reason now written into knip.config.js:** `extract-codex-captures.mjs` (regenerates the codex usage fixtures from REAL rollout lines; the decoder is in the closure) and **`tsx` as a devDependency** — the rehearsal caught that deleting the last `tsx …` package script strands it as "unused" to knip while `src/room/room-host-process.ts:16` resolves `node_modules/tsx/dist/esm/index.mjs` BY PATH for the test-support source host (FL-075): a string path is invisible to dead-code analysis, and removing the dep breaks every real-host-from-source test.
**Floors (FL-019 closed):** `gate-clamps` ≥300 files scanned (measured 425), `gate-l5-mandates` ≥100 (measured 182), `gate-oracle-registration` ≥15 registered proofs (measured 20), `gate-reachability` 120/120 — each refuses the vacuous pass a broken file walk would produce. `gate-no-claude-p` already fails closed (its tripwire files are existence-checked).
**F8 decided:** the dead V1 chat-bus kinds stay for M2 (a contract-test rewrite; FL-018) — recorded, not silently kept.
Rehearsed whole on a disposable export of `a74e8ce` (4 lab rounds; the lab caught the tsx strand, the dead fixture exports + their header drift — clamps' @exports check bit exactly as designed — my test's inventory contract, and biome's import order) — then applied to WORK by the same script. Operator verdicts folded this morning: **FL-072 RESOLVED** ("super smooth and fast now, even when it opened" — quiet machine + Windows Terminal; the first run's lag was the saturated machine + AV first-scan + VS Code's terminal), FL-071 in fix (its lane runs in parallel with this commit).

## FL-071 — the working animation retires when the answer arrives (squash of `fl071-spinner` `1765fd1`)
**The operator's contract, verbatim:** the working animation shows while waiting and must DISAPPEAR the moment the answer arrives — answer and animation never together. **The brief's hypothesis (Node event ordering, the FL-015 shape) was REFUTED by the operator's own retained run journal** — the run that produced the checkpoint screenshot: `message.committed` → `lane.completed` measured 1–9 ms across all six lanes; the real window was last-chunk → commit, **0.684–7.910 s per lane, all three agents**, during which the complete streamed answer (byte-identical to the committed text, verified programmatically) sat on screen above a counting spinner. Root, with file:line: `rust/crates/codegen/xai-grok-pager/src/room_scrollback.rs` bound the working row's lifetime to the LANE (created at `lane.started`, removed only at `lane.completed/failed/cancelled`) while `commit_message` had already declared the answer final — the lane outlives the answer, so the row outlived it. The Node host needed no change.
**Fix:** the row is retired ON THE CHUNK that delivers the answer (not the next animation tick — the real-ConPTY test caught the tick-late first attempt with the answer on screen and `working` still under it), with exactly-once removal shared by the delivery, commit-without-streaming, and lane-settle paths. **One ratified exemption:** `cancelling` is not a working animation — it is the room acknowledging the operator's own cancel — so it may render under a delivered answer until the lane settles (its own test + falsifier). Two pre-existing tests had PINNED the old behaviour ("the active status stays below streamed content") — changed deliberately, with the reason in-file: the operator's contract overrules. The two ConPTY `working`-presence waits are now ABSENCE assertions on the real binary.
**Proofs (lane handback, quoted):** new `a_delivered_answer_never_renders_beside_its_working_row` RED pre-fix (spinner + answer rendered together in the panic message) and its falsifier; the cancel-acknowledgement test + falsifier; `cargo test --locked --workspace` exit 0 (pager lib 773, conpty_ui real-terminal 1/10.47 s); `npx vitest run src/room` 157/157; biome 483 files exit 0 at repo scope. Merged by squash; verified whole by `verify:staged`.

## FL-074 — a busy DB no longer manufactures a digest catastrophe (squash of `fl074-digest-busy` `e6ed5e3`)
**Two defects behind the one incident, both reproduced before they were believed.** (a) A redundant digest child could not notice it had nothing to do without first taking a WRITE lock: the settled-check sits after `openMemoryDb`, and EVERY open promotes to a write — `src/evidence/schema.sql:9` `INSERT OR IGNORE INTO _schema_version…` inside `applySchema`. (b) **The busy budget was a fiction:** `applySchema` runs in a DEFERRED transaction, and SQLite deliberately refuses to invoke the busy handler on a read→write upgrade it could deadlock on (sqlite.org/c3ref/busy_handler.html, quoted in the lane handback) — measured: with a second connection holding `BEGIN IMMEDIATE`, the open THREW `SQLITE_BUSY` in **3 ms** despite `busy_timeout = 5000` being set (`db.ts:266`); the old 10-attempt ladder therefore burned only its own backoff, **2.7–3.2 s total**, not the ~50 s its comment assumed.
**Timeline correction to the FL-074 row (from the lane):** the catastrophe's `created_at` is captured at the TOP of `runDigestEntry` — 07:27:01.894 was the child's START, not its death; it threw ~07:27:04.6, ~1.8 s AFTER session 2's watermark landed. It missed surviving by roughly one backoff granule.
**Fix (`src/memory/digest-entry.ts` 178 → 283 lines):** a pre-flight settled-check on a READ-ONLY handle inside the lease (WAL readers never take the write lock — measured 3 ms under a held write; same predicate + same reader as the pass via the newly exported `readWatermarkSet`, so it cannot drift; its only possible action is to DECLINE work, and it fails OPEN); a monotonic wall-clock busy budget (default 30 s — a judgment call recorded as such: below the old intent, far above any observed window; the child is detached and holds only its own 300 s lease); `isBusy` widened to extended `SQLITE_BUSY*`/`SQLITE_LOCKED` codes (the predicate ledger.ts already uses; honestly flagged as defence-in-depth, not this incident's cause); an exhausted budget records ONE catastrophe naming its wait (measured: `busy-exhausted-after-1521ms` against a 1500 ms test budget).
**Proofs (lane handback, quoted):** three falsifiers with REAL forked children + a REAL second connection holding `BEGIN IMMEDIATE` + a real write — RED pre-fix reproduced FL-074 itself on demand ("a REDUNDANT already-settled child writes NOTHING while another process holds the write lock" + the wait-out case + the exhausted-budget case); GREEN 3/3 after (redundant child 4,178 → 1,823 ms; exhaustion 3,140 → 559 ms). **The lane also caught its own first falsifier LYING** — it asserted only scratch rows and passed pre-fix because the catastrophe had gone to `digest-failures.log` (the DB was locked for the child's whole life); all three now assert BOTH durable sinks. Full suite 236 files / 1,912 tests green in the lane; oracle 18/18 + the A6 must-fail; merged by squash; verified whole by `verify:staged`.
**Reported up, not done (shared behaviour above the slice):** making `applySchema` `BEGIN IMMEDIATE` is the other real fix for (b) but sits on EVERY opener including the interactive host's render path — recorded as FL-077 (M2 candidate, its own wave) together with the narrow `BUSY_CODES` in `src/chat/evidence-failure.ts:14`.

## Phase 6 — m0irai: the rename, the brand, and a boot you can see (squash of `phase6-rename` `9dc4664..729d097`)
**Two builders.** The first died at the operator's laptop close mid-brand-work; its 18 in-flight files were committed VERBATIM by the second as `7c60a06` "WIP recovered from the builder that died mid-round" with its two compile errors quoted in the message (E0061 render_card arity, E0560 LogoPalette missing `front`) — the boundary between the two builders' work stays readable in history. The second builder verified rather than trusted: every carried-over claim re-checked against the disk (hero box constants vs `hero_box.rs:17,20,23`; all 12 brand assets `cmp`-identical to the lead's generated handoff set).
**The rename (6A, `9dc4664`):** the shipped command is `m0irai` — Cargo `[[bin]]`, verify-rust `BIN`, every `CARGO_BIN_EXE` site, package.json name+description, and the binary's own voice (`--version`/`--help`/error prefixes; falsifier `the_cli_introduces_itself_by_the_name_it_ships_under`). The room header follows: **"m0irai · the room"** / `m0irai · #<session>` (`app/room_runtime.rs:1006`, `:1138`) with all four pins that guarded the old name changed deliberately — each old assertion restored and run RED first, quoted in the lane handback. package-lock.json's two name fields renamed surgically at merge by the lead (no resolution run; `npm ci --dry-run` exit 0). Out of scope by decision (M2): sidecar filenames, `.zer0/` journal dir, `ZER0_*` env vars, the `zer0.room` protocol id, crate directory names.
**The brand (operator's pick; gemini concurring independently):** the TRISKELION mark — three arms around a hollow center, the Moirai — generated parametrically (`.lead/tools/gen-logo.mjs`) at all 11 asset sizes (≤6-row sizes are a deliberate simplified variant: ring + three heads), and the DOTTED `m0irai` wordmark (braille, slashed 0, `assets/logo/wordmark.txt`), typed on per letter group (groups DERIVED by splitting on blank braille columns, not hard-coded) in a six-stop neon gradient `LETTER_COLORS` = #39FF14 #14F07A #14D9E0 #4E8BFF #8A3DFF #9D00FF (`room_welcome.rs:62`, the single swap point — linear RGB muddied the middle letters and was rejected on the decision page). Tagline: **three minds · one thread**.
**The boot:** a grok-style hero box (mark left; wordmark + `v0.1.0` + tagline right; grok's own V_PAD/H_INSET/LOGO_H_PAD values; one stated deviation — the rounded border is drawn through the room's capability boundary so a legacy console gets ASCII, not box-drawing tofu). The entrance is an EVENT: a neon-green front (`FRONT_COLOR` #39FF14) burns across the mark and cools into the settled shade (`IGNITE_SHARE` 0.16; `REVEAL_EDGE` widened 0.34→0.55 after the operator's verdict on the first cut — "literally nothing changed" — an entrance nobody can see is not an entrance); the wordmark types in place without sliding (anchored, pinned); any keypress skips the animation instantly AND still reaches the composer (falsifier with no timing wait — the entrance clock starts inside the opening paint); content mid-entrance kills the card on that frame; reduced motion opts out entirely. Settled art short-circuits to `lit` so a 256-color terminal's quantization cannot make an entranced card differ from a never-entranced one.
**The pump, and the bug the "one-line fix" exposed:** the redraw pump's guard gained `welcome_entrance_running()` (reinstated as LIVE wiring by lead ruling — the dead-API ruling was about deadness, and a caller ends that). The obvious clock predicate SHIPPED A FIVE-LETTER WORDMARK — the sixth letter reveals at exactly `ENTRANCE_SECS`, the guard is evaluated after the paint, so any paint straddling the boundary retired the pump one frame early. Both real-ConPTY suites caught it on the real binary. Fixed by OBSERVING rather than intending: `welcome_settled_painted` latches inside the one call every card paint makes, when the value it returns is a settled one — the pump owes frames until a settled frame has actually reached the screen (falsifier quoted RED against the clock version). Measured with the pump deliberately armed: a wakeup over a frozen card diffs to zero cells and emits ZERO bytes — the ConPTY probe `settled empty room must not redraw` (state-based wait, load-robust) ships as the terminal-visible guarantee, its comment stating plainly it is NOT the pump falsifier (the unit test is). A settled empty room is now quiet in both senses: no wakeups, no bytes. FREEZE after the entrance stands (operator question — "want it to keep breathing?" — travels WITH the demo).
**The demo (`--welcome-demo`, feature-gated):** plays the full package (r replays, q quits), drives the REAL `run_room` over an empty reducer — absent data renders absent (no usage suffixes, cwd = the process's own, version = `cli::version_text()`). Compiled OUT of release-dist: binary-searched 0 hits; the shipping exe rejects the flag exit 4.
**READMEs (6C, FL-026 → FIXED):** root + rust/ describe this tree honestly.
**Proofs at lane head `729d097` (clean):** `npm run verify` exit 0 — unit 1,911/1,911 (236 files), integration 4/10-skipped + 2/3-skipped, cargo workspace 78 s, host_lifecycle 9, digest_handoff 6, release-dist `m0irai.exe` 8,084,480 B; biome 470 files exit 0; `cargo fmt --check` exit 0; pager lib 805 (run AFTER fmt — the pins' layout-independence proven, not assumed); both ConPTY suites green after the RED demonstrations restored the tree. Merged by squash; verified whole by `verify:staged` (external receipt by tree id; its receipts-table row is added by the next update, as always).

## Boot tuning — the mark is the hero, the shine lives, the name is plain text (squash of `boot-tuning` `522fb8a..e36ebfa`)
**The operator's round-2 verdict on the landed boot, executed** ("logo bigger than the text; keep the shining pulsing/going; text smaller, just like normal text; drop the dots — nice tight and clean") **plus gemini's re-critique and the design DB, which independently concurred on mark-as-hero.** The card is now a vertical lockup in a snug rounded box: the triskelion at 12 rows (the hero), one blank row, the wordmark as PLAIN TEXT `m0irai` (one row, per-character LETTER_COLORS gradient), one dim line `v0.1.0 · three minds · one thread`. The big dotted wordmark is GONE — `assets/logo/wordmark.txt` deleted and deregistered (tracked-surface 2074 → 2073, `--update` as the last provenance step), the letter-group splitter machinery removed with it.
**FREEZE OVERRULED — the shine lives.** After the entrance the card settles into the living ~12 fps shimmer for as long as the room is empty; the pump guard returned to card-visibility (two terms); `entrance_complete` is `#[cfg(test)]` and `card_size` private (no dead production surface). The freeze-era pins were changed DELIBERATELY with the old assertions run RED first and the operator's ruling written in-file. **The byte-quiet ConPTY probe was INVERTED, not deleted:** "settled empty room must not redraw" was right under freeze and wrong by decision now — its replacement `wait_bytes_beyond` proves the settled EMPTY room KEEPS emitting (the shine is alive), state-based like its twin; the shine-STOPS-on-content guarantee is carried by the existing idle-after-content assertion, whose comment now says so.
**The marker design that survived FL-081's trap:** the product name is room chrome on every frame (header, footer, ConPTY console title, cwd), so the suites key the card on the name ALONE ON ITS OWN ROW inside the border — no chrome row reduces to it, and a half-typed row trims to `m0ir`, so one predicate covers card-presence AND settled-state. The first attempt at that predicate failed on the REAL binary (quoted in the lane: the row still carried the border glyphs — "The card was right; the predicate was wrong") — fixed by trimming border+padding before comparing. The demo suite's raw-stream typed-not-printed check was DROPPED deliberately with the reason recorded (the needle was a braille block no chrome could contain; ordinary text made the check fail on chrome, and the reveal math is pinned by unit tests + the replay key is the real-terminal animation proof).
**From the formal handback (numbers and trades):** the card measures **45 × 18** (mark 9 rows after trimming logo12's blank padding + gap + name + meta + V_PAD/border; width = meta 35 + insets), one FIXED hero size — **short terminals (~18–24 rows) now get no card rather than a shrunken one** (card minimum 45×18 measured and pinned by `a_window_too_small_for_the_box_paints_nothing`, which also proves the exact minimum DOES render; the ~47×25 full-terminal minimum is derived, not measured) — a deliberate trade recorded as an OPERATOR PRODUCT CALL if a fallback tier is wanted. The skip is a clock OFFSET, not a latch (a latch would freeze the shine it just restored; winding the `Instant` back can underflow near boot) — it lands exactly on the settle point so the shine picks up without lurching. The lane also caught two bugs in its own tests: the anchoring test searched the WHOLE frame for an `m` and found "three **m**inds" on the meta line (now scoped to the name's row, hazard written down), and three stale comments still describing the freeze era (swept for the class, costing a deliberate second full verify).
**Proofs at lane head `e36ebfa` (clean):** `npm run verify` exit 0 (log `verify-boot-tuning-2` ⚠ **LOG ABSENT** - that file is not on disk; `verify-staged-26.log` proves exit 0 only, not the detailed counts): unit suites green, cargo workspace 75 s, host_lifecycle 9, digest_handoff 6, release-dist `m0irai.exe` 8,075,776 B; pager lib 805/805. Merged by squash; verified whole by `verify:staged` (receipt by tree id; its receipts-table row lands with the next update, as always).

## Release decisions (recorded 2026-08-19/20, operator)
- **Ship soon; free and open forever.** No paywall (9€ idea considered and rejected: kills stars/forks/CV value; the category is free; users already pay vendor subscriptions). Money path if ever earned: sponsors + open-core tier AFTER traction. Licensing already clean: root MIT (CodeZero) + rust/ Apache-2.0 (SpaceXAI copyright + THIRD-PARTY-NOTICES retained) + root NOTICE declaring the grok-build fork; README tells the fork story proudly and invites forks.
- **m0irai itself has NO login, ever** (subscription-first law): it spawns the native CLIs which carry their own auth. Release adds boot-time detection: per-agent "ready / installed-but-not-logged-in (with the exact login command) / not installed".
- **Three seats + a bench.** Discovery finds every installed agent; the room seats three (brand = the Moirai; everyone-answers sanity; warm-session cost); fewer available → seats collapse gracefully (release scope); more → the bench, with click-to-swap (click the agent chip → bench panel → click the replacement) and `/agents`; seating persisted per project; `@` autocompletes SEATED agents only.
- **Vendor matrix (sourced, docs/research/2026-08-19-acp-vendor-matrix.md):** Grok Build speaks ACP natively (`grok agent stdio`, xAI's own docs) — the general ACP slot unlocks grok, Qwen Code, Kimi, OpenCode, Copilot CLI, Cursor (+ Goose experimental); Gemini CLI's consumer path sunset 2026-06-18 (validates the Antigravity chair); DeepSeek ships no official CLI.
- **Release-polish wave scope (after checkpoint 2 + Phase 7):** subset-boot + login detection · state-aware Ctrl+C cancel (FL-089) · tool-output folding (FL-088) · buried-answer both-fixes (FL-090) · jump-to-my-last-message (FL-092) · README quickstart with the fork story. Post-release: general ACP slot + bench UI (grok UI adoption map: docs/research/2026-08-20-grok-ui-adoption-map.md) + the agent-synergy program (reviewed 2026-08-19, adopt as the post-standalone program).

## Research day 2026-08-20 — the channel inventories, the usage root cause, and the locked display policy
**Three sourced inventories land in `docs/research/` with this commit:** the ACP full inventory (every update kind the pinned adapters emit, each marked used/DISCARDED with the discard site — headline: per-turn token breakdowns from BOTH vendors die at ONE line, `acp-turn-session.ts:304-307`; Plan/TodoWrite checklists have ZERO consumers; our own patch already streams claude subagent lifecycle to a single debug line), the agy channel inventory (agy 1.1.16: `--output-format stream-json` is a vendor-documented NDJSON stream with `tool_info` + `subagent_info` we never enabled — gemini activity AND subagent parity with no MCP work; the statusLine payload we already poll carries `agent_state` + `transcript_path`, silently dropped by our Zod schema; `--input-format stream-json` may allow a persistent gemini session — EXPERIMENT before designing on it), and the grok UI adoption map (landed with the boot-tuning commit). The capability matrix crossing all three is the operator's menu (page under `.lead/reviews/`); their pick order is pending.
**FL-094 root-caused end-to-end from the operator's own room journals** (`.council/runs/*/room-events.jsonl`): agent.status snapshots replace wholesale (deliberate, pinned — reducer.rs:668); claude's producer carries known windows forward, codex's does NOT (`usage-reporter.ts:357-361` folds claude only) — every codex ctx-only ACP update erased its weekly; the post-turn rollout read returns stale instantly (`codex-rate-limits.ts:135-138`) instead of waiting like claude's 5 s poll; the boot prefetch once published an already-EXPIRED window (correctly hidden by the render — the "codex blank at boot" frame); the 96%→0% weekly flip was a REAL window rollover at 03:34Z (both true). claude's 5h IS collected but arrives end-of-turn after three ctx-only clobbers, and a SILENT 3 s race in the vendored patch (`acp-agent.js:2432-2455`) can eat it entirely — timeout and "no plan limits" are indistinguishable today (FL-099). ACP's `usage_update` is context-only BY SPEC (quota rides `_meta` for claude, never for codex) — ACP owns context freshness, the file sources own quota; merge, never race. **V1's real display rule quoted at source** (`usage-display-policy.ts:42-44`): weekly shows unconditionally when no 5h number exists (codex's only meter always shows); the ≥75 gate applies only when a 5h is present — V2 ported 2 of 3 rules and dropped exactly this one (gemini's wk 1% over-display).
**THE LOCKED DISPLAY POLICY (operator-ruled 2026-08-20):** boot = NO meters for anyone (name+mode only; the codex boot prefetch is to be removed — ⚠ NOT YET LANDED, corrected 2026-08-20 per codex docs audit #6: `startEagerSessionBoot` still calls `prefetchCodexQuota` at `src/chat/eager-session-boot.ts:83`, still emits rollout usage at `:109` and still installs `bootUsageTap` at `:136`. This paragraph states the operator's LOCKED POLICY, which slice H implements; it is not a record of shipped behaviour); after the first message: ctx always · 5h always for vendors that have one (claude, gemini — codex's was removed by OpenAI: 3,497 rollout files scanned ⚠ [EVIDENCE NOT RETAINED - the scan corpus and command transcript were not kept; do not restate as proved], June 786 with 5h → August 0) · weekly per V1's verbatim rule · absent renders absent, never a fabricated zero (the boot `wk 0%` was REAL data, verdict confirmed twice). Fix = **slice H** (`docs/specs/2026-08-20-slice-h-usage-fix-contract.md`): keyed carry-forward fold at the seam both codex producers cross, bounded codex read, a receipt for the 3 s race, V1's `fitCompactMeters` port (truncation at meter boundaries — never inside a number), reducer untouched. Codex hostile referee dispatched on the contract (uncapped — the operator's weekly reset).
**Also decided today (operator):** boot design LOCKED (living shine stays; short-terminal no-card trade accepted) · codex usable without limits again · the release-polish wave spec v2.1 + slice-H contract are tracked in `docs/specs/` from this commit so no plan lives only in an excluded folder.



## POSITION 2026-08-21 13:1x — where the WAVE stands, asked for by the operator

**The wave is 8 slices, A…H** (`docs/specs/2026-08-20-release-polish-wave-spec.md`, schedule at §12.2).
Six months of defect work today has been FL-rows, not slices; this is the slice ledger.

| slice | what it is | state |
| --- | --- | --- |
| wave 0 | seam foundation, no feature | ✅ merged `e10969a` |
| B | state-aware cancel | ✅ merged `632a5c0` |
| C | tool-output folding | ✅ merged `8986d76` |
| H | usage display fix | ✅ merged `dc14a8a` |
| **A** | **subset boot + login detection** | ⚠ **PARTIAL — see below** |
| **D** | **buried answers, both fixes** | ❌ **not started — UNBLOCKED, C is landed** |
| **E** | **jump to my last message** | ❌ not started — needs D |
| **F** | **README rewrite** | ❌ not started — needs A |
| **G** | **mouse-capture toggle, conditional** | ❌ not started — no stated dependency |

⚠ **SLICE A IS THE MESSY ONE AND ITS STATE IS NOT FULLY ESTABLISHED.** A's Rust half (`d25a1b9`) rode into
the merged tree on the H squash — `.lead/DO-THIS-NEXT.md:180` records it as *"a half-built slice A rode into
the merged tree"*, so **some H-review blockers are against work nobody claimed was finished.** The branch
`slice-a-h` head is `376bd72` (*"the remedy lines go under the card, not through it"*), recorded as unmerged
and held back because merging would *"move the tree off the proven version with no time to re-verify"*.
⚠ **THE LEAD COULD NOT CONFIRM THIS CHEAPLY AND SAYS SO RATHER THAN GUESSING:** `git merge-base --is-ancestor`
reports both `5bc4966` and `376bd72` as non-ancestors, **but that instrument cannot see a squash merge**, and
H landed as one — so the ancestry answer is uninformative, not negative. **A content-level check on a quiet
machine is the open action.** Do not record A as done or as missing until that runs.

**WHAT IS DISPATCHABLE THE MOMENT BUDGET ALLOWS, in order:**
1. **D** — needs C, and C is landed. The only wave-2 slice with no blocker.
2. **G** — the spec gives it no dependency.
3. **A's remainder → then F**, once A's true state is established.
4. **E** — needs D.

⚠ **NOT DISPATCHED 2026-08-21 13:1x ON THE OPERATOR'S INSTRUCTION** — *"dont launch more that we have now
since we are at the 97 usage and we got reset at 3"*. Four fix lanes and one codex review were already live
(`fl146-cancel-race`, `fl141-cancel-row`, `fl144-fourth-seam`, `fl133-room-cancel-proof`). **The cap is a
budget decision, not a technical one; D and G are ready to go the moment it lifts.**

⚠ **AND THE HONEST FRAMING OF TODAY:** the wave has shipped **three of eight slices** since wave 0, and every
hour since the operator's morning test has gone into defects they found in five minutes of using the app —
FL-125 through FL-147, of which FL-146 (a cancel that misses a connection still opening) is their original
report finally diagnosed. **That is not drift; the app was reporting failures on a clean cancel and taking
agents offline. But it means the slice ledger has not moved today and the operator asked the right question.**

## Wave 0 — the seam foundation three parallel builders work across (squash of `wave0-foundations` `6844cca..8605fd6`, 14 lane commits)
**No feature shipped.** Wave 0 exists so slices A, B, C and H can be built in parallel without fighting over the same predicates: one busy predicate, one guidance-row owner, retained rects, and a defaulted `quit_armed_until: Option<Instant>` on `RoomView` (`room_view.rs:109`, doc comment ending *"Written by nothing yet."*) that slice B consumes. Payload is 7 files, 1,887 insertions, 91 deletions — the 1,566-line `room_runtime_seam_tests.rs` is most of it.
**Merged by hand, and the method was the risk.** A codex audit of the lead’s own merge material returned **9 BLOCK** (`.lead/reviews/codex-audit-the-lead-2026-08-20.md`, verdict: *"the lead’s merge material is unsound and must not be executed"*). Four died with the discarded `apply-wave0-merge-docs.mjs` — one of its five anchors matched **zero** times against spec v2.4, repairing it would have produced duplicate §3 rule 7s, its `// ATOMIC` claim was three bare `writeFileSync` calls in a loop, and its `VERIFY_LOG` dependency was circular. Five were merge findings and were handled here.
**The doc-conflict scare was a diff artifact, and naming it matters more than the merge.** `git diff master..wave0-foundations` appeared to show the lane rewriting `docs/FINDINGS.md`, `docs/HANDOFF-m0irai.md`, `docs/STATE.md` and both 2026-08-20 specs — 1,599 lines on the wave spec alone, which would have reverted v2.4 to a v2.2-era text. **The lane never touched any of them.** The merge base is `6844cca`, not master; every one of those files changed on *master’s* side of the fork. Proved by splitting the diff at the base (`git diff --name-status 6844cca..wave0-foundations` lists 7 files, none of them docs/) and by an empty `comm -12` against a positive control that returns all 7. `git merge --squash` therefore passes the docs through untouched **by construction** — but `git checkout wave0-foundations -- docs/`, `git merge -X theirs`, or resolving a hunk by “keeping the lane’s side” would each have destroyed master’s doc work silently. The method was the only thing standing between the record and that loss.
**Verification.** The lane tip’s own `npm run verify` was GREEN (`D:/m0irai-evidence/wave0/verify/verify-8605fd6-GREEN.txt`, md5 `6ea7f418419be185a4eaaf61df186f36`, six zero-status steps, `legacyGlyphTests: 11`), and the frame dump is byte-identical across eight tips (`base-v2.txt` and `tip-8605fd6.txt` both md5 `84b4625fc6f55dff34fffe6919ede44c`, against a mutation control that differs). ⚠ **Neither is a receipt for the merged tree** — the lane log was an in-place verify against the lane’s own (older) docs. The authoritative proof is this commit’s `verify:staged`. Code-carry was checked before staging: `git diff --cached wave0-foundations --name-only -- rust/ scripts/` empty, against a docs/ positive control that is not.
**`scripts/verify-rust.mjs` gained a third gate step** — `cargo test -p xai-grok-pager --lib seam_tests` under `GROK_FORCE_LEGACY_CONSOLE=1`, because a chip pin that PANICS on ASCII had shipped green (it located a chip by the identity glyph, and codex’s ASCII glyph `#` is also printed by the metadata row). The counted-test assertion was **extracted** rather than copied a third time, because a third inline copy pushed `verifyRust` past the cognitive-complexity clamp — ratchets only fall. `rust/README.md` renumbered to match, which is FL-111’s fix.
**Ledger:** FL-104…FL-113 added with this commit, per the same-commit rule. Six of the ten drafted rows were **wrong at the merge tree** and carry their corrections in place — see the FINDINGS ledger-history entry for the list. FL-112 (the packaged-boot gate) had no drafted row and was written from scratch; FL-113 is the chord-table row renumbered off FL-103, which spec v2.4 had taken for the picker/Ctrl+C defect.

## POSITION 2026-09-06 — resume after the 2026-09-03 pause (operator: usage limit); every fact below from a command run 2026-09-06 10:2x–10:5x

**Branch `overnight/2026-08-21` @ `1bd57d2`; `master` untouched at `c6d1e95` — nothing is pushed until the operator tests a staged build and approves.** No agent survived the pause (ListAgents: none); the lead is the only writer. The stack above `4087a2f` (the last commit with a receipt, RED under load — see the receipts table), oldest first, from `git log --oneline 4087a2f..HEAD`; the proof column quotes what was run on the merged INDEX before each commit (the full record is the TOP block of `.lead/RESUME-2026-08-21-AFTERNOON.md`, outside the tree, and the lane reports under `D:/m0irai-evidence/wave1/lanes-0901/`):

| commit | lane | what landed | proof on the index before the commit |
| --- | --- | --- | --- |
| `9f03907` | M5 (session 2, squash of 18 lane commits at `9956fdb`) | the evidence DB opens in migration tiers — readiness, FK posture, tier rollback, open-race atomicity | typecheck 0, lint + gates 0, vitest src/evidence + src/chat + src/memory 177 files green; tracked-surface 2160 |
| `eca83c1` | ledger | FL-150..FL-180 written from the 2026-08-21..25 record (31 rows) | validator: ALL 179 ROWS VALID |
| `e7a01c0` | M4 (squash of `m4-file-projection` `757fd15`) | `journal_entry_files` projection — v21 migration on M5's tiers, drift repair that announces itself, plan pins at 1/2/4 keys | typecheck 0, lint + gates 0, vitest src/evidence src/memory src/chat 183 files green, build 0, oracle ok:true; tracked-surface 2165 |
| `60c2f25` | D (squash of `d-buried-answers` `b24fb19`) | buried answers relocate below the prompt that displaced them, with a pill and Ctrl+T while they are off screen | lint 0 (biome 543 files, 7 gates), tracked-surface 2168 unique = count, cut-closure 0, cargo fmt 0, `cargo test --locked --workspace` 3313 passed 0 failed |
| `840c05f` | FL-175 (squash of `fl175-flaky-deadlines` `7238b69`) | the src/room process-spawn tests stop being wall-clock lottery — waits derived from budgets, budgets from measurements, shutdown failures never mask the real one | typecheck 0, lint 0, tracked-surface 2169 unique, cut-closure 0, reachability 194/201/7/0, l5 0, target suite 43 files / 229 tests green (275.88 s, under load) |
| `459540b` | M1b (squash of `m1b-briefing-boundary` `2de6c6e`) | no stored text escapes the briefing frame, no shortening splits a code point, marker disguises detected without touching real text | suites 148 files / 1,381 tests green on the index |
| `1bd57d2` | RP (squash of `rp-rust-parity`) | a failed lane's streamed text survives reload, both consoles are proven, a rebuild is linear in the room's own history (8,000-row reload 238.6 s → 252 ms) | cargo workspace 3322 passed 0 failed, legacy-console pager 1023/0, fmt 0, lint 0, tracked-surface 2175 |

Batch size: `git diff --stat 4087a2f..HEAD` = 78 files, +14,780 / −1,137.

**None of the seven carries a GREEN `verify:staged` receipt of its own** — every attempt window had the box at 97 % CPU from other sessions, and the src/room process-spawn tests are wall-clock tests (the 2026-09-01 RED on `4087a2f` was 10 timeouts and nothing else; FL-175 fixed those budgets at `840c05f`). The batch is proved ONCE, by the receipt of this docs commit's own tree: `node scripts/verify-staged.mjs --assert-head` is the statement, and the receipts table gets this commit's row in the next update, as always. The operator-process rule (2026-08-25) is full gates once per batch at merge, not per lane.

**Parked, unmerged, worktrees live (`git worktree list`):** H2 `009f163` on `h2-hermetic-gates` at `D:/m0irai-work-h2` (round 3 — the hermetic-seams gate is AST-based, cut-closure reads the previous inventory through an override file inside the verify:staged stage, tracked-surface refuses on an unresolved index; awaiting its final delta review) · AG `9352853` on `ag-agy-transcript` at `D:/m0irai-work-ag` (round 2 — a resumed gemini turn finds its own answer and only its own; awaiting its delta review; the F5 follow-up is a carrier change outside AG's surface: a classified reason for a text-less gemini turn instead of the generic failure at `src/chat/headless-carrier.ts` `markEmptyOutputFailed`) · MN `dacf356` on `mn-memory-notice` at `D:/m0irai-work-mn` (the one-quiet-grey-row briefing-failure notice as a durable `room.notice` event, never reviewed; will conflict with RP in `room_scrollback.rs`). Briefed, not started: SL (`session-store-integrity.md`), PB (`perf-boot.md`) under `D:/m0irai-evidence/wave1/briefs/`. Deliberately unmerged: `fl130-busy-guard`, `fl130-flight-recorder` (FL-130 OPEN — needs a capture from the operator's real terminal).

**The app last FAILED to boot for the operator** (2026-09-02, build `D:/m0irai-demo/main-60c2f25`, screenshot: `m0irai: host request timed out: zer0-request-2`): request 2 is `session/new`, which shares one fixed 20 s startup budget with `initialize` (`rust/crates/zer0-v2-bin/src/cli.rs:10` `STARTUP_TIMEOUT`), and the host answers it only after the synchronous first-open DB migration and the git liveness spawns — on a 100 %-CPU box that took longer than the budget. UNVERIFIED whether the same build boots on a quiet box; a rebuild at `1bd57d2` and a retest are the next operator-facing step, and a fix lane (progress-based startup waiting instead of a fixed budget) is a candidate.

**Ledger rows owed at the end of this batch, in one pass when H2/AG/MN land:** the codex quality findings CQ-02/03/04/05/08/09 (7 of 9 confirmed by the Opus triage, `D:/m0irai-evidence/wave1/lanes-0901/codex/cq-triage-r1.md`), FL-181 (M1b's data-loss row, written in its report, not yet in `docs/FINDINGS.md`), corrections to FL-135 / FL-156 / FL-172 / FL-174, R5-F5 closed by H2, the four sealed-lane findings, RP-4 / RP-6 (the displaced-row worst case, 10.6 s, still open), the FL-143 `host_lifecycle` 5 s deadline pattern (pre-existing, measured by two builders — a budget lane is owed), the conformance-corpus drift MN found (a regenerate-and-diff gate is owed), and the startup-deadline finding above.

## POSITION 2026-09-08 — the harness/ACP wave's first batch: 13 commits above `dc6b721`, proven once by this tree's receipt; every fact below from a command run 2026-09-08 (wall clock 15:0x–19:3x)

**Branch `overnight/2026-08-21` @ this commit; `master` untouched at `c6d1e95` — nothing is pushed until the operator tests a staged build and approves.** The last GREEN receipt is the tree of `dc6b721` (`fd3681eb…`, the 2026-09-06 docs commit). The stack above it, oldest first, from `git log --oneline dc6b721..HEAD`; the proof column quotes what was run on the merged INDEX before each commit (the full record is the TOP block of `.lead/RESUME-2026-08-21-AFTERNOON.md`, outside the tree, and the run log `D:/m0irai-evidence/wave1/release-plan-2026-09-06.md`; lane reports under `D:/m0irai-evidence/wave1/lanes-0901/`). The spec for this wave is `docs/specs/2026-09-07-harness-acp-wave-spec.md` (committed with this update).

| commit | lane | what landed | proof on the index before the commit |
| --- | --- | --- | --- |
| `e549e96` | H2 (squash of `h2-hermetic-gates` `009f163`) | the hermetic flag cannot leak by spelling, the cut's gates test themselves, and hermetic seams are a gate | reviewed MERGEABLE after three rounds; index checks in the run log (2026-09-07 04:1x) |
| `1658644` | MN | a lost memory briefing is announced in the room as a notice on both halves, and the memory failure log names its cause | generated-inventory conflict only; typecheck 0, biome 0 on the reconciled tree |
| `a37ce45` | RS | a paged resync seeks the cursor by binary search and reads the journal once per page, instead of rescanning history from the start | two source conflicts with MN reconciled by hand (`@exports` header union; `room-engine.ts` imports `seekAfterEventSeq`, drops the chunker M1b replaced); typecheck 0, biome 0 |
| `8024867` | BRIDGE | the ACP bridges and their bundled CLIs re-pinned to the registry's latest, the vulnerable production packages gone, a stale pin is a number the release can read | generated conflict only; `npm ci` 0 after (1 patch); typecheck 0, biome 0 |
| `ed33c0a` | BOOT rounds 1–2 | the terminal waits on the host's own progress instead of a fixed 20 s, names the stage that stalled, and prefers the node.exe beside it | source conflict in `room-host-support.ts` reconciled as the union; typecheck 0, biome 0. Receipt attempt 1 on this tree: RED at gate-clamps (`room-host.ts` 601 lines after MN + BOOT) → next row |
| `201316f` | lead extraction | the recovery reconciliation moves to `src/room/room-host-recovery.ts` (+ sibling test); `room-host.ts` 588 lines | clamps, tracked-surface 2214, l5, reachability, oracle-registration, lint, typecheck green. Receipt attempt 2: RED at typecheck (the new test's untyped mock, TS2493 — a lead edit after the proof) → next row |
| `40e30d5` | lead | the test's mock typed | receipt attempt 3: RED on ONE wall-clock Rust test (`boot_progress::tests::readiness_that_never_arrives_is_a_named_stall_like_every_other_step`, `tests.rs:199`) with every Node suite green (7 gates, vitest 314 files / 2502 tests, integration 4 passed / 10 skipped, oracle ok) — the budget class BOOT r3 replaced |
| `45a04c6` | SL (squash of `sl-session-store` `6d1e0a9`) | the session store never loses a room in silence; a damaged transcript is named, quarantined and rebuilt instead of hidden | typecheck 0, lint 0, l5 0, reachability 208 src / 0 orphans, tracked-surface 2224, cut-closure 0, vitest touched tests + src/room 55 files / 321 passed |
| `1c27abe` | BOOT r3 (squash of `boot-fix-r3` `39daded`) | a refused Node is never the one that runs, and the test budget comes from a measurement | cargo fmt 0; `cargo test --locked -p zer0-v2-bin`: lib 43, conpty_ui 3, room_dependency_closure 2, transport_protocol 14, 0 failed; tracked-surface 2224; cut-closure 0 |
| `911d1b0` | SL-A (squash of `sl-a-notice` `af67805`) | a room rebuilt from the evidence ledger says so, once, with a condition that is true only when something was restored | typecheck 0, lint 0, vitest src/room 51 files / 285 passed, cargo fmt 0, `cargo test --locked -p xai-grok-pager` 1026 passed, tracked-surface 0, cut-closure 0 |
| `a14e95e` | L2 (squash of `l2-drain-receipt` `1fcc1a1`) | a verified drain receipt: enumerate before kill, identity by creation time, fail-closed on total enumeration failure, no test reaches a real signal on any platform, Windows-only assertions guarded | one provenance conflict regenerated (tracked-surface 2227); typecheck 0, lint 0 (biome + 7 gates), vitest src/shared/kill-tree + process-table + src/adapters/acp + src/memory 55 files / 533 passed, cut-closure 0, l5 0, reachability 209 src / 0 orphans, oracle-registration 21 proofs, hermetic-seams 212 files / 20 seams |
| `701158e` | ledger (squash of the ledger lane `48fd4a1`) | `docs/FINDINGS.md` true at `a14e95e`: rows moved by the four merges re-read, FL-202 half-closed, new rows for BOOT r3, SL-A, L2, the test runner and the codex BOOT findings | validator ALL 253 ROWS VALID |

Batch size: `git diff --stat dc6b721..HEAD` = `131 files changed, 13491 insertions(+), 1794 deletions(-)`.

**Receipts.** The three RED attempts of 2026-09-07 (trees of `ed33c0a`, `201316f`, `40e30d5`, 15:12–15:55 UTC) each failed on exactly one item — a clamp, a type error, one wall-clock Rust test — and the first two were fixed on mainline the same evening; the third is the class BOOT r3 replaced with a measured derivation. The batch is proved ONCE by this docs commit's own tree: `node scripts/verify-staged.mjs --assert-head` is the statement, and the receipts table gets this commit's row in the next update, as always.

**Reviewed, NOT merged, fix rounds open (worktrees live):** L0 identity contracts `c4ef130` on `l0-identity-contracts` — NOT MERGEABLE (Opus reviewer-max 5 BLOCKING + 10 IMPORTANT: TypeScript-vs-Rust wire disagreements, a digest collision from unescaped joins, an unbounded roster, a branded alias type never created; codex cross-check 9 IMPORTANT) → fix round 2 from the consolidated list · L1 seat discovery `c136a3f` on `l1-seat-discovery` — NOT MERGEABLE (Opus B-1 + IMP-1..6: a stale tracked-surface row, the credential denylist in `acp-seat-config.ts` / `child-env.ts`, the scrub missing `src/evidence/queries.ts`; codex cross-check CRITICAL: seat credentials reach SQLite past the post-serialization scrub, plus 8 IMPORTANT) → fix round 2, and the vendor table's claude row flips to `none` (decision D1) · BOOT round 4 on the merged code (`boot-fix-r4` from `911d1b0`; codex on `1c27abe`: duplicate PATH entries eat the candidate allowance, discovery depends on PATHEXT, relative entries come back relative, slow probes erase the diagnostics) · H-0 `9d2065e` on `h0-fl143-race` awaiting its review · REL WIP `044f552` on `release-artifact` (round 2: Node 24 is the runtime; legal — DCO for contributors, Apache-2.0 §4(b) modified-file notices for the grok-build fork, Claude Code NOT bundled so no Commercial Terms and no first-start fetch).

**Operator decisions recorded this batch:** 2026-09-07 — Node 24 is the runtime; the legal work is done properly before open-sourcing. 2026-09-08 — D1 Claude Code is not bundled ("everybody will already have it installed"); D2 DCO; D3 the grok-build attribution ships and the per-file modification notices are owed. Review order: codex cross-check on every lane review; never gemini.

**The app last FAILED to boot for the operator** (2026-09-02, build `main-60c2f25`, `host request timed out: zer0-request-2`). Since then BOOT rounds 1–3 landed: the terminal waits on the host's own progress and names the stalled stage; a refused sibling Node never runs; a missing Node is a four-line refusal at exit 4. Whether this tree boots on the operator's box is UNVERIFIED until the staged build at this commit is tested — that is the next operator-facing step, right after this tree's receipt.

**Not started (spec v3):** L3 generic ACP runtime (after L0 merges; its brief carries L2's I10), L4, L5, L6, L7.
