# agy/gemini channel inventory — 2026-08-20 (lead's transcription of the agy-seam-scan report; full text in the session transcript)
agy 1.1.16 live-verified on this machine (`agy --version`, full `--help`). Our adapter uses ONE channel today (buffered stdout read after exit, then overridden by scraping the undocumented brain transcript). At least four unused channels exist, three vendor-documented.

## Channels
| Channel | What flows | Used today | Could power | Push/pull |
|---|---|---|---|---|
| Buffered stdout (text mode) | raw ConPTY render | USED: agy-runner.ts:121-123 buffers to exit; agy-output.ts strips/extracts | nothing new (fallback) | pull after exit |
| brain transcript.jsonl | structured steps: type (USER_INPUT/PLANNER_RESPONSE/LIST_DIRECTORY/SEARCH_WEB…), source, content, tool_calls[{name}] (shape pinned agy-transcript.test.ts:39-61) | USED-BUT-WASTED: agy-transcript.ts:103-116 reads EVERY step, keeps only the last MODEL line — all tool_calls discarded | the tool-step activity lines claude/codex get | pull after exit (live-tail untested) |
| statusLine payload (global settings statusLine.command; vendor doc /docs/cli/statusline) | FULL schema incl. **agent_state: idle/thinking/working/tool_use/initializing**, **transcript_path**, conversation_id, model, context_window, vcs, tool_confirmation_pending, task_count, quota{gemini-5h, gemini-weekly} | PARTIAL: agy-statusline-payload.ts:19-31 Zod reads ONLY ctx/cwd/quota/session — agent_state + transcript_path silently dropped (no .strict()); polled post-dispatch (usage-reporter.ts:309-323) | agent_state = live "what is gemini doing"; transcript_path retires the heuristic brain-dir scan (agy-transcript.ts:82-100) | vendor frames as TUI event; mid-turn firing in headless UNVERIFIED (post-turn proven) |
| .agents/mcp_config.json reverse path (PROVEN seam) | probe artifacts re-read this session: server/discover→initialize fallback (negotiated 2024-11-05); tools/call _meta carries **antigravity.google/artifacts_dir + conversation_id + progressToken**; client capabilities elicitation{form,url} + roots.listChanged | NOT used (Phase 1 of the synergy plan) | gemini voluntarily calling zer0_context / pushing structured results mid-turn; conversation_id/artifacts_dir handed free | PULL from gemini's side only (MCP is client-initiated); notifications/progress renderability UNVERIFIED |
| **--output-format json** (SOURCED /docs/cli/headless; flag live in 1.1.16) | one envelope: conversation_id, status (SUCCESS/ERROR/CANCELED/INTERRUPTED/INVALID/WAITING/RUNNING), clean response, usage{input/output/thinking/cache_read/total tokens}, duration_seconds, num_turns | ABSENT from our argv (buildAgyArgs agy.ts:443-475 never sets it) | replaces heuristic conversation-id capture + reply extraction with documented fields | pull, one-shot, reliable |
| **--output-format stream-json** (SOURCED + changelog-confirmed, bug-fixed once) | NDJSON progressive on the SAME stdout: init(cwd,tools,permission_mode,model,agent) · **step_update(step_type, tool_name, text_delta, tool_info{name,parameters,output,error}, subagent_info{subagents:[{type_name,role,conversation_id,log_uri,workspace_uris}]})** · result(=json envelope) | ABSENT | **the richest channel**: live gemini activity lines + SUBAGENT display with zero MCP work; needs collectAgyTurn to line-parse incrementally (even post-exit full-buffer parse beats transcript heuristics) | pull at process level, incremental at pipe level — a line-tailer = real-time |
| **--input-format stream-json** (live --help ONLY; no docs page) | "one NDJSON message per line... runs a turn for each; requires --output-format stream-json" | ABSENT; behavior UNVERIFIED, never run | potentially a PERSISTENT BIDIRECTIONAL stdio session — closest thing to ACP for gemini; could retire spawn-per-turn | UNKNOWN — dedicated experiment required before designing on it |
| --log-file | CLI log redirect (issue #60 diagnostics used the default) | not used | unknown diagnostic stream | UNVERIFIED |
| agy mcp subcommand | manage MCP servers | not used (we write .agents/mcp_config.json directly, proven) | minor alt registration | UNVERIFIED |
| native send_message/manage_task/schedule tools | agy's OWN inter-agent messaging (docs/subagents) | N/A | NOT ours — internal to agy; do not confuse with the MCP reverse path | N/A |
| permission-denial notice (stderr→ConPTY) | vendor-confirmed soft-deny notice | USED: agy-output.ts:91-98 | covered | N/A |

## Top 3 opportunities
1. **Turn on --output-format stream-json** — vendor-documented NDJSON w/ tool_info + subagent_info on the pipe we already read: gemini activity lines AND gemini subagent display, zero MCP work.
2. **Stop discarding agent_state + transcript_path** from the statusLine payload we already poll — widen one Zod schema; transcript_path retires the undocumented brain-dir scan outright.
3. **The proven MCP reverse path** for what it's structurally good at: gemini voluntarily reading room context / pushing structured results, with conversation_id/artifacts_dir free in _meta.

## Honest ceilings
No unprompted push (MCP is client-initiated; nothing can interrupt gemini's turn to ask). No ACP (issue #31; the reason the one-shot adapter exists). Interactive-TUI-only panels (/agents, /tasks, /teamwork-preview) unreachable from headless — their data only via stream-json where mirrored. statusLine mid-turn firing in headless: unconfirmed, not disproven — the one open question that most changes opportunity 2's value.

## Sources
Adapter+chat sources read (agy.ts, agy-output/transcript/runner/pty, agy-statusline-*, usage-reporter); probe artifacts D:\Zer0 Chat V2\.zer0\mcp-probe-test\* (4 files); synthesis doc §6; 2026-06-17 agy capabilities doc; vendor matrix; curl'd antigravity.google/docs/cli/{headless,mcp,reference,statusline,permissions,plugins,modes,subagents}.md + /docs/{mcp,subagents}.md + changelog + GH issue #60. Live: agy --version (1.1.16), agy --help.
