| # | Sev | Claim/Finding | Evidence (file:line, command + output) | Consequence for the operator | Fix |
| --- | --- | --- | --- | --- | --- |
| 1 | P0 | **OBSERVED —** The 14-crate Rust premise is false: the shipping binary also compiles one `prod/` crate and four `third_party/` crates. | **OBSERVED —** Parsing `.cache/target/release-dist/zer0-v2.d:1-2` returned `crate roots=14`, `crate .rs=215`, `prod .rs=11`, `third_party .rs=66`, `all .rs=292`; “220” is 215 Rust files plus five non-Rust inputs. `cargo tree --locked --offline --target x86_64-pc-windows-msvc -p zer0-v2-bin -e normal,build -i prod-mc-cli-chat-proxy-types` produced `prod-mc-cli-chat-proxy-types -> xai-grok-config -> xai-grok-pager-render -> xai-grok-pager -> zer0-v2-bin`. The equivalent command for `mermaid-to-svg` produced `mermaid-to-svg -> xai-grok-mermaid -> xai-grok-pager -> zer0-v2-bin`. Manifests: `xai-grok-config/Cargo.toml:13`, `xai-grok-mermaid/Cargo.toml:23-25`, root `Cargo.toml:181`. | **INFERRED —** Deleting `prod/` and `third_party/` as P4 requires makes the fresh-clone Cargo build fail before the product can ship. | **INFERRED —** Replace or relocate those dependencies under tests first, or change the contract to the observed 19 enabled local packages. Recompute the result from `cargo metadata`, `cargo tree`, and a new `.d`; do not reuse “14.” |
| 2 | P0 | **OBSERVED —** `git subtree add` imports the entire Rust source tree and its history; “trim workspace members” removes neither. | **OBSERVED —** Spec S4 is `docs/specs/2026-08-14-standalone-v2-spec.md:208`. `git ls-tree -r --name-only zer0-v2/b13-shipping` returned `all_files=3166`, `prod_files=12`, `third_party_files=77`, `crate_files=3063`; `git rev-list --count` returned `27`; `git count-objects -vH` returned `size-pack: 39.45 MiB`. | **INFERRED —** The resulting working tree immediately violates P4, and the supposedly clean public history still contains every removed Rust surface. | **INFERRED —** Build and verify a Rust file allow-list first, then import a separately filtered Rust history. If history cannot pass a secret, provenance, and licence audit, use a vetted squash plus explicit source provenance instead. |
| 3 | P0 | **OBSERVED —** The shared protocol corpus belongs to neither source repository, and both retained test suites use path depths that become wrong after relocation. | **OBSERVED —** `git ls-files protocol` returned `node_repo_protocol_files=0`; Rust `git ls-tree ... -- protocol` returned `rust_repo_protocol_files=0`; the outer workspace contains `39` protocol files. Node reads `../../../../protocol/conformance/v1/` at `src/room/room-protocol.test.ts:23`. Rust uses `../../../../../protocol/...` at `crates/zer0-room-protocol/tests/conformance.rs:23-117` and `crates/zer0-v2-bin/tests/transport_protocol.rs:12-24`. S4 names no operation that imports the outer corpus. | **INFERRED —** The retained Node test fails under `npm run gates`. Rust’s corresponding break stays hidden because P1 runs `cargo build`, not `cargo test`. | **INFERRED —** Import the corpus with recorded provenance, resolve it from stable repository roots rather than fixed ancestor counts, and add `cargo test --locked` to baseline and fresh-clone proof. |
| 4 | P0 | **OBSERVED —** P4 deletes a fixture required by the spec’s own sibling-test rule. | **OBSERVED —** P4 bans all `.zer0/` files at spec `:72-75`; `src/chat/agent-prompt-stack.test.ts:39-44` reads `.zer0/workflows/adversarial-locked-override.md`; `git ls-files .zer0` returns that exact file. Separately, spec `:168` requires `.zer0/state.json`, contradicting P4. | **INFERRED —** Following P4 makes `npm run gates` fail; retaining either file makes P4 fail. | **INFERRED —** Move the adversarial fixture under a tracked test-fixture directory and define P4 as “no tracked runtime state” if `.zer0/state.json` is generated rather than committed. |
| 5 | P0 | **OBSERVED —** The oracle sequence cannot exercise the database and cannot cleanly invoke the stated shutdown. | **OBSERVED —** The oracle sends `initialize`, then only a deliberately invalid `session/new`, then shutdown at spec `:180-188`. `zer0-v2-host.ts:241-247` makes `initialize` validation-only; `:251-254` requires an attached room before room shutdown; the wrong-CWD request is rejected before `createRoom`; `src/evidence/db.ts:275-280` reads `schema.sql` only when the DB opens. | **INFERRED —** The real host cannot complete the described green sequence, while a targeted fake can pass by checking that `schema.sql` merely exists. The deletion falsifier does not prove migrations, DB usability, or room creation. | **INFERRED —** Use separate processes: positive `initialize -> valid session/new -> DB migration/write/read -> shutdown`; negative `initialize -> wrong-CWD session/new -> asserted error -> EOF`. Delete `schema.sql` against the positive path. Enrol this oracle in `npm run gates`/CI rather than leaving it as a one-time manual run. |
| 6 | P1 | **OBSERVED —** The allow-list algorithm does not close over config string references, generator inputs, CI consumers, or legal/provenance inputs. | **OBSERVED —** `vitest.config.ts:7,13-17` names `vitest.setup.ts` and three `tests/setup/*.ts` files by strings. `generate-agent-files.mjs:14-19` reads four `docs/agents/*.md` inputs and generates `CLAUDE.md`, `AGENTS.md`, and `GEMINI.md`; `package.json:41` runs its gate. These files are absent from spec module map `:131-152`. **UNVERIFIED —** `.github/workflows/gates.yml` and `.gitattributes` are also absent from S3/S4, despite Windows CI and enforced LF behaviour. | **INFERRED —** A filtered repo can fail test collection or the agent-file gate, or silently lose CI/legal safeguards while local gates still appear green. | **INFERRED —** Derive closure from imports plus `readFile*`, `new URL`, `include_*`, config entry arrays, spawned paths, generated-input/output maps, packager copy maps, workflows, attributes, licences, and fixtures. Commit an exact root allow-list and a rejection test for unclassified tracked files. |
| 7 | P1 | **OBSERVED —** “Trim workspace members to 14” is insufficient Cargo surgery even after correcting the five enabled dependencies. | **OBSERVED —** Current metadata reports `workspace_members=83`. `xai-grok-pager/Cargo.toml:93-120,170-182` declares numerous optional and development path/workspace dependencies outside the 14; `zer0-v2-bin/Cargo.toml:44-46` has a `ptyctl` path dev-dependency. `Cargo.lock` contains `1306` package entries, including excluded local crates. S4 regenerates only `package-lock.json`, not `Cargo.lock`. | **INFERRED —** Removing the excluded manifests can break `cargo metadata` and tests even when the selected production feature graph builds; the lockfile can also remain stale or be silently rewritten. | **INFERRED —** Inventory and prune every dependency, feature, target, benchmark, example, and dev-dependency in the retained manifests; regenerate `Cargo.lock` once; require `cargo metadata --locked`, `cargo test --locked`, and the target release build. |
| 8 | P1 | **OBSERVED —** S0 omits the entire Rust baseline and the history tool it later requires is unavailable. | **OBSERVED —** S0 at spec `:204` runs only Node commands, while P1 at `:65-67` requires Rust. Command output: `python --version -> Python 3.13.7`; `git filter-repo --version -> git: 'filter-repo' is not a git command`; `python -m pip show git-filter-repo -> Package(s) not found`. `git filter-branch -h` warns about mangled-history gotchas. | **INFERRED —** Rust/toolchain failures discovered in S4 cannot be distinguished from pre-existing failures, and execution stops halfway through S4. | **INFERRED —** Extend S0 with exact Node/Rust SHA and tree assertions, Cargo metadata/test/build, binary-sidecar smoke, and a pinned `git-filter-repo` preflight. Python is present, so `git-filter-repo` remains the right history tool; `filter-branch` is not the fallback. A vetted squash is the safer alternative if history is not required. |
| 9 | P1 | **OBSERVED —** S4 does not pin the Node revision it filters. | **OBSERVED —** Spec `:208` says only `git clone`. `git remote show origin` reports `HEAD branch: wip/w5-loop`, while the inspected checkout is `codex/v2-room-layer` at `0a041fb6a597b880ece41d4f62de909d89907352`. | **INFERRED —** A local-path clone and an origin clone can begin from different histories without violating the written instruction. | **INFERRED —** Clone/fetch explicitly, detach at the full SHA, assert its tree ID, and record that immutable input before filtering. |
| 10 | P1 | **OBSERVED —** The release assembly contract does not ensure that the Rust binary and Node launcher are co-located. | **OBSERVED —** `crates/zer0-v2-bin/src/host_process.rs:1047-1067,1119-1134` finds `zer0-v2-host.mjs` beside the executable and invokes bare `node.exe`. `package-zer0-v2-sidecar.mjs:59-60` writes the launcher beside the caller-supplied release directory. P1/S4 defines no target-directory or copy step joining those outputs. | **INFERRED —** Cargo build and sidecar packaging can each pass while the packaged product fails at its first host launch. | **INFERRED —** Add one idempotent release command that builds Rust, stages the sidecar beside the exact executable, asserts all expected paths, and launches that assembled artifact from a clean temporary project. Declare whether Node is an external prerequisite or shipped runtime. |
| 11 | P1 | **OBSERVED —** The clean-Windows prerequisite matrix is incomplete. | **OBSERVED —** `better-sqlite3/package.json:33` uses `prebuild-install || node-gyp rebuild`; `node-pty/package.json:41-42` uses `prebuild.js || node-gyp rebuild` plus postinstall. The sidecar test uses `install:false` at `package-zer0-v2-sidecar.test.mjs:35-39`. `agy-pty-spawn.ts:16-25` requires `%LOCALAPPDATA%\agy\bin\agy.exe` for the required Gemini/AGY lane. Current commands report Node `v24.18.0 abi=137`, while `where cl` and `where msbuild` find nothing. | **UNVERIFIED —** Whether available prebuilds cover the supported Node/Windows matrix; absent coverage, the staged `npm ci --omit=dev` fails without Visual Studio build tools. **INFERRED —** A clean machine also cannot complete P2 without AGY installation/authentication. | **INFERRED —** Run a clean Windows VM matrix for every supported Node architecture, with an empty npm/Cargo cache; document network, AGY, authentication, Rust, Git, Python, and native-build prerequisites before S4. |
| 12 | P1 | **OBSERVED —** S6 names a V2 lifecycle that does not exist and lacks the data required to construct `DigestRequest`. | **OBSERVED —** `DigestRequest` requires `projectId` at `digest-runner.ts:20-25`. `AliveRoomHost.create` receives it but retains only the liveness lock at `room-host.ts:116-131`. `session/new` and `session/load` both require no attached room at `zer0-v2-host.ts:292-315,416-419`; there is no V2 “session swap.” Explicit shutdown is `:367-370`; EOF cleanup is `:437-450`, but S6 names only shutdown and swap. | **INFERRED —** The slice cannot be implemented as written; implementations will either invent project resolution, miss EOF closure, or duplicate/misattribute digest dispatches. | **INFERRED —** Define an attached-session state containing session ID, project ID, DB path, and repo root. Route explicit shutdown, EOF, and any deliberately added replacement operation through one idempotent `closeAttachedSession(reason)` owner. Run catch-up only after project scope is resolved. |
| 13 | P1 | **OBSERVED —** The compiled digest entry requires unmentioned path and packager changes. | **OBSERVED —** `digest-runner.ts:30-34` calculates a three-parent source `REPO_ROOT`; from compiled `dist/src/memory` that resolves to the staged `dist/`, while `package-zer0-v2-sidecar.mjs:44-48` puts `scripts/` and `node_modules/` one level above. `assertProductionJavaScript` is applied only to the host and launcher at packager `:40-42,59-61`; the sidecar test `:35-85` has no digest assertion. | **INFERRED —** A wrong or TypeScript-dependent digest child can pass packaging and then fail only after session closure. | **INFERRED —** Resolve the compiled child directly relative to `import.meta.url`, remove all source-root/TSX assumptions, scan every staged production JS file, and run the digest through the packaged launcher in an integration test. |
| 14 | P1 | **OBSERVED —** The trust-boundary statement is factually false and violates the imposed Zod invariant. | **OBSERVED —** Spec `:103-109` claims existing Zod schemas in `zer0-v2-rpc.ts`; that file imports no Zod. `room-protocol.ts:25-83` uses `JSON.parse` plus manual guards. `digest-run.ts:98-109` manually parses `process.argv`; `zer0-v2-host.ts:442-443` reads raw environment variables and `:572` parses a file. | **INFERRED —** External stdio, argv, environment, and file inputs cross the standalone boundary without the mandated schemas; malformed values can follow validation paths the spec never tests. | **INFERRED —** Write failing invalid-input tests first, then add Zod schemas at JSON-RPC, environment, digest-argv, and JSON-file boundaries. Preserve the existing frame-size and exact-key protections inside or adjacent to those schemas. |
| 15 | P1 | **OBSERVED —** The spec directly contradicts the 250-line modularity requirement. | **OBSERVED —** Spec `:154-155,164` preserves a 500/600 line gate and says only two files shrink. Counting the 181-file closure returned `over_250=43`; retained examples include `room-engine.ts=598`, `room-recovery.ts=596`, `room-host.ts=596`, and `zer0-v2-host.ts=590`. | **INFERRED —** The implementation cannot satisfy both the approved spec and the operator’s non-negotiable limit; this is not a local cleanup. | **INFERRED —** Revise the specification before implementation and define decision-complete module boundaries plus a 250-line enforced gate. Do not conceal the required decomposition inside S4. |
| 16 | P1 | **OBSERVED —** Structured logging and recoverable error feedback are not part of the plan. | **OBSERVED —** `shared/logger.ts:198-207` emits human text (`[timestamp] [LEVEL] [context] ...`), not JSON. `package-zer0-v2-sidecar.mjs:166-169` ends with `console.error(error)` and no recovery instruction. `zer0-v2-rpc.ts:221-222` emits a raw stack to stderr. | **INFERRED —** Failures remain hard to correlate and violate both the structured-logging and user-recovery invariants. | **INFERRED —** Emit one JSON object per event with correlation/session/request IDs, stable error codes, safe causes, and explicit recovery actions; add a test that packaging failures state what failed and how to recover. |
| 17 | P1 | **OBSERVED —** The retained `session/list` contract violates the required cursor-pagination pattern. | **OBSERVED —** `zer0-v2-host.ts:257-264` rejects every non-empty cursor; `:280-289` sorts and slices to `ROOM_SESSION_ROW_LIMIT` without a continuation token. No slice changes this behaviour. | **INFERRED —** Older sessions become unreachable once the limit is exceeded, while a shallow test still passes. | **INFERRED —** Add an opaque, Zod-validated cursor over the deterministic `(updatedAt, sessionId)` order and test multiple pages, ties, invalid cursors, and replay. |
| 18 | P1 | **OBSERVED —** The implementation sequence is not test-first. | **OBSERVED —** S6 at spec `:210` orders entry, runner, and host edits as `(a)-(c)`, then tests as `(d)`; S7 at `:211` similarly names implementation before falsification. | **INFERRED —** The plan violates the explicit “failing test before implementation” contract and permits tests to be shaped around completed code. | **INFERRED —** For every slice, commit or retain the failing acceptance/regression test output first, then implement, then rerun the same test unchanged. |
| 19 | P1 | **OBSERVED —** The proposed provenance record is stale before extraction begins, and public-history safety is ungated. | **OBSERVED —** `sources.lock.json:6-21` records Node `b3186df...` and Rust `a422116...`; inspected inputs are Node `0a041fb...` and Rust branch `82d25249...`. Spec `:234-235` says to carry the old record. P5 `:76-77` checks history only for `src/tui` and one surviving file; it does not scan the other banned paths, Rust history, secrets, authors, or historical licences. | **INFERRED —** The debut can publish false attribution or unwanted historical material while every written postcondition passes. | **INFERRED —** Generate a new immutable provenance manifest mapping source commit/tree IDs to filtered outputs and licence hashes. Run banned-path, secret, identity, large-blob, and licence-history audits before choosing filtered history versus squash. |
| 20 | P1 | **OBSERVED —** S2’s destructive V1 precondition is stale. | **OBSERVED —** Spec `:206` says “commit the 11 dirty files.” Current `git status --short` at `C:\Users\<user>\VibeCoding\zer0-agent-ci` returned `12` untracked entries; `git tag -l v1-ink-final` and `git remote` returned nothing. | **INFERRED —** Blindly following S2 can freeze or publish unreviewed files, and its remote/tag proof cannot currently run. Its claimed serial dependency on S4 is also unsupported because S4 filters the separate D: Node source. | **INFERRED —** Re-inventory and approve every V1 path before committing. Treat the V1 freeze as an independent safety checkpoint, not a technical prerequisite for functional extraction. |
| 21 | P1 | **OBSERVED —** Several postconditions can pass without proving the operator’s outcome. | **OBSERVED —** P2 `:68-69` and S5/S8 accept “operator confirmation in words” without asserting three identified replies or ledger deltas. P6 `:78-79` accepts any one new row without a watermark/content/replay check. P7 `:80-81` can pass without first opening PTYs and tracking owned PIDs. P8 `:82-83` says “byte-identical except for commits,” an undefined comparison that includes changing Git metadata. P5 checks only one doomed-history path. | **INFERRED —** A non-engineer can be shown green commands while persistence, agent participation, PTY cleanup, or repository cleanliness is still broken. | **INFERRED —** Produce one machine-readable acceptance artifact keyed by a unique correlation/session ID: three named replies, matching ledger rows, digest watermark/content and replay proof, opened/closed PTY PID set, exact source/output SHAs, and a pre/post hash manifest with explicit exclusions. |
| 22 | P2 | **OBSERVED —** “`compileMemory` is called only inside `buildPrompt`” is false. | **OBSERVED —** `prompt-builder.ts:27,353` calls it, but `temporal/activities/prepare-context.ts:29,264` also imports/calls it. The current V2 module path is `host -> headless-turn.ts:38 -> prompt-builder -> memory-compiler`, although V2 calls only `estimateTokens`; no closure caller of `buildPrompt` was found. | **INFERRED —** The extraction conclusion survives only because `prepare-context` is outside the closure and all four `estimateTokens` imports are rewired; the stated proof is still unreliable. | **INFERRED —** Replace the claim with the narrow, checkable statement: no retained V2 caller invokes `buildPrompt` or `compileMemory`, and the four static prompt-builder imports are removed before filtering proof is accepted. |
| 23 | P2 | **OBSERVED —** The spec disagrees with itself about the observability count. | **OBSERVED —** Module map `:143` says 10 observability schema files; S3 `:207` says 11. A current non-test scan found 11 when the barrel is counted, but the intended convention is unstated. | **INFERRED —** Two implementers can produce different allow-lists and both claim compliance. | **INFERRED —** Enumerate the exact paths and whether the barrel counts; never use an unexplained count as the allow-list source. |
| 24 | P2 | **OBSERVED —** The blanket blame-preservation claim is false for the seam merged into an existing file. | **OBSERVED —** Spec `:142,208,224-226` merges `src/tower/types.ts` into existing `src/chat/types.ts` and claims `--follow` preserves seam history. `git log --follow` shows independent histories beginning at `e1a1904...` and `978d202...`; `--follow` handles renames, not copied symbols merged into another file. | **INFERRED —** The stated reason for filtered history over squash is overstated. The rejection of “subtract in place” is valid for mutating D:, but does not distinguish a disposable scratch-clone subtraction from filtering. | **INFERRED —** Verify blame per seam, record source-path provenance for merged symbols, and decide filtered history versus a vetted squash only after the public-history audit. |
| 25 | P3 | **OBSERVED —** This severity band is empty. | **OBSERVED —** No cosmetic-only defect was needed to reach the verdict. | **OBSERVED —** None. | **OBSERVED —** None. |

- **VERDICT: REJECT** — **INFERRED** — The false Rust closure, missing protocol import, contradictory fixture policy, and non-falsifying oracle make the written fresh-clone proof impossible.
- **The single biggest risk** — **OBSERVED** — The plan treats 14 Rust crates as complete even though Cargo and the compiler dependency file prove five additional enabled local crates under directories P4 requires deleting.
- **The one thing you would reorder or add** — **INFERRED** — Build and fully prove a history-free standalone candidate first, including Node gates and Cargo metadata/test/build; only then replay the verified file set through filtered Node and Rust histories.
- **What you could not determine** — **UNVERIFIED** — Native installs, Git dependencies, and the live three-agent path on a genuinely clean Windows machine; settle them on the generated candidate with `npm ci && npm run gates && npm run build && npm run package:zer0-v2-sidecar <dir>` and `cargo metadata --locked && cargo test --locked && cargo build --locked --profile release-dist -p zer0-v2-bin`, followed by the packaged live turn.
- **Confidence** — **INFERRED — GREEN (97%)** — The rejection rests on source, manifest, `.d`, Cargo-tree, and Git-object evidence; both inspected worktrees remained unchanged, while the missing 3% is the clean-VM/live execution that cannot exist until the candidate is specified correctly.